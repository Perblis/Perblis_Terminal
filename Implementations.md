# Implementations — agent progress log

Append-only handoff log so any Claude/agent instance can resume mid-stream.
Skim **Current status** first, then the newest log entries. The entry format and
the session-start protocol that drives this file live in `CLAUDE.md`.

---

## Current status — _keep this section current_

**Wave:** Waves 0–6 **COMPLETE, merged to `main`, FOUNDER-SIGNED-OFF** (Waves 4+5 2026-06-21; Wave 6 2026-06-22, PR #37); backend deployed on Railway (auto-deploy from `main` to the **natural-cat** project). **Wave 7 (Supplier Portal) is BUILT and MERGED to `main`** — founder go 2026-07-03, slices 7-0 → 7E on PR #38 (merged), plus founder-driven fix PRs **#39/#40 (photo upload BFF proxy, photo DELETE, yards/location UX) and #42 (photo display, yards-on-Assets, nav logout)**. P1–P12 built, BFF cookie auth, vitest + Playwright smoke green, portal CI has an e2e job. **Wave 7 is NOT yet signed off** — still open: founder direction check (wordmark, P1 photography, /styleguide), prod-demo exit criterion (J1 under an hour + J6 rhythm) on the deployed Worker, founder sign-off. The Cloudflare Workers deploy is **largely closed 2026-07-06** — a Workers Builds Git integration now auto-builds `terminal-portal` on push (branch previews; first seen green on PR #43); confirm the production Worker serves `main` against natural-cat. Wave 7 portal surfaces freeze at sign-off, not before. Backend suite green (**503 tests**; coverage ≥70% overall / ≥85% hires+payments held); slice 7-0 added 5 additive endpoints + 2026-07-06 added `DELETE /api/v1/listings/{id}/photos/{photo_id}` — OpenAPI regenerated and committed both times, all frozen Wave 2–5 contracts byte-untouched.

- **Built:** backend `core` + Wave 1 `accounts` + Wave 2 `suppliers`/`listings` + Wave 3 `search` + **Wave 4 `hires` + `payments`**. Wave 4 (slices 4A–4F, PR #27): `hires/fees.py` (FSD §3.1 vectors), `hires/state.py` (sole status writer, append-only `HireEvent`), `availability.py` (soft vs hard holds, `SELECT FOR UPDATE` race rule), request/accept/decline/cancel APIs with **D-014** role-shaped serializers + Basic cap, §7.6 refunds, idempotent sweeps + handovers + disputes, payouts (+ D-015 withheld-day) + reconciliation + FSD §9 email notifications. Closed the Wave-3 `available` stub (real availability in search). Migrations: hires 0001, payments 0001–0002.
- **Payments provider:** **Paystack** (collect-only) is the MVP provider per **D-018** (supersedes D-017's Bachs.io), selected behind `payments.gateway` (`PAYMENT_PROVIDER=paystack|bachs`, default paystack; Bachs adapter retained). Integer-kobo native; webhook `charge.success` HMAC-SHA512 of raw body w/ secret key; verify-before-transition via `/transaction/verify/{ref}`; `callback_url` → portal (`PAYSTACK_CALLBACK_URL`, PR #31). PRs #30 (switch) + #31 (callback) merged.
- **Ops Console (admin):** **Wave 6 done, merged (PR #37) & signed off**. `ops` app: `OpsAdminSite`/`OpsOTPAdminSite` (django-otp TOTP, prod-gated by `ADMIN_2FA_REQUIRED`), dashboard (`ops/services/metrics.py` + `templates/admin/ops_index.html`), `ReconciliationRun` model + read-only admin, weekly digest (`ops/services/digest.py`, `ops/tasks.py`) with real Ably stats + best-effort R2, `enroll_totp`/`run_daily_reconciliation`/`run_weekly_digest` commands. Queues live in their apps' admins: payout queue (`payments/admin.py`), listings/reports moderation (`listings/admin.py`), hires + dispute resolution (`hires/admin.py`), users/suspension (`accounts/admin.py`). New services: `accounts/services/moderation.py` (suspend/reactivate/strikes), `listings/services/moderation.py` (pause/remove/award-tier), `listings.state` `remove` transition, `payments.services` freeze/unfreeze/freeze_supplier_payouts + payout-paid email. Every admin mutation writes a Django `LogEntry`; domain transitions keep writing `HireEvent`. Migrations: ops 0001, accounts 0006 (`internal_notes`).
- **Deploy:** self-hosted on the founder VPS (D-027, 2026-08-07). Stack at `/opt/terminal/` — api **https://terminal-api.lab.perblis.com** (`/healthz`+`/readyz` green), portal **https://terminal.lab.perblis.com**, loopback `127.0.0.1:8100`/`8101`. Secrets in Infisical `perblis-terminal` prod (`/backend` + root compose keys) and `/opt/terminal/.env`. `/etc/cron.d/terminal` installed. **Railway natural-cat remains live as fallback** until Phase-6 teardown. **Deploy-only carry-over:** Paystack dashboard webhook → `https://terminal-api.lab.perblis.com/api/v1/payments/webhook`; **Railway `DATABASE_URL` (public/proxy form)** needed for one-time `pg_dump` → VPS restore (founder-provided Railway env had an unresolved `${{PostGIS.*}}` template).
- **Decisions since specs:** D-017 → Bachs.io; **D-018 → back to Paystack, pluggable** (current); **D-019/D-020** bespoke token-driven portal components + unstyled headless allowlist (shadcn/ui superseded); **D-021** corner-bracket panel motif. D-014 (fee confidential), D-015 (withheld-day payout), D-002/004/005/006/008 all enforced in `hires`/`payments`.
- **Supplier Portal (`portal/`, Wave 7):** Next.js 15 App Router + @opennextjs/cloudflare, BFF cookie auth (`/auth/*` mints httpOnly pair; `/bff/*` adds Bearer w/ single-flight refresh), P1–P12 per `ux/03` (auth/OTP, dashboard, assets table, 6-step asset form, yards w/ MapLibre, hires P6/P7/P8 incl. LockedTerms + refund-preview manifest + CalendarGantt, messages, storefront + public `/s/[id]` w/ OG card, settings w/ bank vault, error pages), Ably realtime + 15s-polling fallback, Sentry keyless-degraded. Media uploads and public media now proxy through the BFF (`/bff/media-put`, `/bff/media/public`) — **R2 bucket CORS is no longer required for the portal**. Playwright smoke 4/4 green (register→OTP→activation · full publish incl. photo upload · accept w/ ack · refund-preview figures == API).

**Next — close Wave 7 wave-end:** founder direction check → Cloudflare Workers deploy → prod-demo exit criterion → founder sign-off (then freeze Wave 7 contracts + record it here). **Founder-approval queue (additive backend, not yet approved):** `hire_id` on ConversationSerializer (P9 hire side-strip), hirer-card fields on the supplier-shaped HireSerializer, yard-scoped stats (per-yard dashboard tabs), payout day-series (30-day sparklines) — photo DELETE from this queue already shipped 2026-07-06. **Wave 8 (Hirer app) remains gated on explicit founder approval** (backend deps met; the gate is binding, design.md §7). Deploy-only carry-over still open from Wave 4: set the **Paystack dashboard Webhook URL** → `https://api-production-101c8.up.railway.app/api/v1/payments/webhook`.

**Carry-over follow-ups (non-blocking):**
- **Wave 3:** `badge` has two meanings (documented in `search/services/common.py`) — solo-pin/list-row `badge` = listing trust tier; yard `supplier.badge` = account verification. Founder may override before downstream (Wave 8) consumes the frozen contract. The `available` flag is a Wave-3 stub (always `true`) — **Wave 4 §4B wires the real availability engine** and removes the stub note from the search OpenAPI schema.
- Set **`LOCATIONIQ_KEY`** in the natural-cat prod env so geocode (and the portal's location text-search) returns results. Backend CORS is **not** needed for the portal BFF (server-to-server); `CORS_ALLOWED_ORIGINS` only matters if a browser client ever calls the API directly.
- `accounts/integrations/email.py::send_otp_email` copy still reads "verify your phone" / "fallback" — should read "verify your email". One-line copy fix.
- Wave 2 DEFERRED (founder call): "Other (describe)" asset type → Ops review NOT built (unknown types rejected with `invalid_asset_type`; Ops surfaces are Wave 6). Photo orphan-sweep task is a logged no-op until an upload ledger / R2 lifecycle policy lands.
- Wave 0/7 portal-deploy item, **largely closed 2026-07-06**: a **Cloudflare Workers Builds Git integration** is now connected for `terminal-portal` (auto-builds on push; branch preview URLs — first seen green on PR #43, preview `claude-session-start-1dp8vs-terminal-portal.nwabueze.workers.dev`), so manual `pnpm deploy` is no longer the path. Still to confirm: the production Worker serves `main` end-to-end against natural-cat (`API_BASE_URL` in `wrangler.toml`) — exercised by the Wave 7 prod-demo exit criterion.
- Portal local-run gotcha: `seed_e2e` re-runs can silently fail on `availability_conflict` once prior runs consumed the +7..+9 window — reset the DB (CI is always fresh). A 409 on listing save = archived listing (`listing_not_editable`).
- Termii SMS sender approval (above) for the real phone-OTP flow.

**Local test-DB:** PostGIS via `docker compose up -d`; `pytest -x` (coverage gates: 85% hires/payments, 70% overall). **Live (current auto-deploy = natural-cat):** https://api-production-101c8.up.railway.app/healthz (earlier project `perblisterminal-production` may also still be up).

---

## Log — _append new entries at the bottom_

## 2026-06-15 00:00 (local) - Railway MCP install for Cursor
- tag: CHORE
- area: Cursor MCP config (C:\Users\nwabu\.cursor\mcp.json)
- summary: Ran `railway mcp install --agent cursor` to register Railway's local stdio MCP server in Cursor.
- reason: User requested Railway MCP agent setup for Cursor.
- notes: Restart Cursor for the MCP server to register. Uses local CLI-backed server (`railway mcp`), not remote OAuth server.

## 2026-06-16 00:20 (local) - Railway setup agent for workspace
- tag: CHORE
- area: Railway CLI agent tooling (skills + MCP); D:\Projects\Terminal
- summary: Ran 
ailway setup agent -y from project root. Installed use-railway skill to Universal (.agents), Claude Code, Factory Droid, and Cursor; configured MCP for Claude Code and Factory Droid. Cursor MCP already present in mcp.json from prior install.
- reason: User confirmed full Railway agent tooling setup for Cursor and related editors.
- change_ref: 2026-06-15 00:00 (local) - Railway MCP install for Cursor
- notes: CLI reported already logged in (nwabueze.finance@gmail.com). Restart Cursor recommended to load skills and MCP. Cursor railway MCP: command railway, args mcp.

## 2026-06-16 (local) - Railway failed deploy log gather (resplendent-gentleness)
- tag: CHORE
- area: Railway project resplendent-gentleness; Perblis_Terminal + Worker services
- summary: Pulled build/deploy logs for latest FAILED deployments via CLI (44322e9a main, d21b1fe8 worker). Docker build succeeded; deploy failed on gunicorn `` literal and healthcheck; reported to user for manual fix.
- reason: User requested failure logs only�no redeploy or config changes.
- notes: Awaiting user before continuing deployment.

## 2026-06-16 01:10 (local) - Railway deploy fix (resplendent-gentleness)
- tag: FIXED
- area: backend/railway.json, backend/railway.worker.json, backend/Procfile, backend/Dockerfile, backend/settings/prod.py; Railway services Perblis_Terminal + Perblis_Terminal Worker
- summary: Fixed PORT expansion (`/bin/sh -c` + `${PORT:-8000}`), split shared vs per-service Railway config (removed startCommand/healthcheck from railway.json; set web/worker start commands via Railway environment config), added railway.worker.json, hardened Dockerfile CMD fallback, disabled SECURE_SSL_REDIRECT default for Railway internal HTTP healthchecks. Deployed web + worker via `railway up` from repo root.
- reason: Gunicorn failed with literal `$PORT`; railway.json startCommand overrode worker dashboard config; healthcheck failed on internal HTTP due to SSL redirect.
- change_ref: 2026-06-16 (local) - Railway failed deploy log gather (resplendent-gentleness)
- notes: Live https://perblisterminal-production.up.railway.app/healthz returns 200. Worker SUCCESS on db_worker. Push local changes to GitHub for durable auto-deploy; set worker service config file to `/backend/railway.worker.json` in dashboard if git-only worker deploys regress.

## 2026-06-16 04:20 (local) - Railway PostGIS wiring and deploy follow-up
- tag: CHORE
- area: Railway resplendent-gentleness; Perblis_Terminal + Worker + PostGIS; backend/railway.json, railway.worker.json, Dockerfile, Procfile, settings/prod.py
- summary: Verified PostGIS connected to web and worker via `DATABASE_URL` (`postgis://` + `postgis.railway.internal:5432`) on both services per DEPLOY.md. Production probes pass (`/healthz`, `/readyz` database ok; worker `db_worker` running). Restored web `startCommand`/`healthcheckPath` in `railway.json`; added `railway.worker.json` for worker-only start. Redeployed web from local fixes after env-var change triggered failed main-branch deploy (`$PORT` literal).
- reason: User requested Railway deployment follow-up � DB wiring verification, endpoint tests, wave-zero check, and PR for durable git deploy.
- change_ref: 2026-06-16 01:10 (local) - Railway deploy fix (resplendent-gentleness)
- notes: Portal Cloudflare Workers deploy still pending (wave-0 exit partial). Worker `railway.worker.json` config path should be set in Railway dashboard when git auto-deploy lands. R2/Bachs integrations `not_configured` in readyz (expected).

## 2026-06-16 04:45 (local) - Git branch push for Railway deploy PR
- tag: CHORE
- area: git branch deploy/railway-wave0-followup; .gitignore
- summary: Pushed deploy fixes to `origin/deploy/railway-wave0-followup` (commits 0150715, 4a12ba3, 127386d). Removed tracked `.cursor/settings.json`; added `.cursor/` to `.gitignore`. Production redeployed SUCCESS from repo root for web + worker.
- reason: Durable git-based auto-deploy on merge to main; PR workflow per user request.
- change_ref: 2026-06-16 04:20 (local) - Railway PostGIS wiring and deploy follow-up
- notes: `gh` installed but not authenticated � user must `gh auth login` then create PR, or open compare URL manually.

## 2026-06-16 06:30 - Session-start protocol + progress-tracking convention
- tag: CHORE
- area: CLAUDE.md, Implementations.md
- summary: Updated CLAUDE.md "Current state" to reflect Wave 0 built + backend deployed (was stale "pre-build"). Added a "Session start protocol" (read Implementations.md, study design.md/docs, derive precise wave status from code not just docs, confirm the founder-approved next wave) and a "Tracking progress" section defining the Implementations.md entry format. Gave Implementations.md a self-describing header + a maintained "Current status" snapshot.
- reason: User wants every new session to scan/understand the codebase, pin down exactly which wave is done and what's next, and use Implementations.md as the durable handoff log.
- change_ref: 2026-06-16 04:45 (local) - Git branch push for Railway deploy PR
- notes: Verified in code: only core+accounts have models (User mig 0001); domain apps empty; no /api/v1/ endpoints — i.e. Wave 0 only. Branch docs/session-protocol → draft PR. Next instances: keep "Current status" current and append here per change.

## 2026-06-16 07:30 - Wave 1: accounts identity, auth, verification
- tag: FEATURE
- area: backend/accounts (models, services/, integrations/, views, serializers, urls, admin, tasks, tests), settings/base.py, core/urls.py, backend/openapi/schema.yml, .env.example, pyproject (boto3+httpx)
- summary: Implemented Wave 1 end-to-end. Models: User gains full_name, NDPR consent timestamps, token_version, phone_verified_at, purged_at; new OtpCode, VerificationRequest (one-pending-per-kind partial-unique), PasswordResetToken. Auth: register (hirer/basic + consent) → Termii OTP (HMAC-hashed, 10-min, 3 resends/h, 5 attempts) → JWT login (60m access / 7d rotating refresh, custom claims) with IP failure-lockout (5/15min) + suspended/deleted/phone-not-verified guards; refresh re-validates user + tv; logout blacklists refresh; no-enumeration password reset that bumps token_version (kills live access+refresh). me GET/PATCH, activate-supplier. Verification: direct-multipart upload → private R2 (local-disk fallback) with 15-min presigned/Ops-only access; Django admin review queue (pending-first, doc links, approve→level upgrade + notify, reject→reason + notify, resubmit). DELETE me soft-delete + active_hire_guard stub; daily purge task scrubs PII after 30d, retains verification records (NDPR), idempotent. Custom TerminalJWTAuthentication enforces tv.
- reason: Founder approved starting Wave 1 (D: 2026-06-16). Builds the identity foundation Waves 2/4/7/8 depend on; contract freezes at wave end.
- change_ref: 2026-06-16 06:30 - Session-start protocol + progress-tracking convention
- notes: All gates green locally — 80 tests / 93% cov (gate 70%), ruff+format, mypy, makemigrations --check, .env.example. OpenAPI committed at backend/openapi/schema.yml (auth/* + me + verification, frozen). Dev integrations only — set Termii/Resend/R2 keys in Railway for the prod demo. SPEC GAPS flagged in PR: DELETE /api/v1/me missing from TSD §3.8; added User columns (full_name, phone_verified_at, consent, token_version, purged_at); new deps boto3+httpx; dedicated PasswordResetToken model; direct-multipart verification upload (generic presign deferred to Wave 2); login-lockout(failures) vs login throttle(requests) are complementary; prod needs a shared cache (DB-cache) for multi-worker lockout. Tests use local PostGIS — env injects a remote Supabase DATABASE_URL that can't build a test DB; override DATABASE_URL to localhost when running tests/migrations locally.

## 2026-06-16 10:50 - TSD reconciliation for Wave 1 deltas
- tag: DECISION
- area: docs/v2/07_TSD.md (§3.3 schema, §3.8 inventory)
- summary: Closed the spec gaps flagged when Wave 1 merged. §3.8 inventory now lists DELETE me (soft-delete + active_hire_guard). §3.3 users gains full_name, phone_verified_at, consent timestamps, token_version, purged_at; otp_codes notes HMAC code_hash + consumed_at; added password_reset_tokens; verification_requests gains rc_number/decided_at + one-pending-per-kind. Added a "Wave 1 build notes" callout: login throttle(5/min)+failure-lockout(5/15min) split, tv-claim session invalidation, direct-multipart verification upload (presign deferred to Wave 2), prod shared-cache note, boto3+httpx deps.
- reason: Founder requested reconciling the implemented-vs-spec deltas in the docs (PR #7 follow-up item).
- change_ref: 2026-06-16 07:30 - Wave 1: accounts identity, auth, verification
- notes: PDF mirrors in docs/v2/pdf/ NOT regenerated (scripts/md2pdf.py) — run if the PDF snapshot matters. No code change.

## 2026-06-16 12:45 - Wave 1 test run + bcrypt dependency fix
- tag: FIX
- area: backend/pyproject.toml, backend/uv.lock
- summary: Ran full Wave 1 test suite locally (80 tests, 93% cov, ruff/mypy/migrations green). Found runtime registration broken in dev/prod settings: `BCryptSHA256PasswordHasher` requires the `bcrypt` package but it was missing from dependencies (tests masked this via MD5 hasher). Added `bcrypt>=4.2`; verified register → OTP verify → login smoke test passes.
- reason: User requested Wave 1 tests and functional verification.
- change_ref: 2026-06-16 07:30 - Wave 1: accounts identity, auth, verification
- notes: Local test env needs PostGIS (`DATABASE_URL=postgis://postgres:postgres@localhost:5432/terminal`). Integration keys (Termii/Resend/R2) optional in dev — console/Mailpit fallbacks work. PR: cursor/wave1-test-bcrypt-fix-5d9a.

## 2026-06-16 12:20 - OTP email fallback when Termii absent
- tag: FIX
- area: accounts/integrations/email.py, tasks.py, services/otp.py, settings/base.py, .env.example
- summary: When Termii is not configured (SMS console fallback), OTP is now also emailed via Resend so register→verify works without SMS keys. Updated DEFAULT_FROM_EMAIL default to `contact@perblis.com` (verified in Resend; perblis.io is not).
- reason: User received welcome email but no OTP code — SMS-only delivery with no Termii key.
- change_ref: 2026-06-16 07:30 - Wave 1: accounts identity, auth, verification
- notes: Production still uses Termii SMS when `TERMII_API_KEY` is set; email is fallback only. R2 + Resend verified working with founder keys. PR: cursor/otp-email-fallback-5d9a.

## 2026-06-16 12:30 - Review merged Cursor PRs (#9 bcrypt, #10 OTP email) + TSD note
- tag: CHORE
- area: docs/v2/07_TSD.md (§3.8 build notes), Implementations.md
- summary: Reviewed two merged Cursor PRs against main. #9 (add bcrypt dep) — correct, necessary: BCryptSHA256PasswordHasher needs bcrypt at runtime; tests masked it via MD5. #10 (email OTP when Termii absent) — sound, tested, OpenAPI unchanged (internal task signature only). Validated main: 81 tests green, schema unchanged. Updated the TSD §3.8 build-notes callout to document the OTP email fallback + the prod caution (must keep TERMII_API_KEY set or phone-channel verification silently degrades to email) and bcrypt as a runtime dep.
- reason: Founder asked to review the externally-merged changes and update affected docs.
- change_ref: 2026-06-16 10:50 - TSD reconciliation for Wave 1 deltas
- notes: No code change. OpenAPI not regenerated (no contract drift). Possible future hardening: warn/refuse in prod when TERMII_API_KEY is unset rather than silently emailing OTPs — flagged to founder.

## 2026-06-16 13:20 - Two-channel verification (separate email + phone OTP)
- tag: FEATURE
- area: backend/accounts (models, enums, errors, services/{otp,delivery,login,registration}, views, serializers, urls, throttles, tasks, factories, tests, migrations 0004/0005), backend/openapi/schema.yml, docs/v2/06_FSD_v2.md §4.2, docs/v2/07_TSD.md §3.3/§3.8
- summary: Replaced the cross-channel "email the phone OTP" fallback (PRs #10/#12) with independent verification of each channel. Phone OTP delivered ONLY by SMS (Termii); email OTP delivered ONLY by email (Resend) — codes never crossed, so an email can't prove phone ownership. Phone delivery is loud, not silent: raises `otp_delivery_failed` (502) when SMS can't be sent (provider error, or no TERMII_API_KEY in prod); dev (DEBUG) prints to console. New: User.email_verified_at + is_email_verified; OtpPurpose.EMAIL_VERIFY; errors EmailNotVerified (403) + OtpDeliveryFailed (502); endpoints auth/email/verify + auth/email/resend (email-keyed throttle); MeSerializer.is_email_verified. Login (Basic) now requires BOTH phone and email verified (FSD §4.1). Registration issues both OTPs inline (resilient: a channel failure is logged, account still created, resend is the strict path). Removed orphaned dispatch_otp_sms task + deliver_otp.
- reason: Founder asked to verify email as well as mobile, with mobile not silently failing.
- change_ref: 2026-06-16 12:20 - OTP email fallback when Termii absent
- notes: Green locally — 91 tests / 92.85% cov, ruff+format, mypy, makemigrations --check, OpenAPI regenerated. Contract change (reopens frozen Wave 1 auth contract) — founder-requested. FSD §4.2 + TSD §3.3/§3.8 updated to match. Branch: feature/two-channel-verification.

## 2026-06-16 14:00 - Wave 1 close-out + Wave 2 handoff doc sync
- tag: CHORE
- area: CLAUDE.md (current-state), docs/waves/README.md (status column), Implementations.md (current status)
- summary: Marked Wave 1 COMPLETE (PRs #7–#13 merged: accounts incl. independent email+phone OTP, both required for login) across the handoff docs, and Wave 2 as the founder-approved next wave. CLAUDE.md "Current state" now describes the full accounts surface (migrations 0001–0005, frozen OpenAPI, TERMII_API_KEY required in prod). waves/README.md status: Wave 0 ✅ (portal deploy still pending), Wave 1 ✅, Wave 2 🟡 approved & starting.
- reason: Founder is about to start Wave 2 in a fresh instance and asked for the handoff docs to be brought current.
- change_ref: 2026-06-16 13:20 - Two-channel verification (separate email + phone OTP)
- notes: Next instance: read Implementations.md → design.md → docs/waves/wave-2.md → FSD/TSD §5–6. Wave 1 auth contract is frozen (breaking it needs founder sign-off). Known non-blocking nit recorded: send_otp_email copy says "phone" instead of "email". Local DB for tests needs PostGIS + DATABASE_URL=postgis://postgres:postgres@localhost:5432/terminal (env injects a remote Supabase URL that can't build a test DB).

## 2026-06-17 - Wave 2 Slice 0: media presign pipeline + supplier profile
- tag: FEATURE
- area: backend/core (media.py, fields.py, encryption.py, views.py, serializers.py, urls.py, tests/test_media.py), backend/suppliers (models, services/profile, serializers, views, urls, admin, factories, migrations/0001, tests), backend/conftest.py, settings/base.py, backend/openapi/schema.yml
- summary: First Wave 2 slice. Added the cross-cutting media pipeline `POST /api/v1/media/presign` (kind-scoped presigned PUT: listing_photo/avatar/logo/verification_doc/handover_photo with per-kind content-type + size caps per TSD §3.9; public vs private bucket; stable codes media_kind_invalid/media_content_type_invalid/media_too_large) in `core/media.py`, plus dev/CI local upload+serve receivers (excluded from schema) so the round-trip works without R2. Added `core.encryption` (Fernet, FIELD_ENCRYPTION_KEY with SECRET_KEY-derived dev fallback) + `core.fields.EncryptedTextField`. Built `suppliers.SupplierProfile` (business name, description, logo_key, bank_name, bank_account_number_enc **encrypted at rest**, bank_account_name, 4 notif bools, strike_count) with `GET/PATCH /api/v1/suppliers/me/profile` (IsSupplier; bank number write-only in / masked `****1234` out; is_complete gate helper for publish). Added shared root `conftest.py` (api/auth/supplier/hirer fixtures).
- reason: Wave 2 §2.1 + §2.5 — supplier business profile with encrypted bank details and the media pipeline every later slice (logos, listing photos) consumes.
- change_ref: 2026-06-16 14:00 - Wave 1 close-out + Wave 2 handoff doc sync
- notes: Green locally — 107 tests / 91.67% cov, ruff+format+mypy clean, makemigrations --check clean, OpenAPI regenerated (0 errors). Pinned ENUM_NAME_OVERRIDES so the new media `kind` enum (MediaKindEnum) does NOT rename the frozen Wave-1 verification `KindEnum` — verified no `kind` drift vs main. Local test/dev DB needs PostGIS + `DATABASE_URL=postgis://postgres:postgres@localhost:5432/terminal` and `DJANGO_SETTINGS_MODULE=settings.test` (env injects a remote Supabase URL + config.settings.production that must be overridden). Next: Slice 1 — Yards.

## 2026-06-17 - Add "prepare for handoff" protocol to CLAUDE.md
- tag: CHORE
- area: CLAUDE.md (new "Handoff protocol" section)
- summary: Documented a repeatable handoff checklist triggered when the founder says "prepare for handoff": sync to main, reconcile FSD/TSD + regenerate OpenAPI if contracts changed, refresh Implementations.md (current status + log entry), CLAUDE.md current-state, docs/waves/README.md status column, record known follow-ups, then open a docs-only draft PR.
- reason: Founder wants the handoff doc-sync routine codified so any instance runs it on command.
- change_ref: 2026-06-16 14:00 - Wave 1 close-out + Wave 2 handoff doc sync
- notes: Added to PR #14 (docs/wave2-handoff). Documentation convention only.

## 2026-06-17 - Wave 2 Slice 1: Yards
- tag: FEATURE
- area: backend/suppliers (models.Yard, errors.YardHasListings, services/yards.py, serializers.YardSerializer, views Yard*View, urls, admin, factories.YardFactory, migrations/0002, tests/test_yards.py), backend/conftest.py (supplier2 fixture), .github/workflows/backend.yml (mypy now covers suppliers), backend/openapi/schema.yml
- summary: Added Yards (FSD §5.1): `GET/POST /api/v1/yards`, `PATCH/DELETE /api/v1/yards/:id` (IsSupplier, owner-scoped). `Yard` has a `geography(Point,4326)` GIST `point`; GeoJSON in/out via rest_framework_gis GeometryField. Delete is guarded by the listing→yard FK being PROTECT (added in the listings slice) — `yard.delete()` ProtectedError surfaces as stable `yard_has_listings`. Added `nearest_yard_within` (100m auto-yard inference helper for the listing form, FSD §5.1). Extended the CI mypy step to include `suppliers`.
- reason: Wave 2 §2.2 — supplier yards with map pins; listings attach to them next.
- change_ref: 2026-06-17 - Wave 2 Slice 0: media presign pipeline + supplier profile
- notes: Green locally — 114 tests / 91.53% cov, ruff+format+mypy clean, makemigrations clean, OpenAPI regenerated (yards paths added, no frozen-enum drift). Tests obtain introspected Yards via the service (typed return) not factory locals, to stay mypy-clean (factory_boy returns aren't typed for mypy). yard_has_listings block-path test lands with the listings slice (no Listing model yet). Next: Slice 2 — spec templates + seed.

## 2026-06-17 - Wave 2 Slice 2: spec templates + seed
- tag: FEATURE
- area: backend/listings (enums.AssetClass/SpecFieldKind, models.SpecTemplate, spec_data.py, services/{spec_seed,specs}, errors, serializers, views.SpecTemplateView, urls, admin, factories, management/commands/seed_spec_templates, migrations 0001+0002 data-seed, tests), backend/core/exceptions.py (TerminalError carries optional fields), core/urls.py (include listings), .github/workflows/backend.yml (mypy +listings), backend/openapi/schema.yml
- summary: Wave 2 §2.3. Added `SpecTemplate` (asset_class+asset_type+version uniq, fields jsonb) seeded from doc 05 §2–§6 via `spec_data.build_templates()` (36 templates: 15 plant / 9 trucks / 5 warehousing / 3 terminals / 4 land) — one ★ filterable headline per class (operating_weight·payload_capacity·floor_area·container_capacity·area). Idempotent `seed_spec_templates()` (upsert) used by both the management command and a data migration (0002). `GET /api/v1/spec-templates?class=&type=&version=` (public). `validate_specs(...)` checks field kinds/options always and required-fields when `require=True` (publish); `missing_required_specs` is the publish-gate helper; drops unknown fields; `invalid_asset_type`/`spec_invalid` codes. Enhanced `core.exceptions.TerminalError` to carry optional per-field `fields` (additive, backward-compatible) so custom-code errors surface field detail.
- reason: Wave 2 §2.3 — the spec registry listings validate against; publish gate needs required-spec checks.
- change_ref: 2026-06-17 - Wave 2 Slice 1: Yards
- notes: Green locally — 123 tests / 90.80% cov, ruff+format+mypy clean, makemigrations clean, OpenAPI regenerated (no frozen-enum drift). Run `manage.py seed_spec_templates` in prod (also applied by migration 0002). Next: Slice 3 — listings core (CRUD), which adds the listing→yard PROTECT FK that activates the yard delete-guard.

## 2026-06-17 - Wave 2 Slice 3: Listings core (CRUD)
- tag: FEATURE
- area: backend/listings (enums Listing{Status,Tier}/ReportReason, models.Listing+Unit, errors.ListingNotEditable, services/{listings,geocoding}, serializers Listing*, views Listing*View, urls, admin, factories.ListingFactory, migration 0003, tests/test_listings.py), backend/openapi/schema.yml
- summary: Wave 2 §2.4. `Listing` (+`Unit`) per TSD §3.3 — kobo pricing (daily required), specs validated + version stamped on create, location precedence yard→pin→geocode with denormalised `point`, stored `completeness_score` (not serialized). `GET/POST /api/v1/listings` (IsSupplier, mine), `GET /listings/:id` (public iff Live else owner-only 404), `PATCH /listings/:id` (Draft/Paused/Live editable; archived/removed → `listing_not_editable`). Yard FK is PROTECT → the yard delete-guard (`yard_has_listings`) is now live and tested. Auto-yard 100m suggestion returned on create. GIN index on specs; status+asset_class / supplier+status indexes. LocationIQ geocoding helper (graceful degrade without key). D-014: no fee fields on listings.
- reason: Wave 2 §2.4 — real listings can now be authored; publish/photos/state come next.
- change_ref: 2026-06-17 - Wave 2 Slice 2: spec templates + seed
- notes: Green locally — 135 tests / 89.92% cov, ruff+format+mypy clean, makemigrations clean, OpenAPI regenerated (listings paths added, no frozen-enum drift). status is never written in the service (state machine = Slice 4). DEFERRED: "Other (describe)" asset type → Ops review not implemented (unknown types rejected with invalid_asset_type; Ops surfaces are Wave 6) — flag for founder. Live-edit hire-term lock is a no-op until Wave 4 (asserted no cascade). Next: Slice 4 — photos + state machine (publish gates, pause/archive/duplicate).

## 2026-06-17 - Wave 2 Slice 4: photos + state machine (publish gates, duplicate)
- tag: FEATURE
- area: backend/listings (models.ListingPhoto, state.py, services/{photos,listings(transition+duplicate)}, errors publish-gate+photo, serializers photo/reorder/duplicate, views photo/action/duplicate, urls, factories.ListingPhotoFactory, tasks.sweep_orphan_photos, migration 0004, tests/test_publish.py), backend/openapi/schema.yml
- summary: Wave 2 §2.4/§2.5. `listings/state.py::apply` is the single status writer (Draft→Live⇄Paused→Archived; removed Ops-only). Publish gates (FSD §5.2), each a stable code: publish_requires_daily_price/_photo/_location/_specs (per-field), verification_required, business_profile_incomplete; tier auto-Basic at publish. `POST /listings/:id/{publish,pause,archive,duplicate}`. ListingPhoto (≤10 enforced, first photo auto-cover, reorder + single cover) via `POST /listings/:id/photos` + `PATCH /listings/:id/photos/order` (keys come from media/presign). Duplicate → new Draft copying class/type/specs/pricing/units/yard, tier resets Basic, optional photo-key copy (no re-upload). Weekly orphan-sweep task stub.
- reason: Wave 2 §2.4/§2.5 — listings can now go Live through enforced gates; fleet duplicate + photos complete the supply build.
- change_ref: 2026-06-17 - Wave 2 Slice 3: Listings core (CRUD)
- notes: Green locally — 147 tests / 91.25% cov, ruff+format+mypy clean, makemigrations clean, OpenAPI regenerated (listing action+photo paths, no frozen-enum drift). Publish-gate matrix fully tested (each missing precondition → its code). Orphan sweep is a logged no-op until an upload ledger / R2 lifecycle policy lands (flagged). Next: Slice 5 — reports + storefronts (closes the wave).

## 2026-06-17 - Wave 2 Slice 5: reports + storefronts (wave build complete)
- tag: FEATURE
- area: backend/listings (enums.ReportState, models.Report, services/{reports,storefront}, errors.ListingNotReportable, serializers Report*, views ListingReportView/StorefrontView, urls, admin.ReportAdmin, factories.ReportFactory, migration 0005, tests/test_reports_storefront.py), listings/services/listings.py (hide suspended-supplier listings)
- summary: Wave 2 §2.6 — closes the wave. `POST /api/v1/listings/:id/reports` (IsHirer, throttle 5/day/user, only Live listings → else 404); never auto-hides; **3 reports in 30 days sets `priority_review_flag`** (freezegun-tested, window-correct). Ops `ReportAdmin` shows the supplier's sibling listings. `GET /api/v1/storefronts/:supplier_id` (public): business name, logo, verification badge, member-since, about, yards (mini-map data + live count), Live listings (cover photo, ₦ display) — no hire CTA, no fee fields (D-014). Suspended/deleted supplier → storefront 404 AND listing-detail 404 (hidden together, FSD §5.3).
- reason: Wave 2 §2.6 — abuse reporting + the public supplier page; completes the Supply wave.
- change_ref: 2026-06-17 - Wave 2 Slice 4: photos + state machine
- notes: Green locally — 156 tests / 91.14% cov, ruff+format+mypy clean, makemigrations clean, OpenAPI regenerated (reports + storefronts paths, no frozen-enum drift). Lexicon-clean (no owner/renter/booking). All 6 Wave 2 slices on PR #15. Wave-end checklist remaining: prod seed, founder demo, founder approval before Wave 3.

## 2026-06-17 13:10 - FIX: concurrent-migrate race broke Wave 2 prod deploy
- tag: FIX
- area: backend/core/management/commands/deploy.py (new), backend/core/management/{__init__,commands/__init__}.py, backend/railway.json, backend/railway.worker.json, backend/Procfile, backend/core/tests/test_deploy_command.py
- summary: Wave 2 deploy crashed at the migrate step — `duplicate key value violates unique constraint "pg_type_typname_nsp_index"` / `Key (typname, typnamespace)=(listings, 2200)` while applying `listings.0003` (CreateModel `listings`). Root cause: BOTH the web (railway.json) and worker (railway.worker.json) services ran the identical `preDeployCommand: migrate --noinput && seed_superuser`, so on a deploy Railway boots them concurrently and two `migrate` processes race on `0003`. One wins (creates `listings`, records 0003); the loser — here the WEB pre-deploy — has already read the pre-migration state and dies on the duplicate CREATE, so the web deploy fails and the prior Wave-1 image keeps serving (confirmed live: `/healthz` 200 but `/api/v1/listings` 404 though the route is in merged main). Fix: new `manage.py deploy` command runs migrate + seed_superuser under a Postgres session advisory lock (`pg_advisory_lock(0x5445524D494E4C)`), so the second service waits then no-ops; both `preDeployCommand`s and the Procfile `release` now call it. `seed_superuser` is also check-then-create, so it's serialized too.
- reason: A green CI merge auto-deployed but the concurrent migrate race kept Wave 2 from going live; lock makes deploy-time DB work idempotent and race-free.
- change_ref: 2026-06-17 - Wave 2 Slice 5: reports + storefronts (wave build complete)
- notes: Because 0003 is atomic, the orphan `listings` table existing means the winning process DID commit and record 0003 — so the DB is already fully migrated and the next (serialized) deploy's migrate is a clean no-op that brings web live; no DB surgery expected. CONTINGENCY (only if a redeploy still hits the duplicate, i.e. 0003 somehow unrecorded): run a one-off on the api service — `python manage.py migrate listings 0003 --fake` if `listings`+`units`+the 3 indexes all exist, else `DROP TABLE IF EXISTS units, listings CASCADE` (orphan tables are empty — no listings feature was ever live) then `python manage.py deploy --noinput`. New tests (3) mock the cursor + call_command (no DB): lock-before-work, migrate-before-seed, unlock-on-error. ruff+format clean, makemigrations clean (no model changes).

## 2026-06-17 14:40 - FEATURE: brand the Django admin as the "Terminal Ops Console" (Heavy Duty theme)
- tag: FEATURE
- area: backend/settings/{base,prod}.py, backend/pyproject.toml, backend/templates/admin/base_site.html (new), backend/static/admin/css/heavy-duty.css (new), backend/static/admin/fonts/*.woff2 (new, vendored), backend/core/admin.py (new), backend/core/tests/test_admin_theme.py (new)
- summary: Visual theming of the admin (the Ops Console). Found the prod admin was rendering UNSTYLED — no WhiteNoise + gunicorn doesn't serve /static/ when DEBUG=False, so not even Django's own admin CSS loaded. Added `whitenoise` dep + `WhiteNoiseMiddleware` (right after SecurityMiddleware) and wired `STORAGES["staticfiles"] = CompressedManifestStaticFilesStorage` in prod.py ONLY (dev/test keep default storage so they need no collectstatic/manifest). Added project-level `templates/` (overrides admin/base_site.html — app-dir loading can't, admin precedes LOCAL_APPS) and `static/` dirs. New `heavy-duty.css` re-skins the admin by remapping Django's own admin CSS variables onto the design-system palette (amber-500 accent/fill, amber-600 link text to respect the forbidden contrast pair, ink #16181D, paper #F7F7F5, 1px borders, squared, no gradients) + light/dark coverage. Self-hosted Archivo/Inter/IBM Plex Mono woff2 (latin subset, ~115KB, no external CDN per design system) with system fallbacks. `core/admin.py` sets site_header "Terminal Ops Console" / site_title "Terminal Ops" / index_title "Operations" (lexicon). base_site.html adds an amber inline-SVG "T" mark + wordmark.
- reason: Founder asked to build out + beautify the admin. Scope confirmed = VISUAL THEMING ONLY (custom Heavy Duty CSS), explicitly NOT pulling Wave 6 Ops-Console features (dashboards/2FA/queues/disputes) forward; existing ModelAdmin classes left unchanged.
- change_ref: 2026-06-17 13:10 - FIX: concurrent-migrate race
- notes: No migrations, no API/contract change, no new env vars (.env.example unchanged). Verified: `collectstatic` under prod manifest storage succeeds (486 post-processed, font url()s rewritten to hashed names — proves every reference resolves; gzip variants emitted); base_site.html renders the branding DB-free; ruff+format clean. Tests: 4 in test_admin_theme.py — site labels + WhiteNoise-position pass locally; the 2 admin-render tests need Postgres (green in CI). Dark mode keeps Django's dark surfaces with the amber brand carried via vars. Follow-up (deferred, Wave 6): functional Ops Console (queues, dashboards, 2FA). After merge+deploy, confirm /admin/ renders themed in prod.

## 2026-06-17 15:15 - FIX: promote existing SEED_SUPERUSER + prod Wave 2 E2E via Ops admin
- tag: FIX
- area: backend/accounts/management/commands/seed_superuser.py, backend/accounts/admin.py, backend/accounts/tests/test_seed_superuser.py, scripts/admin_ops.py
- summary: `nwabueze@perblis.com` was registered in Wave 1 before `SEED_SUPERUSER_*` was set, so `seed_superuser` skipped and admin login failed. PR #18: promote existing email to staff/superuser + set password once; expose `email_verified_at` / editable channel timestamps in Ops admin. Post-deploy: promoted test user `nwabueze+wave2-live-1781705494@perblis.com` (verify phone+email, `account_level=verified`, `is_supplier`) via admin; full prod API E2E green — profile → yard → listing → R2 photo → publish Live → duplicate → storefront (`live_listings` count 1). Termii sender still `pending` (SMS 502); admin channel verify is the interim Ops path.
- reason: Founder provided SEED_SUPERUSER creds and asked to use Ops admin to unblock live Wave 2 testing while Termii approval is pending.
- change_ref: 2026-06-17 13:10 - FIX: concurrent-migrate race broke Wave 2 prod deploy
- notes: Admin URL https://perblisterminal-production.up.railway.app/admin/ — `nwabueze@perblis.com` now staff. `scripts/admin_ops.py` automates verify+promote (datetime fields `_0` date / `_1` time). Wave-end founder demo criterion met on prod API. Termii sender approval still needed for real phone OTP flow.

## 2026-06-17 15:35 - FIX: admin 500 in prod — bake static manifest at build + leading-slash STATIC_URL
- tag: FIX
- area: backend/Dockerfile, backend/settings/base.py
- summary: After the Heavy Duty admin theme merged (#19), prod `/admin/` returned HTTP 500. Root cause: the Dockerfile ran `collectstatic ... || true` under `settings.prod`, which requires `SECRET_KEY`/`ALLOWED_HOSTS`/`CORS_ALLOWED_ORIGINS` from env — absent at `docker build` time — so collectstatic failed and `|| true` swallowed it, shipping an image with NO `staticfiles.json` manifest. With `CompressedManifestStaticFilesStorage` (added in #19) the missing manifest makes every `{% static %}` raise → 500 on all admin pages (previously, default storage just rendered unstyled). Fix: run collectstatic with build-only throwaway env values (`SECRET_KEY=build-only ALLOWED_HOSTS=* CORS_ALLOWED_ORIGINS=...`) and DROP `|| true` (fail the build loudly). Also fixed `STATIC_URL` `"static/"` → `"/static/"` (no leading slash = assets resolve relative to the page path, e.g. /admin/login/static/…, breaking every link).
- reason: #19's manifest storage exposed a pre-existing build-time collectstatic failure; without the baked manifest the admin 500s instead of merely being unstyled.
- change_ref: 2026-06-17 14:40 - FEATURE: brand the Django admin as the "Terminal Ops Console" (Heavy Duty theme)
- notes: Verified locally by reproducing the build (collectstatic under settings.prod + build env → manifest present) then rendering the admin under settings.prod WITH the manifest → theme resolves to `/static/admin/css/heavy-duty.<hash>.css`, no ValueError. WhiteNoise serves the baked STATIC_ROOT at runtime. Build-only SECRET_KEY is scoped to the RUN command (not persisted as image ENV); Railway injects real values at runtime. URGENT: prod admin is 500ing until this merges + redeploys.

## 2026-06-18 - Prod demo: nwabueze@perblis.com as verified supplier
- tag: DEPLOY
- area: production API, scripts/admin_ops.py
- summary: Founder manually verified phone for `nwabueze@perblis.com`; agent promoted account via Ops admin (`is_supplier`, `account_level=verified`) and completed full supplier demo on prod API — profile (Perblis Plant & Haulage), yard (Apapa Main Yard), listing (CAT 320D published Live), duplicate draft, storefront with 1 live listing. 13/13 live checks passed.
- reason: Founder asked to set up their primary account as the live Wave 2 supplier demo.
- change_ref: 2026-06-17 15:15 - FIX: promote existing SEED_SUPERUSER + prod Wave 2 E2E via Ops admin
- notes: Storefront https://perblisterminal-production.up.railway.app/api/v1/storefronts/019ed075-5f3f-7336-8f20-334160a78c41 · Listing `019edb68-44a3-7389-8c20-702f398c7625` Live at ₦95,000/day. Fixed `admin_ops.py` login check (admin title is "Terminal Ops", not "Site administration").

## 2026-06-19 - Prod E2E demo on new Railway API (api-production-101c8)
- tag: DEPLOY
- area: production API (https://api-production-101c8.up.railway.app)
- summary: Full Waves 0–3 founder demo on the new prod URL after founder promoted `nwabueze@perblis.com` (supplier + verified). 21/21 steps passed: healthz → login → profile (Perblis Plant & Haulage) → Apapa Main Yard → CAT 320D published Live (₦95k/day, R2 photo) → duplicate draft → storefront (1 live) → search/map solo pin + search/list (asset + location) + radius + geocode (LocationIQ live, 2 results for Apapa Lagos). Wave 3 contracts live (31 OpenAPI paths).
- reason: Founder approved account setup and asked for full prod E2E on the new Railway deployment.
- change_ref: 2026-06-18 - Prod demo: nwabueze@perblis.com as verified supplier
- notes: IDs — user `019ede63-eb70-752c-be3e-522af7540bf8`, yard `019edeed-c9f9-7285-b29c-b1e16e41bebb`, listing `019edeed-cac2-7e05-99ea-44f83645355b`, duplicate `019edeed-d049-7bef-a2f7-37ff55aeaf50`. Solo pin (1 listing at yard) — yard aggregation kicks in at ≥2 Live listings per yard. Primary prod URL is now `api-production-101c8.up.railway.app` (old `perblisterminal-production` is stale/pre-Wave-3).

## 2026-06-18 15:50 - CHORE: prepare handoff for Wave 3 (Discovery)
- tag: CHORE
- area: Implementations.md, CLAUDE.md, docs/waves/README.md (docs-only)
- summary: Handoff prep so a fresh instance can resume cold into Wave 3. Reconciled docs to the code that actually shipped: Waves 0–2 are merged to `main` and the backend is deployed; Wave 2's prod API E2E demo criterion was met; the Django admin "Terminal Ops Console" theme + the static-manifest 500 hotfix are merged and confirmed styled & live in prod. Refreshed the Implementations.md **Current status** block (built/deployed, Ops Console is visual-only, Wave 3 is the gated next wave with a "read docs/waves/wave-3.md first" pointer + carry-over gotchas), updated the CLAUDE.md repo-state snapshot, and set the docs/waves/README.md status column (Wave 2 ✅ done & merged; Wave 3 next, gated on explicit founder go).
- reason: Founder asked to "prepare the handoff for wave 3."
- change_ref: 2026-06-17 15:35 - FIX: admin 500 in prod — bake static manifest at build + leading-slash STATIC_URL
- notes: Docs-only, no behavior change. **No OpenAPI regeneration** — nothing since Wave 2's contract freeze touched an API contract (the admin theme + static fix are non-API). Wave gating is binding: this handoff does NOT authorize Wave 3 — the next instance must confirm explicit founder approval (and record Wave 2 sign-off) before coding. New-instance reading path verified: Implementations.md → design.md → docs/waves/wave-3.md → FSD §6 / TSD §3.1, §3.7, §3.8. Opening a docs-only draft PR with this.

## 2026-06-18 22:50 - DECISION: Wave 2 sign-off + Wave 3 (Discovery) approved to build
- tag: DECISION
- area: wave gating
- summary: Founder gave explicit go to start Wave 3 (Discovery — Map Search & Yard Aggregation). Wave 2 is signed off (merged, deployed, prod E2E demo passed). Wave 3 builds the `search` app as 4 vertical slices: (1) `search/map` server-side yard aggregation, (2) `search/list` + cursor pagination + group_by, (3) LocationIQ `geocode` proxy w/ 24h cache, (4) performance pass (~500-listing seed, EXPLAIN ANALYZE, P95<500ms). Contracts (`search/map`, `search/list`, `geocode`) freeze at wave end.
- reason: D-gate: "never start the next wave without explicit founder approval." Approval recorded here per the handoff note's requirement.
- change_ref: 2026-06-18 15:50 - CHORE: prepare handoff for Wave 3 (Discovery)
- notes: Slice loop does not authorize Wave 4 — that needs its own founder go.

## 2026-06-18 22:55 - Wave 3 Slice 1: map search & yard aggregation
- tag: FEATURE
- area: backend/search (services/aggregation.py, serializers.py, views.py, urls.py, tests/test_map_search.py), backend/listings/spec_data.py (star_field helper), backend/core/urls.py (mount search), backend/.github/workflows/backend.yml (mypy +search), backend/openapi/schema.yml
- summary: Wave 3 §3.1 / TSD §3.7. `GET /api/v1/search/map` — anonymous-allowed (throttle `search_anon` 60/min), params `bbox` XOR `lat,lng,radius_km` (+ `asset_class`, `q`, `price_min/max`, `spec_min/max`). Server-side aggregation over Live, visible (non-suspended/-deleted supplier, FSD §5.3) listings in the viewport: a yard with ≥2 Live listings → one `yards[]` entry (authoritative `listing_count`/`matching_count`, `class_mix`, `price_from`(+display), `supplier{id,name,logo,badge}`, embedded listing summaries); yardless or lone-at-yard listings → solo `listings[]` pins (with `distance_km`). Counts computed over the viewport-bounded Live set regardless of content filters, so a zero-match yard still appears (`matching_count:0`, client dims). One indexed spatial query (bbox `point__intersects` envelope / radius `point__dwithin` geography) + select_related/prefetch — N+1-free (asserted ≤6 queries). `★` spec range filter targets the per-class filterable headline field via new `listings.spec_data.star_field()` (single source of truth). `available` stubbed True until Wave 4. Distance = ST_Distance/1000 rounded 0.1km, ordering nearest-first.
- reason: Wave 3 §3.1 — the map read model behind the hirer home screen.
- change_ref: 2026-06-18 22:50 - DECISION: Wave 3 approved
- notes: Green locally — 188 suite tests (29 in search) / 92.99% overall cov; search aggregation 97%, serializers+views 100%. ruff+format+mypy clean (search added to CI mypy), makemigrations clean (no model changes — star_field is pure logic, no schema touch). OpenAPI regenerated: +218 lines, 0 deletions (purely additive — Wave-2 frozen contracts untouched; search contracts freeze at wave end). Local test DB: PostGIS 3.4 on pg16 (docker unavailable in remote env) at postgis://postgres:postgres@localhost:5432/terminal. DECISION-pending (not blocking, revisit at wave freeze): solo-pin `badge` = listing trust tier (basic/verified/inspected); yard `supplier.badge` = account verification badge — two distinct "badge" meanings, documented in aggregation.py. Next: Slice 2 — `search/list` (cursor pagination + group_by=asset|location).

## 2026-06-19 04:40 - Wave 3 Slice 2: search/list (cursor pagination + group_by)
- tag: FEATURE
- area: backend/search (services/common.py [new, shared primitives], services/aggregation.py [refactored onto common], services/listing.py [new], pagination.py [new keyset cursor], serializers.py [+List* shapes], views.py [+ListSearchView], urls.py [+search/list], tests/test_list_search.py [new]), backend/openapi/schema.yml
- summary: Wave 3 §3.2 / TSD §3.2. `GET /api/v1/search/list` — same params as `search/map` + `group_by` + cursor pagination; anon, `search_anon` throttle. `group_by=asset` (default): flat distance-ordered matching listings, each with `more_at_yard` (sibling matching listings at the same yard) for "+N more at this yard" sub-lines. `group_by=location`: yard cards (≥2 matching listings) interleaved with solo listings, ordered by distance. Unlike the map the list is filtered (no zero-match dimming). Extracted Slice-1's spatial/base-queryset/match/summary helpers into `search/services/common.py`; `aggregation.py` (map) now rides on them (map response shape byte-identical — schema diff confirms). New `search/pagination.py` = true keyset cursor over `(distance_km, tiebreak_id)`, DRF-style `{results,next,previous}` opaque `?cursor=` envelope; boundaries are values not offsets → stable under inserts (tested: insert a nearer row mid-walk, page 2 neither duplicates nor skips). N+1-free (≤6 queries asserted, both modes).
- reason: Wave 3 §3.2 — the list view of the discovery result set.
- change_ref: 2026-06-18 22:55 - Wave 3 Slice 1: map search & yard aggregation
- notes: Green locally — 209 suite tests (16 new list + refactored map intact) / 93.46% overall; search modules 96–100%. ruff+format+mypy clean, makemigrations clean (no model changes), `.env.example` complete. OpenAPI regenerated: +114 lines, 0 deletions (additive; search/map unchanged, Wave-2 frozen contracts untouched). search/map + search/list contracts still freeze at WAVE END (not slice end). Same pending DECISION as Slice 1: solo/row `badge` = listing tier vs yard `supplier.badge` = account verification (documented in common.py). Next: Slice 3 — `GET /api/v1/geocode` LocationIQ proxy (24h cache, no Redis).

## 2026-06-19 05:10 - Wave 3 Slice 3: geocoding proxy
- tag: FEATURE
- area: backend/search (services/geocode.py [new], serializers.py [+Geocode* shapes], views.py [+GeocodeView], urls.py [+geocode], tests/test_geocode.py [new]), backend/openapi/schema.yml
- summary: Wave 3 §3.4 / TSD §3.8. `GET /api/v1/geocode?q=&limit=` — server-side LocationIQ forward-geocode proxy; the API key never reaches the client. Anonymous, `search_anon` throttle (reusing the documented anon-search rate; no new throttle invented). Results cached 24h via Django's cache framework (LocMem dev / DB prod — no Redis, D-010); cache key = sha256(normalised-lowercased-query | limit). Nigeria-scoped (`countrycodes=ng`). Graceful degradation: when `LOCATIONIQ_KEY` is absent (dev/CI) or the upstream errors, returns 200 `{results: []}` with `provider_configured` flag — transient failures are NOT cached (re-fetched next call). Response: `{query, provider_configured, results:[{display_name, lat, lng}]}`. Mounted at `/api/v1/geocode` (not under search/) per TSD §3.8 misc group.
- reason: Wave 3 §3.4 — address→coords for the map/list pickers and listing-creation location step.
- change_ref: 2026-06-19 04:40 - Wave 3 Slice 2: search/list
- notes: Green locally — 221 suite tests (12 new geocode) / 93.56% overall; geocode service 98%, views+serializers 100%. ruff+format+mypy clean, makemigrations clean, `.env.example` complete (LOCATIONIQ_KEY already documented — no env change). OpenAPI regenerated: +65 lines, 0 deletions (additive). Mandatory checks covered: key never in response, cache-hit on repeat query (upstream called once), limit forwarded, query normalised for cache, unconfigured/failure degrade. Next + LAST Wave 3 slice: Slice 4 — performance pass (~500-listing seed, EXPLAIN ANALYZE hot queries, P95<500ms) + then WAVE-END contract freeze (search/map, search/list, geocode) and the badge-meaning DECISION resolution.

## 2026-06-19 05:35 - Wave 3 Slice 4: performance pass (wave build complete)
- tag: FEATURE
- area: backend/search (management/commands/seed_search_demo.py [new], tests/test_performance.py [new]), docs/waves/README.md (status)
- summary: Wave 3 §3.5 — closes the wave build. New `seed_search_demo` management command seeds dual-corridor (Apapa + Lekki) demo data: N Live listings across yards + suppliers, all 5 asset classes with a numeric ★ headline spec, ~80% yard-attached (→ yard pins) / 20% solo, reproducible RNG, `--clear` (deletes in dependency order — listing→yard FK is PROTECT). Verified GIST spatial indexes already exist on listings.point + yards.point (GeoDjango spatial_index default; no migration needed). Committed guardrail tests assert query count stays CONSTANT (≤6) at 150 listings — no N+1 at scale (latency itself is measured out-of-band, too noisy to gate in CI).
- reason: Wave 3 §3.5 — prove the discovery endpoints hold P95<500ms and are N+1-free under realistic data.
- change_ref: 2026-06-19 05:10 - Wave 3 Slice 3: geocoding proxy
- notes: **P95 measurement (local, 500 seeded listings, 60 reqs/endpoint, throttle disabled for the bench):** map(no filter) p50 97ms / **p95 173ms**; map(class+spec) p95 165ms; list asset p95 166ms; list location p95 180ms — all well under the 500ms gate (>2.5x margin; Railway differs but ample headroom). **EXPLAIN ANALYZE** of the hot bbox query: single query joining supplier/profile/yard, **Execution Time 3.5ms** — the ~95ms endpoint cost is Python serialization of the in-viewport rows, not the DB (GIST index present and used when the viewport is selective). Green locally — 226 suite tests (5 new) / 93.71% overall; seed command 100%, all search modules 96–100%. ruff+format+mypy clean, makemigrations clean, `.env.example` complete, OpenAPI unchanged (no new contract). **WAVE-END status:** build complete on PR #25 (Slices 3+4). Wave-end checklist — [x] FSD §6 acceptance tests, [x] P95 recorded (above), [x] OpenAPI committed + search contracts stable/frozen, [ ] founder demo + sign-off before Wave 4. DECISION resolved (documented, founder may override at demo): solo-pin/list-row `badge` = listing trust tier; yard `supplier.badge` = account verification badge. Founder demo path: deploy, `manage.py seed_search_demo --listings 500`, then exercise `/api/v1/search/map` for the four canonical cases + `/search/list` + `/geocode`.

## 2026-06-19 06:05 (local) - Railway deploy to natural-cat project
- tag: DEPLOY
- area: Railway project natural-cat (7752aace-34c8-4ae6-928b-06ded98fb25a); services PostGIS + api + worker; backend/railway.json, backend/railway.worker.json
- summary: Brought up full backend stack on the **natural-cat** Railway project via CLI (MCP list_projects worked; deeper MCP calls returned Unauthorized — used linked `railway` CLI instead). PostGIS was already online. Created **api** and **worker** empty services; configured root directory `backend`, Dockerfile builder, env vars (DATABASE_URL → `${{PostGIS.*}}` reference, all founder-provided keys), api healthcheck `/healthz`, worker `configFile=/backend/railway.worker.json` (required because `railway.json` startCommand overrides dashboard). Deployed both via `railway up`. Public API domain: **https://api-production-101c8.up.railway.app**. Probes green: `/healthz` database ok; `/readyz` database/r2/resend/termii/ably configured, bachs not_configured (empty keys). Migrations applied; superuser `nwabueze@perblis.com` seeded. Worker confirmed running `db_worker` (`queues=default`).
- reason: Founder requested Railway MCP setup + deploy on natural-cat with supplied production env vars.
- notes: Update `CORS_ALLOWED_ORIGINS` when the portal Workers URL is known. Set `LOCATIONIQ_KEY` for geocode. Bachs keys empty (Wave 4). Railway MCP may need `railway login` refresh in Cursor for full MCP access. Admin: https://api-production-101c8.up.railway.app/admin/

## 2026-06-19 06:20 (local) - GitHub auto-deploy on main (natural-cat)
- tag: CHORE
- area: Railway natural-cat; api + worker services
- summary: Connected both **api** and **worker** to `Perblis/Perblis_Terminal` branch **main** with root directory `backend` (worker keeps `configFile=/backend/railway.worker.json`, `checkSuites=false`). Merges/pushes to `main` now trigger Railway builds for both services.
- reason: Founder requested GitHub auto-deploy on merge to main.
- change_ref: 2026-06-19 06:05 (local) - Railway deploy to natural-cat project
- notes: Requires Railway GitHub App authorized for the `Perblis` org/repo — if deploys don't fire after merge, install/approve at https://railway.com/account/integrations

## 2026-06-19 06:00 - Wave 3 demo run + Wave 4 handoff (docs-only)
- tag: CHORE
- area: Implementations.md, CLAUDE.md, docs/waves/README.md (docs-only)
- summary: Founder asked for a demo run + Wave 4 handoff. Ran the Wave 3 exit-criterion demo locally against 500 seeded dual-corridor listings (`seed_search_demo --listings 500`): all four canonical map cases verified — (1) yard pin with counts + supplier block + embedded summaries (Demo Yard 7, listing_count 25), (2) solo pin with distance_km + badge, (3) filter-aware matching_count (partial 3-of-20) AND dimmed zero-match yards (impossible ★-spec → 24 yards present, all matching_count 0, solos filtered to 0), (4) empty offshore viewport → `{yards:[],listings:[]}`; plus `search/list` asset (distance-ordered, more_at_yard, next cursor) + location (yard cards interleaved with solos) and `geocode` (graceful `provider_configured:false` in dev). Then refreshed the handoff docs: Implementations.md Current-status block (Waves 0–3 done/deployed; Wave 4 next + read-wave-4.md pointer + carry-overs incl. LOCATIONIQ_KEY-in-prod, badge meaning, available-stub→Wave 4), CLAUDE.md repo snapshot, docs/waves/README.md status column.
- reason: Founder: "demo run, pull latest code from git and then do the wave 4 handoff."
- change_ref: 2026-06-19 05:35 - Wave 3 Slice 4: performance pass (wave build complete)
- notes: Docs-only, no behavior change; no OpenAPI regeneration (search contracts already committed/frozen at Slice 4). Pulled latest `main` (9a72cfd — incl. the natural-cat Railway bring-up + main→Railway auto-deploy, so Wave 3 is live in prod once #25's merge built). Wave gating remains binding: this handoff is NOT Wave 4 authorization — the next instance must record explicit founder go (+ Wave 3 sign-off) before coding `hires`/`payments`. New-instance reading path: Implementations.md → design.md → docs/waves/wave-4.md → FSD §3/§7/§9 + TSD §3.2–§3.6. Opening a docs-only draft PR.

## 2026-06-19 - Wave 4 authorized (founder go) + Slice 4A: fee engine
- tag: DECISION
- area: wave gating
- summary: Founder gave explicit approval to start **Wave 4 (Hires & Money)** ("let's start wave 4"). Recording the Wave 3 sign-off (exit criterion demonstrated — see prior demo entry) and Wave 4 authorization together. Building per docs/waves/wave-4.md as six ordered slices (4A fee engine → 4B model/state/availability → 4C request/accept/decline → 4D Bachs payments → 4E timers/handovers/disputes → 4F payouts/reconciliation/notifications), each its own PR, 85% coverage gate on hires+payments. Naming default (flagged to founder): webhook-dedup table is provider-neutral `payment_events` (not TSD's `paystack_events`) since D-017 supersedes Paystack with Bachs.io.
- reason: Founder authorization; wave gating is binding (CLAUDE.md / design.md §7).
- change_ref: 2026-06-19 06:00 - Wave 3 demo run + Wave 4 handoff (docs-only)
- notes: Wave-4.md/TSD text still says "Paystack" — treat as Bachs.io per D-017 (HMAC-SHA256 hex, decimal-naira strings at the adapter boundary only).

## 2026-06-19 - Wave 4 Slice 4A: fee engine (hires/fees.py, pure)
- tag: FEATURE
- area: backend/hires (fees.py [new], enums.py [new — Scheme], tests/test_fees.py [new])
- summary: FSD §3.1 / TSD §3.2 fee engine, pure (no I/O, no ORM, no clock). Integer per-mille `RATE_TABLE` by (asset class, scheme); `FEE_FLOOR = 250_000` kobo (₦2,500). `quote(asset_class, *, days, daily_price, weekly_price?, monthly_price?) -> FeeQuote(hire_value, service_fee, payout_amount, fee_basis, scheme)`: best-price over only the set schemes (`min(daily×d, weekly×ceil(d/7), monthly×ceil(d/30))`, D-008), ties → longer scheme; `service_fee = max(value×rate//1000, FEE_FLOOR)` (D-002); `payout = value − fee` (D-005); `fee_basis` e.g. "10% weekly (min ₦2,500)". `duration_days(start,end) = (end−start).days+1` inclusive. Money is integer kobo throughout; rates as ‰ so no float/Decimal ever.
- reason: Wave 4 slice 4A — the locked-at-acceptance financial truth every downstream slice depends on.
- change_ref: 2026-06-19 - Wave 4 authorized (founder go)
- notes: Green — the **five FSD §3.1 worked examples are named test vectors** (₦240k/₦28,800; ₦900k/₦90,000; ₦640k/₦76,800; ₦15k→₦2,500 floor; ₦350k/₦21,000), all pass byte-exact; + hypothesis properties (accounting identity payout+fee≡value, floor honoured, monotonic-in-days, best-price optimality) + tie-break + guard rejects. 16 tests, **hires.fees + hires.enums at 100%** coverage. ruff+format+mypy clean (TextChoices members are `tuple[str,str]` to mypy without django-stubs → tables keyed by `str(member)`; fee_basis built from the scheme code word, no `.label`). No models/migrations yet (4B). Next: Slice 4B — Hire model + state machine + availability engine.

## 2026-06-19 - Wave 4 Slice 4B (part 1): Hire model + state machine + availability engine
- tag: FEATURE
- area: backend/hires (models.py, enums.py [+HireStatus/ActorKind/CancelledBy/HandoverKind], errors.py, availability.py, state.py, factories.py, migrations/0001_initial.py, tests/{test_availability,test_state,test_race}.py)
- summary: TSD §3.3/§3.4/§3.5, FSD §7.3. Models: `Hire` (financials locked@accept; hot-path index (listing,status,start,end) + (hirer,status) + (supplier,status)), `HireEvent` (append-only — `save` rejects updates, `delete` forbidden at model layer + migration `REVOKE UPDATE,DELETE` for restricted prod roles), `HandoverRecord` (model now; logic in 4E). State machine `state.apply(hire, action, *, actor, actor_kind, **meta)` is the SOLE writer of `Hire.status` — 7-state TRANSITIONS table (accept/decline/expire/pay/cancel/start/complete/dispute/resolve_*), each writes a HireEvent + `transaction.on_commit` side-effect hook (notifications stubbed → 4D/4F). Availability engine (TSD §3.4): **two checks** — public/soft (`is_available`/`free_units`: confirmed+on_hire+accepted-live, for search/date-picker/new requests) and hard (`can_confirm`: confirmed+on_hire only, for accept/pay). Binding race rule: accept+pay take `SELECT … FOR UPDATE` on the listing row then re-check `can_confirm`; on Confirmed the same txn auto-cancels overflowed Requested/Accepted competitors (`no_longer_available`). Supplier may **oversell** at accept (soft holds); **first-to-pay wins**.
- reason: Wave 4 slice 4B — the hire lifecycle + capacity engine the money loop runs on.
- change_ref: 2026-06-19 - Wave 4 Slice 4A: fee engine
- notes: Green — 47 hires tests (full suite 273 pass), **hires at 98%** coverage (availability/enums/errors/factories/fees 100%, state 97%, models 95%). The **threaded double-payment race test** (`test_race.py`, django_db(transaction=True)) confirms exactly one Confirmed on the last unit + loser bumped; stable across 5 reruns. ruff+format+mypy clean (14 files); makemigrations --check clean; migration 0001 reversible. **Modeling note (resolved):** FSD §7.3's "auto-decline overflowed Requested/**Accepted** on Confirm" + "first-to-pay wins" only cohere if accept permits oversell — so accept/pay gate on *hard* holds (can_confirm), while *public* availability counts soft holds. Documented in availability.py. **Still in 4B (next commit):** wire real availability into search payloads (`available` flag + yard "n of m free") via a BULK hold-count (respect search's ≤6-query gate) + remove the stub note from search OpenAPI. Then 4C (request/accept/decline APIs).

## 2026-06-19 - Wave 4 Slice 4B (part 2): close the Wave 3 `available` stub
- tag: FEATURE
- area: backend/hires/availability.py (+availability_map), backend/search/services/{common,aggregation,listing}.py, backend/search/serializers.py, backend/search/tests/test_availability_flag.py [new], backend/openapi/schema.yml
- summary: Wired the real availability engine into search, closing the Wave 3 stub (`available` was hardcoded `true`). New `availability.availability_map(listings)` returns `{id: available_now}` in **one** grouped aggregate query (soft holds covering today vs unit_count). `common.annotate_availability(listings)` tags each listing `_available`; both summary builders now read it; both service entry points (search_map, _matching_listings) call it. So map pins, list rows, and yard-embedded summaries all carry a truthful `available` — and the per-listing flags in a yard's embedded `listings[]` are exactly the "n of m free" caption data (ux/02 S5), no new aggregate field needed. Serializer help_text updated; OpenAPI regenerated (description-only, contract shape unchanged).
- reason: Wave 4 §4B — discovery must reflect real capacity once hires exist.
- change_ref: 2026-06-19 - Wave 4 Slice 4B (part 1)
- notes: Green — 276 tests (3 new: no-hold→available, fully-held→unavailable, multi-unit partial→available). search perf gate still holds (≤6 queries — the bulk availability query fit the budget). ruff+format+mypy clean (34 files). `available` semantics: free *now* (today) against soft holds (confirmed+on_hire+accepted-live); date-range availability is enforced at request/accept (4C) + pay (4D). **Slice 4B complete.** Next: Slice 4C — POST hires (preview via 4A, D-014 total-only, Basic ₦250k cap, blocked-date validation), accept/decline/cancel APIs + role-shaped hire serializers (D-014 tested both directions).

## 2026-06-19 - Wave 4 Slice 4C: request/accept/decline/cancel APIs + D-014 serializers
- tag: FEATURE
- area: backend/hires (services.py, serializers.py, views.py, urls.py, errors.py [+HireNotFound/ListingNotHireable/CannotHireOwnListing], tests/test_hires_api.py), backend/core/urls.py (mount hires), backend/openapi/schema.yml
- summary: FSD §7.1–§7.2, §4.1, D-014. Endpoints under `/api/v1/`: `POST hires` (listing+dates+terms ack → fee preview via 4A; **Basic ₦250k cap** at request time for unverified hirers → `basic_cap_exceeded`; soft-availability blocked-date check; can't hire own/non-Live listing; writes the initial `requested` event), `GET hires?role=&status=` (cursor-paginated, party-scoped), `GET hires/:id` (+ `events[]` timeline), `POST :id/accept` (supplier-only; terms lock, 4h `payment_deadline` opens; payment/messaging hooks deferred to 4D/5), `POST :id/decline` (supplier, mandatory reason), `POST :id/cancel` (role/state-aware per §7.6 — hirer withdraws Requested; either party pre-payment; supplier-from-Requested rejected → must Decline). **D-014 role-shaping** in `HireSerializer.to_representation`: `service_fee`/`payout_amount`/`fee_basis` present iff requester is the hire's supplier or staff — **tested both directions**. Mutations go through `state.apply` (sole status writer); views stay thin.
- reason: Wave 4 slice 4C — the hirer/supplier request→accept loop, the product's core interaction.
- change_ref: 2026-06-19 - Wave 4 Slice 4B (part 2)
- notes: Green — 292 tests (16 new hires-API), **hires at 97%** coverage (full suite); ruff+format+mypy clean (19 hires files). OpenAPI regenerated additively (+555 lines: 5 hire paths; Wave-2/3 frozen contracts untouched; 2 new cosmetic enum-naming warnings — schema technically correct, ENUM_NAME_OVERRIDES polish deferred). Confirmed-cancel **refund math is 4D** (cancel transitions now; the refund side-effect lands in 4D). Next: Slice 4D — Bachs.io (D-017) checkout init on accept + `POST payments/webhook` (HMAC-SHA256, dedup, verify-before-transition) + refunds per §7.6 + money invariant.

## 2026-06-19 - Wave 4 Slice 4D (part 1): Bachs collect flow (checkout + webhook)
- tag: FEATURE
- area: backend/payments (enums, models [Payment/PaymentEvent/Refund], errors, bachs.py [adapter], services.py, tasks.py, serializers.py, views.py [webhook], urls.py, migrations/0001, tests/{test_payments,test_bachs}.py), backend/hires (services.accept_hire on_commit→init payment, views/urls +GET hires/:id/payment), backend/core/urls.py (mount payments), backend/openapi/schema.yml
- summary: TSD §3.6 / D-017 (Bachs.io, collect-only — supersedes Paystack). `payments/bachs.py` is the **single money boundary**: integer kobo ↔ decimal-naira strings live only here; HMAC-SHA256 hex signature over `"{ts}.{raw_body}"` (5-min tolerance); graceful keyless degradation (stub checkout URL, verify→not-ok — never auto-confirms). Flow: supplier accept → `transaction.on_commit` opens a Bachs checkout (≤3 attempts/window), `Payment(initiated)` with reference `THR-{hireid12}-{attempt}`. `POST /api/v1/payments/webhook`: AllowAny, verify signature (else 400) → dedup on envelope `id` (savepoint-wrapped insert → duplicate=200 skip) → 200 fast → enqueue task → **verify-before-transition** (`GET charges/{id}`: SUCCEEDED + amount + NGN) → `state.apply(pay)` under listing lock + mark `Payment.success`. `GET /api/v1/hires/:id/payment` returns status + authorization_url. Client redirects never trusted; idempotency layered (envelope dedup → `processed_at` → state-machine guard).
- reason: Wave 4 slice 4D part 1 — the real collect loop (request→accept→checkout→webhook-confirmed→Confirmed).
- change_ref: 2026-06-19 - Wave 4 Slice 4C
- notes: Green — 26 payments tests (webhook signature reject, envelope dedup, verify-before-transition incl. amount-mismatch, E2E confirm, processor replay-idempotent, attempt cap, money-conversion roundtrip, adapter HTTP paths via mocked httpx, signature edge cases); **combined hires+payments coverage 96%** (CI gate `--cov=hires --cov=payments --cov-fail-under=85`); full suite 318 pass; ruff+format+mypy clean (33 files); makemigrations --check clean; OpenAPI +73 lines (webhook + payment endpoints; frozen contracts untouched; 0 errors). `BACHS_*` env already in `.env.example`. **Part 2 (next commit, same slice):** refunds per §7.6 (the six rows as named tests) + supplier strike + money invariant `collected − refunded − paid_out − retained_fee ≡ 0`. Then 4E (sweeps/handovers/disputes), 4F (payouts/reconciliation/notifications).

## 2026-06-19 - Wave 4 Slice 4D (part 2): refunds (§7.6) + supplier strike
- tag: FEATURE
- area: backend/payments (refunds.py [new — pure §7.6], services.py [+issue_refund/retained/_record_supplier_strike], tests/test_refunds.py), backend/hires/services.py (cancel_hire → on_commit refund for paid hires)
- summary: FSD §7.6 cancellation table, pure in `payments/refunds.py::compute_refund_plan(hire, cancelled_by, now) -> RefundPlan(amount, withheld_day, strike, kind)`: pre-payment free (handled upstream); hirer >72h → 100%; hirer ≤72h → `value − one_day_equiv − 1.5% processing`, withheld day → supplier payout due (D-015, Payout created in 4F); supplier-cancel (any time) → 100% to hirer + **strike** (3 ⇒ suspension-review log, Ops UI Wave 6); no-shows map per table (hirer→≤72h, supplier→supplier-cancel). `issue_refund` creates a `Refund` (COMPLETED if Bachs settled, else PENDING for Ops retry — keyless dev → PENDING), calls `bachs.create_refund`, increments `SupplierProfile.strike_count` (existing field) under lock. `hires.cancel_hire` fires it post-commit when cancelling a CONFIRMED hire. `services.retained(hire) = collected − refunded − paid_out` for the money-conservation assertions.
- reason: Wave 4 slice 4D part 2 — money out (refunds) per the normative §7.6 table.
- change_ref: 2026-06-19 - Wave 4 Slice 4D (part 1)
- notes: Green — 34 payments tests (8 new: each §7.6 row, strike increment, full-refund retains 0, late-cancel conserves money, E2E cancel→refund via on_commit capture); combined hires+payments **96%**; full suite 326 pass; ruff+format+mypy clean (35 files); makemigrations --check clean (strike_count already existed). **Money invariant:** conservation asserted for the cancel/refund terminal states now; the *full* `collected−refunded−paid_out−service_fee≡0` at completion needs the Payout model → **4F**. **Slice 4D complete.** Next: Slice 4E — 5-min sweeps (expire/auto-cancel/auto-on-hire/auto-complete, idempotent), handovers (FSD §7.4), disputes (freeze payout).

## 2026-06-19 - Wave 4 Slice 4E: timers/sweeps + handovers + disputes
- tag: FEATURE
- area: backend/hires (tasks.py [new — sweeps], management/commands/run_hire_sweeps.py [new], services.py [+submit/confirm handover, raise/resolve dispute], serializers.py [+Handover/Dispute], views.py [+4 views], urls.py [+4 routes], state.py [dispute also from COMPLETED], tests/{test_sweeps,test_handovers_disputes}.py), backend/openapi/schema.yml
- summary: TSD §3.5 + FSD §7.3/§7.4. **Sweeps** (`hires/tasks.py`, idempotent — select strictly by current status so a double-run is a no-op): requested past 24h → Expired; accepted past payment_deadline → Cancelled(system, payment_expired); confirmed past start+24h → On Hire; on_hire past end+48h → Completed. Each hire's transition is its own txn (one failure doesn't block the batch). Invoked by `manage.py run_hire_sweeps` (Railway cron) or the `sweep_hires` task. **Handovers** (§7.4): `POST hires/:id/handovers` (kind on_hire/off_hire, ≥2 private-bucket photos, class reading) + `POST handovers/:id/confirm` (the *counterparty* confirms; submitter can't) → a confirmed on-hire handover starts the hire, an off-hire one completes it (the manual path; sweeps are the fallback). **Disputes**: `POST hires/:id/dispute` (either party, mandatory reason, On Hire or ≤72h after end) → In Dispute (payout freeze enforced in 4F); `POST hires/:id/resolve-dispute` (staff-only → Completed/Cancelled; Ops console UI is Wave 6).
- reason: Wave 4 slice 4E — the time-driven lifecycle automation + field evidence + dispute path.
- change_ref: 2026-06-19 - Wave 4 Slice 4D (part 2)
- notes: Reminder/notification jobs (supplier 20h nudge, hirer 60-min payment warning) travel with the FSD §9 matrix in **4F** (they're notification-shaped). Dispute freeze + the withheld-day payout enforce in 4F (Payout model). ruff+format+mypy clean (41 files). Next: Slice 4F — payouts (pending→due→paid|frozen + D-015 withheld-day), daily Bachs reconciliation, FSD §9 notification matrix, completion money invariant. **Live-Bachs validation still pending founder egress allowlist for `sandbox-api.bachs.io` (403 from this env's egress proxy); key stored in gitignored .env.**

## 2026-06-19 - Wave 4 Slice 4F: payouts + reconciliation + notifications (WAVE BUILD COMPLETE)
- tag: FEATURE
- area: backend/payments (models.py [+Payout], enums.py [+PayoutState/PayoutKind], services.py [+create_completion_payout/freeze_payouts/mark_payout_paid/reconcile, retained() now counts paid payouts, withheld-day payout in issue_refund, strike email], bachs.py [+list_ledger], tasks.py [+daily_reconciliation], migrations/0002_payout, tests/test_payouts.py), backend/hires (notifications.py [new — FSD §9 email matrix], tasks.py [+send_due_reminders], state.py [post-commit notification dispatch], services.py [payout wiring on completion paths + dispute freeze], management/commands/run_hire_sweeps.py [+reminders], tests/test_notifications.py)
- summary: FSD §3.2/§9, TSD §3.6, D-015. **Payouts**: `Payout(pending→due→paid|frozen)`, one completion payout per hire (unique (hire,kind)); created `due` on completion via all three completion paths (auto-complete sweep, off-hire handover confirm, dispute resolve-complete); a dispute freezes due payouts; the **D-015 withheld-day** payout is created on a ≤72h hirer cancel; `mark_payout_paid` records the Ops reference (weekly manual payout; Ops UI Wave 6). **Completion money invariant**: `retained()` now nets paid payouts → `collected − refunded − paid_out − service_fee ≡ 0` once the payout settles (tested). **Reconciliation**: `reconcile(ledger)` diffs the Bachs ledger vs local SUCCESS payments (missing/amount/status/extra), logs mismatches at error (Sentry) — daily task. **Notifications (FSD §9)**: `hires/notifications.py` fires per-transition emails post-commit from the state machine — **D-014 enforced** (hirer accepted/receipt/warning copy shows only the total; supplier gets the breakdown; tested both ways), supplier prefs honoured, strike email; windowed reminder sweeps (supplier 20h nudge, hirer 60-min payment warning — idempotent by the ~5-min window).
- reason: Wave 4 slice 4F — closes the money loop (payouts), the reconciliation launch criterion, and the notification matrix.
- change_ref: 2026-06-19 - Wave 4 Slice 4E
- notes: Green — full suite + combined hires+payments coverage (see commit); ruff+format+mypy clean (44 files); migration 0002 reversible; no new API endpoints (payouts/reconciliation/notifications are internal services + tasks + the Ops-facing mark-paid service) so OpenAPI shape unchanged. **Wave 4 build COMPLETE (4A–4F).** Wave-end checklist: [x] FSD §3.1 vectors + §7.6 refund rows named tests, [x] hire_events append-only (model + DB REVOKE), [x] OpenAPI committed, [ ] **founder demo** (5 sandbox hires) — gated on Bachs egress allowlist + `BACHS_WEBHOOK_SECRET` + Railway env, [ ] **founder sign-off before Wave 5**. Carry-overs: live-Bachs validation pending egress for `sandbox-api.bachs.io`; need `BACHS_WEBHOOK_SECRET`; in-app notification *badges* (vs email) ride the Wave 5 realtime surface; reconciliation ledger-fetch shape (`/payments/charges`) is a D-017 assumption to confirm against the real API.

## 2026-06-19 - Bachs adapter aligned to docs.bachs.io + prod E2E re-run
- tag: FIX
- area: backend/payments/bachs.py, services.py, errors.py, hires/services.py, hires/views.py, scripts/wave4_prod_test.py
- summary: Founder enabled Railway `BACHS_*` (`/readyz` → `bachs: configured`). **Merged PR #29 + follow-up on `main`:** adapter matches https://docs.bachs.io/ (`pricing`+`customer_email`→`checkout_url`, payins verify/ledger, refunds path); sandbox `simulated_outcome=success`; accept survives Bachs failure; GET payment returns `checkout_unavailable` (503) not 500. **Local:** 129 tests, 93% hires+payments coverage. **Prod E2E:** readyz/auth/create/accept/D-014/decline/list ✓ · payment **`checkout_unavailable`** · webhook→Confirmed ✗. Bachs sandbox org still **`payments_status: disabled`** → `POST /v1/checkouts` returns 404.
- reason: Full payment E2E after Railway env; old adapter shape caused accept 500 / no checkout.
- change_ref: 2026-06-19 - Wave 4 Slice 4F
- notes: **Founder action on Bachs dashboard:** complete sandbox onboarding until `payments_status` ≠ `disabled`. Railway side is done. Re-run `python3 scripts/wave4_prod_test.py` after Bachs enables payments.

## 2026-06-21 - Switch payment provider to Paystack (D-018, pluggable gateway)
- tag: DECISION | FEATURE
- area: backend/payments (contracts.py [new], paystack.py [new], gateway.py [new], services.py, views.py, tests/{test_paystack.py [new], test_payments.py, test_payouts.py}), backend/settings/{base,test}.py, backend/.env.example, docs/v2/DECISIONS.md (D-018)
- summary: Founder switched the MVP payment provider back to **Paystack** (D-018, supersedes D-017's Bachs.io). Made the provider **pluggable**: `settings.PAYMENT_PROVIDER` (`paystack` default | `bachs`) selected behind `payments.gateway`, a provider-neutral facade exposing `create_checkout / verify_charge / create_refund / verify_signature / parse_webhook / list_ledger` over the shared `payments.contracts` shapes (`Charge`, `WebhookEvent`, `CURRENCY`). New `payments/paystack.py` adapter: **integer-kobo native** (no decimal conversion), `POST /transaction/initialize` (email required), verify-before-transition via `GET /transaction/verify/{reference}` (status=success), webhook `charge.success` signed **HMAC-SHA512 of the raw body with the secret key** (`x-paystack-signature`, no timestamp), dedup on `(event, reference)`, refund via `POST /refund` (by reference), ledger via `GET /transaction?status=success`. Bachs adapter **retained untouched** behind the gateway; services/webhook view no longer import a provider directly. Domain unchanged (kobo, state machine, §7.6, payouts, D-014).
- reason: Founder decision to use Paystack for the MVP.
- change_ref: 2026-06-19 - Wave 4 Slice 4F
- notes: Paystack test secret key stored in **gitignored .env** (never committed); `.env.example` documents `PAYMENT_PROVIDER` + `PAYSTACK_*`. settings.test pins keys empty + `PAYMENT_PROVIDER=paystack` (hermetic). Webhook/processor tests rewritten Paystack-flavored + new `test_paystack.py` adapter unit tests + gateway provider-selection test; Bachs adapter tests retained. **Live validation still blocked by this env's egress allowlist** (`api.paystack.co` 403 — same as Bachs); set egress + `PAYSTACK_SECRET_KEY`/`PAYMENT_PROVIDER=paystack` in Railway natural-cat for the live demo. On a feature branch `claude/switch-to-paystack` → PR.

## 2026-06-21 - Production E2E test pass (Waves 0-4)
- tag: TEST
- area: scripts/{wave0_3_prod_test,wave4_prod_test,live_api_test,comprehensive_prod_test}.py; prod API; portal Workers
- summary: Ran extensive production smoke/E2E against natural-cat. W0 13/14 (healthz/readyz all integrations configured incl. LocationIQ; admin/static/portal live; OpenAPI schema OK). W1 12/12 (supplier+hirer login, JWT refresh, /me, validation, password-reset no-enumeration). W2 12/12 (all 5 spec templates, yards, live listing, storefront, R2 presign). W3 8/8 (map/list/radius/geocode with results, availability flag present). W4 17/17 comprehensive (hire create-accept, D-014, Paystack checkout URLs, state-machine guards, decline/cancel, events trail, availability_conflict on overlap). wave0_3_prod_test 14/14; live_api_test 20/21 (Termii SMS 502 on new register). wave4_prod_test 15/17 (script still asserts Bachs URLs; prod uses Paystack per D-018). Paystack authorization_url confirmed live. Fee spot-check: 285k hire / 34.2k fee / 250.8k payout. CORS preflight from portal origin 200. Local pytest blocked (no Docker/GDAL).
- reason: Founder requested full Waves 0-4 production validation.
- change_ref: 2026-06-21 - Switch payment provider to Paystack (D-018, pluggable gateway)
- notes: Gaps: (1) Termii sender Terminal pending - phone OTP 502 for fresh registrations. (2) Payment-Confirmed needs real Paystack sandbox payment + webhook. (3) Update wave4_prod_test.py for Paystack. (4) Add portal URL to CORS_ALLOWED_ORIGINS before Wave 7. (5) Local suite needs docker compose + GDAL (~1001 test functions; CI green on main).

## 2026-06-21 - Wire Paystack callback/redirect URL
- tag: FEATURE
- area: backend/payments/paystack.py (create_checkout), backend/settings/{base,test}.py, backend/.env.example, backend/payments/tests/test_paystack.py
- summary: `transaction/initialize` now sends `callback_url` (where Paystack redirects the payer's browser after checkout) when `settings.PAYSTACK_CALLBACK_URL` is set; default = the deployed Supplier Portal `https://terminal-portal.nwabueze.workers.dev/`, env-overridable. UX only — confirmation stays webhook-driven (`charge.success` → verify-before-transition); the callback/redirect is never trusted to mark a hire paid. settings.test pins it empty (hermetic). Bachs adapter + gateway untouched (callback is Paystack-only).
- reason: Founder: point callback/redirect at the portal so a real hosted-checkout payment returns to Terminal.
- change_ref: 2026-06-21 - Switch payment provider to Paystack (D-018)
- notes: **Manual founder steps (not code):** (1) Paystack dashboard → API Keys & Webhooks → **Webhook URL** = `https://api-production-101c8.up.railway.app/api/v1/payments/webhook`; (2) Railway natural-cat env — confirm `PAYMENT_PROVIDER=paystack`, `PAYSTACK_SECRET_KEY`, optionally override `PAYSTACK_CALLBACK_URL`. **Out of scope / gated:** the portal-side `/payment/callback` page is Hirer-app/Portal work (Waves 7/8, gated); for the demo it's optional since confirmation is webhook-driven. Tests: callback present-when-set / absent-when-empty. On branch `claude/paystack-callback-url` → PR.

## 2026-06-21 - Merged PR #31 Paystack callback_url
- tag: DEPLOY
- area: backend/payments/paystack.py, settings/base.py; PR #31 → main (ad093e5)
- summary: Merged `payments: send Paystack callback_url (redirect to the portal)`. Paystack `transaction/initialize` now includes `callback_url` defaulting to `https://terminal-portal.nwabueze.workers.dev/`; confirmation remains webhook-driven.
- reason: Founder approved proceeding after review.
- change_ref: 2026-06-21 - Wire Paystack callback/redirect URL
- notes: Railway natural-cat auto-deploy from main. Founder: confirm Paystack dashboard webhook URL = `https://api-production-101c8.up.railway.app/api/v1/payments/webhook`. Portal callback UI still Wave 7/8.

## 2026-06-21 - Wave 4 FOUNDER SIGN-OFF (Hires & Money complete)
- tag: DECISION
- area: wave gating; docs/waves/README.md, CLAUDE.md, Implementations.md (Current status)
- summary: Founder signed off **Wave 4 (Hires & Money)** ("sign off wave 4"). Wave 4 is complete, merged, and deployed: slices 4A–4F (fee engine · hire state-machine + availability · request/accept/decline/cancel + D-014 · collect + §7.6 refunds · sweeps/handovers/disputes · payouts/reconciliation/notifications, PR #27), then the payment provider was switched to **Paystack** behind a pluggable gateway (**D-018**, PR #30) and the `callback_url`/redirect wired to the portal (PR #31). ~373 tests green; combined hires+payments coverage ~88% (≥85% gate). **Wave 5 (Messaging) is now UNBLOCKED but still requires explicit founder go to start** (gating remains binding).
- reason: Founder sign-off — the binding gate that unblocks Wave 5 (design.md §7; wave-4 wave-end checklist).
- change_ref: 2026-06-21 - Wire Paystack callback/redirect URL
- notes: **Deploy-only carry-over for live Paystack payments** (not code): set the Paystack **dashboard Webhook URL** = `https://api-production-101c8.up.railway.app/api/v1/payments/webhook`; confirm `PAYMENT_PROVIDER=paystack` + `PAYSTACK_SECRET_KEY` in Railway natural-cat. Bachs adapter retained behind the gateway (flip via `PAYMENT_PROVIDER`); note `BACHS_API_BASE` is missing its `/v1` suffix in the founder's env (only matters if flipping back to bachs). Wave 4 contracts (`hires/*`, `payments/webhook`, payment-status) are frozen → Waves 7/8 may also start in parallel once founder-approved (TSD §10). Next wave to confirm: **Wave 5** (`docs/waves/wave-5.md`).

## 2026-06-21 - Wave 5 (Messaging) BUILD COMPLETE — conversations, masking, Ably realtime
- tag: FEATURE
- area: backend/messaging (new app: enums, models, masking, services, serializers, views, urls, errors, factories, management/commands/backfill_hire_conversations, tests/*), backend/core (realtime.py [new Ably adapter], urls.py, tests/test_realtime.py), backend/hires (services.py [accept→ensure_hire_conversation], state.py [hire-status realtime publish]), backend/openapi/schema.yml
- summary: Built Wave 5 end-to-end (FSD §8/§9, TSD §3.3/§3.10/§4, D-011). **Realtime adapter** `core/realtime.py` — hand-rolled httpx (no SDK), keyless-degraded: `publish()` (REST, no-op when keyless) + `create_token_request()` (HMAC-SHA256-signed Ably TokenRequest, canonical capability). Lives in `core` so both `messaging` and `hires` publish without an app cycle. **Models** (`messaging 0001`, reversible): `Conversation(kind=enquiry|hire)` — listing-vs-general distinguished by `listing` nullability; partial-unique `(listing,hirer)` + `(supplier,hirer)`-when-general + unique `hire`; `Message(body, body_masked, read_at)` append-only content. **Masking** (`masking.py`, write-time): NG phones `(\+?234|0)[789][01]\d{8}` + spaced/dotted/dashed variants + emails → `body_masked`; `(?<!\d)/(?!\d)` guards keep prices/RC numbers intact; original `body` retained for Ops. **Serve rule**: hire convs serve original once the hire has **ever** reached Confirmed (checked via append-only `HireEvent` to_status=confirmed → permanent unlock, survives later cancel/dispute); enquiry convs masked forever; per-message `masked` flag for the client 🔒 explainer; **original never serialized pre-unlock**. **API** (all `/api/v1/`, participants-only → **404 not 403**): `POST/GET conversations` (idempotent enquiry create + FSD §8 list rows + aggregate `unread_total`), `GET/POST conversations/:id/messages`, `POST messages/read` (bulk, derived unread), `GET realtime/token` (scoped to caller's own `conv:`/`user:` channels; keyless → `not_configured`). **Realtime fan-out** post-commit: message→`conv:{id}` + unread→`user:{counterpart}`; **hire-status retro-wired** into `hires/state.py::_queue_side_effects`→`user:{hirer|supplier}` via `core.realtime` (no hires→messaging import). **Wave 4C hookup**: `accept_hire` now creates the hire conversation post-commit (idempotent) + `backfill_hire_conversations` command for the stub gap.
- reason: Wave 5 build (founder-approved start; plan reviewed + approved in-session). Completes the conversation loop enquire→request→accept→chat→pay→contact-unlocks.
- change_ref: 2026-06-21 - Wave 4 FOUNDER SIGN-OFF (Hires & Money complete)
- notes: **Local suite GREEN — 422 passed** (messaging 50 + core realtime 5 + the rest), ruff+format clean, **mypy clean** on the CI target (`core accounts suppliers listings search settings`), migration reversible (up/down/up verified), OpenAPI regenerated (+messaging paths, 0 errors). Coverage 92.6% overall (gate 70%). FSD §8 acceptance checks all pass as named tests (masking matrix, unmask-after-Confirmed + different-hire-stays-masked + enquiry-masked-forever, 404 non-participant, enquiry uniqueness, hire-conv idempotent auto-create + backfill, unread race, token scoping). **Local test env note:** this env has no Docker; ran Postgres 16 + `postgresql-16-postgis-3` natively and set `GDAL_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu/libgdal.so.34` (host GDAL via `libgdal34t64`) — `prior sessions' "local pytest blocked" is resolved this way`. **Deploy carry-over (not code):** `ABLY_API_KEY` is **unset in prod** (Railway natural-cat) — everything works by 15s polling without it; set it for the realtime half of the founder demo (two devices live-chatting; enquiry masked, hire unmasks after Paystack test payment; pull the key → polling, zero loss). **Wave-end checklist remaining:** Ably 70%-free-tier usage alert into the weekly digest (Wave 6 digest inputs); **founder demo + sign-off before Wave 6.** Contracts (`conversations/*`, `messages/read`, `realtime/token`) freeze at wave end. On branch `main-u2o18d` → PR (draft).

## 2026-06-21 - Wave 5 FOUNDER SIGN-OFF (Messaging complete)
- tag: DECISION
- area: wave gating; docs/waves/README.md, CLAUDE.md, Implementations.md (Current status)
- summary: Founder ran the demo and signed off **Wave 5 (Messaging)** ("demo done sign off wave 5"). Wave 5 is complete, merged (PR #35), and deployed to natural-cat: the `messaging` app (conversations — enquiry listing/general + hire auto-created at acceptance; server-side contact masking with permanent unlock once a hire reaches Confirmed; unread counters; participants-only 404), `core/realtime.py` (Ably REST publish + HMAC-signed TokenRequest scoped to the caller's own `conv:`/`user:` channels, keyless-degraded to 15s polling), hire-status realtime fan-out wired into `hires/state.py`, and the Wave 4C hire-conversation hookup (auto-create + backfill command). 422 tests green; coverage 92.6%; OpenAPI regenerated. **Wave 5 contracts (`conversations/*`, `messages/read`, `realtime/token`) are now FROZEN.** **Wave 6 (Ops Console) is unblocked but still requires explicit founder go to start** (gating remains binding; design.md §7).
- reason: Founder sign-off — the binding gate that unblocks Wave 6 (and clears the Messages-slice dependency for Waves 7/8).
- change_ref: 2026-06-21 - Wave 5 (Messaging) BUILD COMPLETE — conversations, masking, Ably realtime
- notes: **Deploy-only carry-over (not code):** set `ABLY_API_KEY` in Railway natural-cat to enable live realtime fan-out — messaging works fully via 15s polling without it. **Wave 6 input:** wire the Ably 70%-free-tier usage alert into the weekly digest (the one remaining wave-5 wave-end checklist item; it belongs with the Wave 6 digest work). Next wave to confirm: **Wave 6** (`docs/waves/wave-6.md`).

## 2026-06-22 - Wave 6 Slice 6A — Admin hardening + 2FA (django-otp TOTP)
- tag: FEATURE
- area: backend/pyproject.toml, settings/{base,prod}.py, core/admin.py, ops/{admin_site,signals,apps}.py, ops/management/commands/enroll_totp.py, accounts/{models,serializers}.py + 4 callers, ops/tests/*, .env.example
- summary: First slice of Wave 6 (Ops Console). Added **django-otp + qrcode**; `django_otp`+`otp_totp` apps + `OTPMiddleware`. The default `admin.site` is re-pointed (`__class__` swap — the documented django-otp pattern, no re-registration) to `OpsAdminSite` (dev/CI) or `OpsOTPAdminSite` (prod) based on new `ADMIN_2FA_REQUIRED` (env; prod default ON). Admin session cookie isolated (`SESSION_COOKIE_NAME=terminal_ops_sessionid`, `SESSION_COOKIE_PATH=/admin/`). Login IP logging via `user_logged_in`/`user_login_failed` receivers (structlog, admin-path-scoped). `enroll_totp` management command provisions/rotates a confirmed `TOTPDevice` + prints otpauth URL/QR (founder enrolment + lockout recovery). **Name-clash fix:** `User.is_verified` (account-level property) collided with django-otp's request-level `user.is_verified()` injection → renamed the property to **`is_account_verified`** (matches existing `is_phone_verified`/`is_email_verified`) and updated 5 callers; the `/me` API field stays `is_verified` via `source=` (frozen contract intact).
- reason: Wave 6 §6.1 admin hardening (TSD §7) — the foundation every later queue rides on.
- change_ref: 2026-06-21 - Wave 5 (Messaging) BUILD COMPLETE
- notes: **Local suite GREEN — 432 passed** (ops 9 new), ruff+format clean, `makemigrations --check` clean (property rename = no schema change), `.env.example` coverage check passes. Local env rebuilt from scratch this session: `apt install postgresql-16-postgis-3 libgdal34t64 libgeos-c1t64 gdal-bin`, created `terminal` DB + `CREATE EXTENSION postgis`, `uv sync`; GDAL auto-discovered at `/usr/lib/x86_64-linux-gnu/libgdal.so.34` (no GDAL_LIBRARY_PATH needed). **Deploy carry-over:** before flipping `ADMIN_2FA_REQUIRED=on` in Railway prod, run `manage.py enroll_totp <staff-email>` and verify a TOTP login, or you lock out of the admin. Next: Slice 6B (dashboard + metrics).

## 2026-06-22 - Wave 6 Slice 6B — Ops dashboard + metrics aggregation
- tag: FEATURE
- area: backend/ops/{models.py [new ReconciliationRun], migrations/0001_initial.py, services/metrics.py [new], admin_site.py}, backend/templates/admin/ops_index.html [new], ops/tests/{test_metrics,test_dashboard}.py
- summary: Wave 6 §6.2. New `ops.ReconciliationRun` model (run_at/checked/mismatch_count/mismatches jsonb; pulled forward from 6F so the dashboard's reconciliation tile has data — the daily task writes it in 6F). `ops/services/metrics.py`: single-pass read-only aggregates — `gmv_and_fees()` (Σ over hires with a SUCCESS payment, D-014 fee is Ops-only), `payout_liability()` (Σ due+frozen), `hires_by_state()` (zero-filled across all statuses), `new_users(7d)`, `queue_counts()` (pending verifications / due payouts / open reports / in-dispute hires), `reconciliation_status()`, and `dashboard_metrics()` returning pre-formatted naira (kobo never reaches the template). `OpsAdminSite.index` injects `ops_dashboard`; `templates/admin/ops_index.html` (set via `index_template`, named to avoid self-extend recursion) renders Money / Queues / Hires-by-state / Reconciliation panels in the Heavy Duty theme (money in IBM Plex Mono via `--ops-mono`).
- reason: Wave 6 §6.2 founder dashboard — the Ops Console landing page.
- change_ref: 2026-06-22 - Wave 6 Slice 6A
- notes: 20 ops tests green; ruff+format clean; migration `ops 0001` created; `makemigrations --check` clean. Next: Slice 6C (verification age column + Payout queue, mark-paid/freeze/unfreeze, payout-paid email, bank-number decrypt-only-here).

## 2026-06-22 - Wave 6 Slice 6C — Verification age column + Payout queue
- tag: FEATURE
- area: backend/payments/{admin.py [new], services.py, errors.py, tasks.py, models.py, templates/admin/payments/payout_action.html [new]}, accounts/{admin.py, integrations/email.py}, payments/tests/test_payout_admin.py, accounts/tests/test_admin_verification.py
- summary: Wave 6 §6.3/§6.4. **Verification queue:** added an SLA `age` column (>12h pending highlighted red, FSD §4.3). **Payout queue** (`PayoutAdmin`): due-first ordering, state/kind/created-week filters, full **decrypted** supplier bank details shown ONLY here (masked everywhere else); actions `mark_paid` (intermediate reference form → `services.mark_payout_paid` → supplier "Payout paid" email post-commit), `freeze_selected`/`unfreeze_selected` (single-payout, reason), `sum_selected` (batch total). New services `freeze_payout`/`unfreeze_payout`; `mark_payout_paid` now refuses a FROZEN payout (`PayoutFrozen`) and enqueues `notify_payout_paid` on commit. New email helper `send_payout_paid_email` + `payments.tasks.notify_payout_paid` (honours `notif_payouts`, best-effort). `Payout.amount_display` property for templated naira.
- reason: Wave 6 §6.3/§6.4 — the verification SLA surface and the Friday payout batch.
- change_ref: 2026-06-22 - Wave 6 Slice 6B
- notes: Full suite GREEN — **447 passed**; ruff+format clean; no new migrations. Mandatory tests covered: mark-paid stores reference + emails + state→paid; freeze blocks mark-paid; bank number full-only-on-payout-queue vs masked in SupplierProfileAdmin. Next: Slice 6D (reports/listing moderation: resolve/warn/remove + tier award + listings/state.py `remove`).

## 2026-06-22 - Wave 6 Slice 6D — Listings & reports moderation
- tag: FEATURE
- area: backend/listings/{state.py, admin.py, notifications.py [new], services/moderation.py [new], services/reports.py, templates/admin/listings/reason_action.html [new]}, listings/tests/test_moderation_admin.py
- summary: Wave 6 §6.5. Extended `listings/state.py::apply` with the Ops-only **`remove`** transition (live|paused|draft → removed, stores `removed_reason`; still the only status writer). New `listings/services/moderation.py`: `remove_listing` (reason mandatory, notifies supplier post-commit, hire history preserved — no cascade), `pause_listing`, `award_tier` (Basic/Verified/Inspected; tier ≠ status so set directly). New `listings/services/reports.py::resolve_report` (dismissed | warned | removed; warned/removed email the supplier; removed takes the listing down). New `listings/notifications.py` (best-effort supplier emails). `ListingAdmin` actions: pause / remove (reason form) / award tier (choice form) + `priority_review_flag` surfaced in list display/filter; `ReportAdmin` actions: dismiss / warn / remove (note/reason forms via shared `reason_action.html`).
- reason: Wave 6 §6.5 — reports queue + listing moderation surfaces over existing models.
- change_ref: 2026-06-22 - Wave 6 Slice 6C
- notes: Full suite GREEN — **455 passed**; ruff+format clean; no new migrations (REMOVED status + ReportState resolution values already existed in enums). Mandatory tests: remove reason-required + supplier-notified + status→removed + hires intact; tier award changes tier only; report dismiss/warn/remove write state + note. Next: Slice 6E (hires admin + dispute resolution + users admin/suspension cascade).

## 2026-06-22 - Wave 6 Slice 6E — Hires admin + dispute resolution + Users admin (suspension cascade)
- tag: FEATURE
- area: backend/hires/admin.py [new], hires/templates/admin/hires/hire_action.html [new], accounts/{models.py (+internal_notes), migrations/0006, admin.py, services/moderation.py [new]}, accounts/templates/admin/accounts/user_action.html [new], payments/services.py (+freeze_supplier_payouts), + LogEntry calls across accounts/payments/listings/hires admins; tests: hires/tests/test_hire_admin.py, accounts/tests/test_user_moderation.py, ops/tests/test_action_audit.py
- summary: Wave 6 §6.6/§6.7. **HireAdmin** (read-mostly): HireEvent timeline + Payment/Refund/Payout read-only inlines, full financials (Ops sees the fee — not a hirer surface), read-only conversation (original message bodies, FSD §8 dispute context), and the per-hire **money invariant** (`collected − refunded − paid_out − fee`; flagged red only when COMPLETED + payout settled + nonzero). Actions: resolve dispute → Completed/Cancelled and admin cancel — all via existing `hires.services` (state machine, append-only HireEvent). **Users admin**: suspend (reason) / reactivate / strike adjust + `internal_notes` field (migration 0006). New `accounts/services/moderation.py`: `suspend_user` (sets suspension, bumps token_version to kill sessions, freezes supplier payouts via new `payments.services.freeze_supplier_payouts`), `reactivate_user` (payouts stay frozen — deliberate), `adjust_strikes` (clamped). **Audit:** every admin-only mutation now calls `self.log_change` → Django LogEntry (domain transitions keep writing HireEvent).
- reason: Wave 6 §6.6/§6.7 — dispute resolution, hire visibility, user management; satisfies the "every Ops mutation is event-logged" mandate.
- change_ref: 2026-06-22 - Wave 6 Slice 6D
- notes: Full suite GREEN — **470 passed**; ruff+format clean; migration 0006 reversible. Mandatory tests: suspension cascade (one test — login blocked + search-hidden + storefront 404 + payouts frozen); dispute → Completed/Cancelled with payout/refund + invariant balanced once settled; admin cancel reason-required; parametrized audit-trail (LogEntry/HireEvent per action). Next: Slice 6F (digests + reconciliation history: ReconciliationRun persistence, weekly digest with Ably real + R2 best-effort, mgmt commands).

## 2026-06-22 - Wave 6 Slice 6F — Digests & reconciliation surfacing
- tag: FEATURE
- area: backend/ops/{admin.py [new ReconciliationRunAdmin], tasks.py [new], services/{metrics.py (+queue_ages), digest.py [new]}, management/commands/{run_weekly_digest,run_daily_reconciliation}.py [new]}, core/{realtime.py (+ably_usage), media.py (+r2_usage)}, accounts/integrations/email.py (+send_ops_email), payments/tasks.py (daily_reconciliation persists + alerts), settings/base.py (+OPS_DIGEST_RECIPIENT), .env.example
- summary: Wave 6 §6.8. **Reconciliation**: `payments.tasks.daily_reconciliation` now fetches the **provider-neutral** ledger (`gateway.list_ledger`, was hard-coded Bachs), persists a `ReconciliationRun`, and emails Ops on mismatch; read-only `ReconciliationRunAdmin` renders run history + mismatch drill-down. **Weekly digest**: `ops.tasks.send_weekly_digest` builds from `ops/services/digest.py` (DB metrics + queue ages + **Ably real** usage via `core.realtime.ably_usage` REST `/stats` with the 70%-free-tier alert + **R2 best-effort** storage via `core.media.r2_usage`) and emails `OPS_DIGEST_RECIPIENT` (falls back to DEFAULT_FROM_EMAIL). Both keyless-degrade to "n/a". **Scheduling**: management commands `run_daily_reconciliation` / `run_weekly_digest` (Railway cron, mirrors `run_hire_sweeps`).
- reason: Wave 6 §6.8 — completes the reconciliation report + weekly founder digest; closes the wave-5 Ably-70%-alert carry-over.
- change_ref: 2026-06-22 - Wave 6 Slice 6E
- notes: Full suite GREEN — **476 passed**; ruff+format clean; env-coverage passes; no model changes (ReconciliationRun shipped in 6B). Digest scope per founder: Ably real + R2 best-effort. Deploy carry-over: schedule Railway cron for `run_daily_reconciliation` (daily) + `run_weekly_digest` (weekly); set `OPS_DIGEST_RECIPIENT`; `ABLY_API_KEY` enables real Ably stats. Next: Slice 6G (runbooks, OpenAPI regen, wave-end docs).

## 2026-06-22 - Wave 6 Slice 6G — Runbooks, OpenAPI, wave-end docs
- tag: CHORE | DOCS
- area: docs/runbooks/{payout-batch,refund-handling,dispute-resolution}.md [new], backend/openapi/schema.yml (regenerated — no diff), CLAUDE.md, Implementations.md, docs/waves/README.md
- summary: Wave 6 wave-end. Drafted the three Ops runbooks (TSD §9 — payout batch, refund handling, dispute resolution; harden in Wave 9). Regenerated the full OpenAPI: **byte-identical** to the committed `schema.yml` — Wave 6 is admin-only (no API endpoints) and the `User.is_verified`→`is_account_verified` rename preserved the `/me` `is_verified` field via `source=`, so all frozen Wave 2–5 contracts are untouched. Updated the docs snapshots (CLAUDE.md, Implementations.md Current status, docs/waves/README.md) to: Waves 0–5 signed off, **Wave 6 built (this branch) pending founder demo + sign-off**.
- reason: Wave 6 wave-end checklist (runbooks, OpenAPI regen, status reconciliation).
- change_ref: 2026-06-22 - Wave 6 Slice 6F
- notes: **Wave 6 BUILD COMPLETE (6A–6G).** Full suite GREEN — **476 passed**, ruff+format clean, migrations reversible (ops 0001, accounts 0006), `.env.example` coverage passes, OpenAPI unchanged. Wave-end checklist: [x] every FSD §10.3 surface exists, [x] runbooks drafted, [x] OpenAPI regenerated/committed, [ ] 2FA live in prod (deploy step — enrol device first), [ ] **founder demo + sign-off before Wave 7**. Branch `claude/focused-mccarthy-69u1ju` → draft PR.

## 2026-06-22 - Wave 6 FOUNDER SIGN-OFF (Ops Console complete)
- tag: DECISION
- area: wave gating; CLAUDE.md, docs/waves/README.md, Implementations.md (Current status)
- summary: Founder signed off **Wave 6 (Ops Console)** ("signoff wave 6, the carryovers have all been addressed"). Wave 6 is complete, **merged to `main` (PR #37, `claude/focused-mccarthy-69u1ju`)**, and deployed to natural-cat via auto-deploy: slices 6A–6G — admin hardening + django-otp TOTP 2FA, founder dashboard + metrics, verification age + payout queue, listings/reports moderation, hires admin + dispute resolution + users/suspension cascade, weekly digest + reconciliation history, runbooks. 476 tests green; coverage gates held; OpenAPI unchanged (admin-only, no API endpoints added). Founder confirmed the deploy-only carry-overs are addressed: TOTP device enrolled (before `ADMIN_2FA_REQUIRED=true`), `OPS_DIGEST_RECIPIENT` set, Railway cron scheduled for `run_daily_reconciliation` + `run_weekly_digest`, and `ABLY_API_KEY` set. **Wave 7 (Supplier Portal) is unblocked but still requires explicit founder go to start** (gating remains binding; design.md §7).
- reason: Founder sign-off — the binding gate that closes Wave 6 and clears the backend dependency for Waves 7/8.
- change_ref: 2026-06-22 - Wave 6 Slice 6G — Runbooks, OpenAPI, wave-end docs
- notes: Reconciled the doc drift found at session start (Implementations.md + CLAUDE.md still said "built on branch, not yet merged" though PR #37 had merged): CLAUDE.md current-state snapshot, docs/waves/README.md status column (Wave 6 → ✅ signed off; Waves 7/8 → backend deps met, awaiting founder approval), and this Current status block all updated. Remaining deploy-only carry-over (Wave 4, not Wave 6): set the Paystack dashboard Webhook URL → `https://api-production-101c8.up.railway.app/api/v1/payments/webhook`. Next wave to confirm: **Wave 7** (`docs/waves/wave-7.md`).

## 2026-07-03 14:30 - Wave 7 founder vision shaping session (48-question elicitation)
- tag: DECISION
- area: docs/waves/wave-7-vision.md [new]
- summary: Ran a 48-question founder elicitation for Wave 7 (Supplier Portal) visual/UX vision; captured the binding preferences in docs/waves/wave-7-vision.md (additive to wave-7.md, which stays normative). Headlines: premium+industrial feel benchmarked against Linear/Stripe/Mercury; light theme + ink-900 rail (single theme, no dark mode this wave); restrained color; container-corner-bracket motif; hero mono money (full value always); full asset-class identity kit (custom icons + tinted chips + illustrated fallback covers); crafted duotone empty states; curated free-stock duotone port photography with slow pan on P1; contextual-headline dashboard with escalating "Action needed"; phone+web listing preview toggle; market-position pricing bar; lifecycle rail + hero LockedTerms + receipt-style refund preview; full Gantt kit (status blocks, class tints, now-rule, utilisation); hold-to-confirm handover; B2B-hybrid messages; designed OG share card; bank-details vault treatment; precise/restrained motion with exactly 3 signature moments; strictly silent; real mobile polish for P2/P6/P7/P9; deploy 7A to Cloudflare Workers immediately; founder direction-check after 7A before building outward. Wordmark: founder vision = rounded square with excavator — workshop together at 7A.
- reason: Founder requested a vision-drawing session before Wave 7; "the supplier portal should be visually appealing and crazy good looking" — acid test recorded (screenshot-proud, supplier-wow, holds beside north stars, details survive zoom).
- notes: **Formal Wave 7 go NOT yet given** — founder chose "shaping only". Gate remains binding: confirm explicit approval before slice 7A. Branch wave-7-supplier-portal already exists; portal/package.json has an uncommitted local modification (pre-existing) — reconcile at wave start.

## 2026-07-03 16:00 - Wave 7 FOUNDER GO + build plan approved
- tag: DECISION
- area: wave gating; docs/waves/{README.md, wave-7-vision.md}
- summary: Founder approved the Wave 7 build plan (assembled from 3 exploration passes + 2 design passes), which constitutes the formal Wave 7 go (design.md §7). Plan decisions ratified with it: (1) **money count-up DROPPED** — ch.08 "money never animates" stands; signature moments are terms-lock beat + publish "Live" beat only (wave-7-vision.md amended); (2) **5 additive backend endpoints approved** as Slice 7-0 (refund-preview wrapping compute_refund_plan, supplier payouts+summary, hires stats, hires events, additive yard/photo fields + date filters on GET /hires) — additive-only, frozen contracts untouched, OpenAPI to be regenerated; (3) **D-020 ratified**: unstyled headless allowlist (@radix-ui dialog/popover/tooltip/slot + @tanstack/react-table + react-virtual) refining D-019's bespoke-components rule — formal DECISIONS.md entry lands with governance Bundle A in 7A, alongside D-021 (corner-bracket panel motif extension). Slice order: 7-0 backend → 7A foundation/deploy (ends with founder direction check) → 7B supply → 7C hires/money → 7D messages/storefront/settings → 7E hardening. Full plan archived in the session plan file; key content mirrored across wave-7.md + wave-7-vision.md + this log.
- reason: Wave gating (binding) — explicit founder approval recorded before build start.
- change_ref: 2026-07-03 14:30 - Wave 7 founder vision shaping session (48-question elicitation)
- notes: Branch `wave-7-supplier-portal` (local, 2 commits ahead of origin/main: Wave 6 sign-off docs + tokens/D-019). Next: Slice 7-0 backend additions PR. Carry-over reminder: Paystack dashboard webhook URL still unset (Wave 4 deploy-only).

## 2026-07-03 18:30 - Wave 7 Slice 7-0 VERIFIED green + OpenAPI regenerated
- tag: FIX | CHORE
- area: backend/payments/tests/test_payout_api.py, backend/openapi/schema.yml (regenerated, additive-only)
- summary: Ran the full verification pass slice 7-0 deferred ("tests not yet run" on dcb83b8). One test-data bug found and fixed: `test_summary_queue_frozen_and_month_windows` gave the previous-month payout `paid_at=timezone.now()`, so `last_paid` (ordered by `-paid_at`) deterministically picked it over the intended row — its `paid_at` now matches its last-month `created_at` (the service was correct; the fixture was self-inconsistent). Regenerated OpenAPI: **+362 lines, 0 deletions** — purely additive (new paths `hires/{id}/refund-preview`, `hires/stats`, `hires/events`, `payments/payouts`; `from`/`to` params + yard/photo fields on GET /hires); all frozen Wave 2-5 contracts byte-untouched.
- reason: Slice 7-0 was committed blind (no Postgres on the authoring machine); Definition of Done requires the suite green + OpenAPI committed before the next slice.
- change_ref: 2026-07-03 16:00 - Wave 7 FOUNDER GO + build plan approved
- notes: **All gates green locally** (native PG16+PostGIS, GDAL libgdal.so.34): **503 passed** (476 Wave 6 + 27 new), coverage 91.2% overall (gate 70%) / **91.1% hires+payments (gate 85%)**, ruff+format clean, mypy clean on the CI target, `makemigrations --check` clean, `.env.example` completeness passes, spectacular --validate 0 errors. Slice 7-0 complete on branch `wave-7-supplier-portal-fg2lhk` -> draft PR. Next: Slice 7A (portal foundation/deploy - Bundle A governance incl. formal D-020/D-021 DECISIONS.md entries; ends with founder direction check).

## 2026-07-03 23:15 - Wave 7 Slice 7A BUILT — portal foundation, BFF auth, P1, frame, P12, component sheet
- tag: FEATURE
- area: portal/ (app, components, lib, middleware, vitest), packages/tokens/glyphs/brand/t-crane.svg, docs/v2/DECISIONS.md (D-020/D-021), .github/workflows/portal.yml (+test step), portal/wrangler.toml (API_BASE_URL var)
- summary: Slice 7A end-to-end on `wave-7-supplier-portal-fg2lhk` (6 commits: governance -> foundation -> BFF -> frame -> P1 -> P12 -> sheet). **Governance Bundle A**: formal D-020 (headless allowlist) + D-021 (corner-bracket panels) in DECISIONS.md. **Foundation**: next/font self-hosted Archivo/Inter/Plex Mono wired to the token preset; TanStack Query; density custom properties + pre-paint boot script; focus/selection/reduced-motion discipline; hazard-stripe + skeleton utilities; vitest+RTL harness (in CI). **BFF (TSD §5)**: /auth/* (login mints httpOnly Secure SameSite=Lax pair, 60min/7d; logout revokes+clears; public F1 endpoints allowlisted) + /bff/* catch-all adding Bearer with single-flight-per-refresh-token 401 refresh + one retry -> session_expired event; 6 vitest cases incl. the mandatory single-flight + cookie-flag suites. **Frame**: ink-900 rail 240<->64 remembered, amber active bar, density toggle, PageHeader, (app) gate on refresh cookie. **P1+F1**: split-screen with crafted duotone port scene + slow pan (motion-safe); 6-cell paste-aware OTP (tested), 10:00 countdown, resend, dual-channel verify (phone->email), duplicate-phone "Sign in instead", abandoned-OTP resume from login (phone_not_verified/email_not_verified), account_suspended -> /suspended; zod mirrors DRF (normalize_ng_phone port + password policy, 11 parity tests). **P12**: 404/500 (error digest as support ref), suspended, MAINTENANCE=1 middleware; F12 re-auth modal (Radix dialog per D-020) preserving route. **Component sheet** at /styleguide — the founder direction-check artifact. Bespoke tokens-only components throughout (D-019): Button/TextField/Password/Banner/Card/Skeleton/StatusBadge/CountBadge/Money/CornerBracketPanel/OtpInput + brand lockups/T-crane glyph.
- reason: Wave 7 slice 7A per the approved build plan; founder direction check gates building outward.
- change_ref: 2026-07-03 18:30 - Wave 7 Slice 7-0 VERIFIED green + OpenAPI regenerated
- notes: All local gates green — vitest 22 passed, eslint clean, tsc clean, next build clean, `opennextjs-cloudflare build` compiles the Worker. **Deploy (founder or CI step — no Cloudflare credentials in this environment):** `cd portal && pnpm deploy` with `CLOUDFLARE_API_TOKEN`/account set; wrangler.toml now carries `API_BASE_URL` pointing at natural-cat prod. **Direction-check agenda (before 7B):** (1) wordmark workshop (rounded-square excavator vision vs 01 §1 plate — plate ships meanwhile); (2) P1 photography curation (image CDNs blocked from the build env; crafted duotone SVG port scene ships meanwhile — swap is one file); (3) theme/feel review on /styleguide. Backend CORS: not needed for the BFF (server-to-server), carry-over closed. Next: founder direction check -> Slice 7B (onboarding checklist, yards, asset form, P3 table, P2 dashboard).

## 2026-07-04 05:15 - Wave 7 Slice 7B BUILT — onboarding & supply management (P2/P3/P4/P5, F2/F9, J1)
- tag: FEATURE
- area: portal/ (lib/types+queries+naira+asset-classes, components/map|yards|assets|brand/class-glyphs, app/(app)/{dashboard,assets,assets/new,assets/[id],yards}), backend/openapi/fixtures/asset-form-parity.json (committed)
- summary: Slice 7B in five commits (7B-1..7B-5). **API layer**: typed DRF mirrors + TanStack hooks (whole-fleet pagination walk; versioned spec-template cache; yard/listing/activation mutations); naira<->kobo isolated at the form edge; class identity kit (5 custom glyphs + 02 §3 hues + spec_data type lists). **P5 Yards**: MapLibre wrapper (D-013; OpenFreeMap Liberty pending the Terminal Chart style), geocode search w/ keyless degradation, pin-drop primary, delete guard mapped to move-first copy, cluster auto-yard suggestion. **P4 Asset Form**: 6 steps per ux/03 — class cards/type, template specs + completeness tiers (Basic/Strong/Standout), pricing + D-008 14-day best-price sample + nudge + unit stepper/labels, 07 §9 photo pipeline (client resize -> presigned PUT w/ progress -> attach; drag-reorder; cover star; per-item retry), yard-chips-first location, review w/ phone/web toggle + F9 publish gates + inline F2 verification dialog (multipart through the BFF) + the "Live" beat. Per-step save: local until the API draft floor (class+type+title+desc+daily) then server PATCH per Next; resume both ways. **P3 Assets**: react-table DataTable (D-020), search/status/class/yard filters, group-by-yard, density-aware rows, edit/duplicate/pause(6s undo)/archive(07 §6 confirm), bulk pause/archive/assign-yard, hover status explainers. **P2 Dashboard**: contextual headline, corner-bracket 4-stat row (em-dash zeros; Action-needed amber w/ tiered countdown escalating to a banner <4h), F11 payout strip (frozen "on hold — dispute"), day-grouped activity feed, 07 §4 checklist + supplier-activation card.
- reason: Wave 7 slice 7B per the approved plan (founder said proceed; direction check pending async).
- change_ref: 2026-07-03 23:15 - Wave 7 Slice 7A BUILT
- notes: All gates green per commit (tsc, eslint, vitest 22, next build, Workers build unaffected). **Known follow-ups (recorded, not blocking):** (1) no DELETE photo endpoint exists — needs a founder-approved additive backend route (reorder/cover only today); (2) market-position pricing bar (vision) deferred — plain best-price hint ships; needs search/list integration; (3) per-yard dashboard stat tabs (J6) need yard-scoped stats — 7C candidate alongside GET /hires; (4) 30-day sparklines need a day-series endpoint — 7C/7E; (5) Terminal Chart map style JSON still to land in packages/tokens/map (7E); (6) R2 bucket CORS must allow the portal origin for browser presigned PUTs — deploy config item. Next: 7C (hires: P6/P7/P8, refund preview manifest, CalendarGantt) after founder direction-check feedback lands.

## 2026-07-04 06:30 - Wave 7 Slice 7C BUILT — hires: respond, monitor, money (P6/P7/P8)
- tag: FEATURE
- area: portal/ (lib/hire-domain(+tests), lib/queries hires hooks, components/hires/{locked-terms(+test),detail-parts}, app/(app)/hires/{page,[id],calendar})
- summary: P6 tabs table (Needs response w/ live countdown column soonest-first · Upcoming · On hire · History; hire value + payout mono; no bulk — deliberate). P7 detail 8+4: lifecycle rail w/ terminal branches, deadline banners, listing snapshot, HandoverRecords + hold-to-confirm (1.2s fill; two-step keyboard/reduced-motion), EventTimeline, **LockedTerms supplier hero — D-014's one portal home**, conversation slot for 7D; sticky action bar mirrors hires/state.py via legalSupplierActions (table-driven fidelity test, all 9 states); accept locks terms + operator/driver ack (plant/trucks); decline reason-required; cancel shows the §7.6 refund-preview manifest (7-0 endpoint, figures verbatim) + evidence note + strike copy; dispute w/ payout-freeze copy. P8 CSS-grid CalendarGantt (no dependency): yard groups, class-tinted rows + utilisation %, from/to month fetch, 02 §4 block styles (dashed tentative), today rule, weekend banding, click->P7.
- reason: Wave 7 slice 7C per plan.
- change_ref: 2026-07-04 05:15 - Wave 7 Slice 7B BUILT
- notes: Gates green: 37 vitest (incl. mandatory state-machine fidelity + LockedTerms server-figures-only), tsc, eslint, next build. Follow-ups: hirer card (level/completed count) needs hirer fields on the supplier-shaped HireSerializer (additive backend — founder approval); handover SUBMIT from the portal (photos capture) is app-first per spec, portal offers confirm; utilisation % is per-listing not per-unit (unit-level hire allocation isn't in the API). Next: 7D (P9 messages, P10 storefront, P11 settings).

## 2026-07-04 07:45 - Wave 7 Slice 7D BUILT — messages, storefront, settings (P9/P10/P11, F7)
- tag: FEATURE
- area: portal/ (app/(app)/{messages,storefront,settings}, app/s/[id] [new public share page], components/storefront/storefront-view, lib/types messaging shapes)
- summary: P9 two-pane Messages (thread filters, MaskedContact anatomy + 09 explainer, optimistic send w/ retry, bulk mark-read, 15s polling — Ably wiring is 7E, F7 says indistinguishable beyond latency, keyboard nav). P10 storefront preview-as-hirer + edit drawer (name/about/logo presign) + CAC upgrade CTA + Share -> new PUBLIC /s/[id] (SSR off the AllowAny storefront endpoint; OG card 7E). P11 settings: personal, business pointer, bank VAULT (inverse ink + brackets + masked mono + password re-auth ceremony + encryption note), 4 notif toggles, verification status/resubmit, account (reset email; delete w/ 30-day copy, API-guarded), legal.
- reason: Wave 7 slice 7D per plan.
- change_ref: 2026-07-04 06:30 - Wave 7 Slice 7C BUILT
- notes: Gates green (tsc, eslint, 37 vitest, build). P9 hire side-strip is minimal (kind+listing) — ConversationSerializer exposes no hire_id; LockedTerms-mini needs an additive field (founder-approved backend change, queue with the photo-DELETE + hirer-card asks). P7's conversation slot still placeholder — wiring the hire conv into P7 rides with the Ably work in 7E. Next: 7E hardening (Stage-6 sweep, Sentry, Ably realtime, Playwright smoke, OG card, mobile polish).

## 2026-07-04 14:15 - Wave 7 Slice 7E BUILT — hardening: realtime, Sentry, Playwright green, OG card, mobile
- tag: FEATURE | FIX
- area: portal/ (map grade, icon.svg, offline banner, mobile layouts, lib/realtime + shell/realtime-invalidator, lib/sentry, app/s/[id]/opengraph-image, e2e/smoke.spec + playwright.config, lib/api mediaUrl), .github/workflows/portal.yml (+e2e job), packages/tokens/glyphs, CLAUDE.md, docs/waves/README.md
- summary: 7E complete. Quick hardening (runtime Terminal Chart map grade, T-crane favicon, 07 §3 offline banner, phones start rail icon-only, P6 card list under md). Ably realtime behind lib/realtime (TokenRequest auth, user:/conv: channels, silent keyless fallback to the 15s polling that stays as safety net). Sentry client keyless-degraded (errors only; P12 boundary captures with the digest). Designed OG share card for /s/[id] (next/og duotone). **Playwright smoke 4/4 GREEN on a clean stack** (F1 register->fixed-OTP->activation->checklist · F9 full six-step publish incl. presigned photo upload · F4 accept w/ acknowledgment · refund-preview figures == API); portal.yml e2e job runs the same against a postgis service. **Real bug found+fixed by the smoke:** local (non-R2) presigned PUT/public media URLs are API-relative and 404'd from the portal origin — mediaUrl() routes them through the BFF (R2 absolute URLs untouched in prod).
- reason: Wave 7 slice 7E per plan; TSD §8 portal testing mandate.
- change_ref: 2026-07-04 07:45 - Wave 7 Slice 7D BUILT
- notes: **WAVE 7 BUILD COMPLETE (7-0 -> 7E).** Local-run gotcha recorded: seed_e2e re-runs can silently fail on availability_conflict once prior runs consumed the +7..+9 window — reset the DB (CI always fresh). Wave-end checklist: [x] P1-P12 built, [x] mandatory vitest+Playwright suites, [x] Sentry/Ably keyless-degraded wiring, [ ] founder direction check (wordmark, P1 photography, /styleguide), [ ] Cloudflare deploy + R2 CORS (founder), [ ] prod-demo exit criterion (J1 under an hour + J6 rhythm), [ ] founder sign-off. Founder-approval queue (additive backend): photo DELETE, hire_id on ConversationSerializer, hirer-card fields on supplier-shaped HireSerializer, yard-scoped stats, payout day-series.

## 2026-07-06 11:18 - Portal UX fixes — photo upload, yards nav, listing geocode
- tag: FIX | FEATURE
- area: portal/components/assets/form-steps-media.tsx, portal/lib/api.ts, portal/app/bff/media-put/route.ts, portal/lib/server/media-put.ts, portal/components/shell/nav-rail.tsx, portal/app/(app)/storefront/page.tsx
- summary: Fixed listing photo reorder/cover (PATCH + `photos` field per frozen API). R2 presigned PUTs now proxy through `/bff/media-put` so uploads work without bucket CORS. Restricted photo picker to JPEG/PNG/WebP with retry UX. Added **Yards** to the nav rail and links from the asset location step. Listing location step now supports LocationIQ text search (map updates on pick) alongside yard chips and pin drop.
- reason: Founder-reported portal gaps: broken photo upload, undiscoverable yards UI, map-only listing location.
- change_ref: 2026-07-04 14:15 - Wave 7 Slice 7E BUILT
- notes: R2 bucket CORS is no longer required for portal uploads (BFF proxy). Geocode still needs `LOCATIONIQ_KEY` in prod for search results.

## 2026-07-06 11:36 - Portal photo delete + map null-coord fix + CORS docs
- tag: FEATURE | FIX
- area: backend/listings (photos delete), portal PhotosStep, portal MapView/map-coords, DEPLOY.md
- summary: Added `DELETE /api/v1/listings/{id}/photos/{photo_id}` (cover promotion + reindex). Portal Photos step now has remove (X) on hover. MapLibre null-coordinate guard fixes console "Expected number, found null" errors. DEPLOY.md notes BFF upload proxy vs optional R2 CORS.
- reason: Founder can't remove photos when editing assets; console CORS errors on R2 PUT until PR deploys; map errors from null coords.
- change_ref: 2026-07-06 11:18 - Portal UX fixes
- notes: Photo delete requires backend deploy (Railway auto from main). Portal upload CORS fix requires Workers deploy of PR #39. 409 on listing save = archived listing (`listing_not_editable`).

## 2026-07-06 12:10 - Portal: fix photo display, yards from Assets only, nav footer + logout
- tag: FIX | FEATURE
- area: portal/lib/media-url.ts, portal/components/shell/nav-rail.tsx, portal/components/yards/yards-panel.tsx, portal/app/(app)/assets/page.tsx, portal/app/(app)/yards/page.tsx
- summary: Listing photos now load via `/bff/media/public?key=` (API was returning non-public R2 S3 endpoint URLs). Removed Yards from nav; yards management panel on Assets page (`Yards` + `New yard` buttons). Settings + Sign out at nav bottom; collapse moved to header; density as compact icon control. `/yards` redirects to `/assets?yards=1`.
- reason: Founder: uploaded images broken in Assets table; yards only from Assets; missing logout; nav footer UX.
- change_ref: 2026-07-06 11:48 - Harden media upload proxy

## 2026-07-06 13:05 - Docs reconciliation — Wave 7 merged, handoff snapshot refreshed
- tag: CHORE | DOCS
- area: Implementations.md (Current status), CLAUDE.md, docs/waves/README.md, docs/v2/07_TSD.md
- summary: Reconciled the handoff docs to what actually shipped. Current status block rewritten: Wave 7 BUILT & MERGED to `main` (PR #38 + fix PRs #39/#40/#42), pending direction check, Cloudflare Workers deploy, prod-demo exit criterion, and sign-off; founder-approval queue and deploy carry-overs restated (R2 CORS no longer required — BFF media proxy). CLAUDE.md snapshot and docs/waves/README.md Wave 7 status cell updated to match. TSD API table gains the additive `DELETE :id/photos/:photo_id` (already in the committed OpenAPI since commit 3dd0573 — schema.yml unchanged here, verified present).
- reason: The Current status block still said "Wave 7 awaiting approval to start" though the log (and code) showed 7-0 → 7E built and merged; a fresh instance resuming cold would mis-locate itself.
- change_ref: 2026-07-06 12:10 - Portal: fix photo display, yards from Assets only, nav footer + logout
- notes: Docs-only — no behavior change, no OpenAPI regen needed. Wave 7 wave-end items remain OPEN (direction check → CF deploy → prod-demo → sign-off); Wave 8 stays gated on explicit founder approval.

## 2026-07-06 13:20 - Cloudflare Workers Builds Git integration detected — portal-deploy carry-over largely closed
- tag: DEPLOY | DOCS
- area: Implementations.md (Current status + carry-overs), CLAUDE.md
- summary: PR #43's checks surfaced a **Cloudflare Workers Builds Git integration** now connected for `terminal-portal`: it auto-builds on push and deployed the branch green with preview URLs (e.g. `claude-session-start-1dp8vs-terminal-portal.nwabueze.workers.dev`). Docs updated: manual `cd portal && pnpm deploy` is no longer the deploy path; the Wave 0/7 portal-deploy carry-over is largely closed. Remaining: confirm the production Worker serves `main` end-to-end against natural-cat (`API_BASE_URL`) — the Wave 7 prod-demo exit criterion exercises exactly that. (workers.dev is blocked from this build environment's network policy, so the running Worker wasn't probed from here.)
- reason: New deploy fact discovered after PR #43 was opened (and merged before this correction landed); the just-reconciled docs said the CF deploy was still fully open.
- change_ref: 2026-07-06 13:05 - Docs reconciliation — Wave 7 merged, handoff snapshot refreshed
- notes: Wave 7 wave-end still open: founder direction check, prod-demo exit criterion on the deployed Worker, sign-off. `LOCATIONIQ_KEY` in prod still pending for geocode/location search.

## 2026-07-06 14:10 - Wave 8 FOUNDER GO + vision session (Hirer app)
- tag: DECISION
- area: wave gating; docs/waves/{wave-8-vision.md [new], README.md}
- summary: Founder gave the explicit **Wave 8 (Hirer app) go** ("let's proceed to wave 8") and chose a Wave 7-style **vision session first**. Session ran (20 questions, 5 batches); binding outcomes captured in `docs/waves/wave-8-vision.md` (V1–V18). Headlines: consumer-polish + map-as-brand benchmark; **light+dark from day one**; screenshot-proud + trust-first acid test; hero mono money scaled up; live-map-tease onboarding; **full Terminal Chart style JSON → `packages/tokens/map` in 8B, both themes (D-022 to ratify — closes the 7E carry-over)**; springy/chart-instrument pins with pin-tick haptic exception; T-crane app icon; **all four signature moments** (PAID stamp, Confirmed handshake, handover both-ticks, first-launch map reveal); haptics moments-only; **full designed receipt share artefact w/ QR (no fee lines, D-014)**; 3-sound mechanical kit (CC0); vault-panel LockedTerms; chart-motif empty states; immersive gallery; guest pull invisible-until-needed; **hard urgency** ladder; B2B-hybrid messages; verification at-the-gate only; **OTA-per-slice releases** off one dev-client build. Scope note: V1/V4/V7/V10 expand 8A/8B beyond the wave-8.md floor — founder accepted in-session.
- reason: Wave gating (design.md §7) — explicit founder approval + vision recorded before build start.
- change_ref: 2026-07-06 13:20 - Cloudflare Workers Builds Git integration detected
- notes: Build architecture agreed (plan file): CNG + expo-dev-client (no Expo Go — MapLibre RN + MMKV are native), Metro workspace config, NativeWind v4 on the tokens preset, build-time font embedding, single-flight-refresh JWT client, sync-MMKV query persister, Zustand offline handover queue (D-016), D-014 render-layer tree-walker leak gate, CI app.yml with an `expo export` bundle-compile gate. Slices land on `claude/session-start-1dp8vs` (PR #44 will be rescoped to the Wave 8 kickoff PR). Next: slice 8A commits 1–10.

## 2026-07-06 16:40 - Wave 8 Slice 8A BUILT — app foundation, auth, onboarding (S1–S3, F1, F12)
- tag: FEATURE
- area: app/ [new — @terminal/app], packages/tokens/build.ts (nativewindVars), .github/workflows/app.yml [new], pnpm-lock.yaml
- summary: Slice 8A in ten commits on `claude/session-start-1dp8vs`. **Scaffold:** Expo SDK 57 (RN 0.86/React 19.2 — TSD says "current"; 54 was superseded), expo-router, CNG + expo-dev-client (no Expo Go — MapLibre RN + MMKV are native), strict TS, eas.json, **all native modules for the whole wave installed up front** so one dev-client build carries 8B–8F via OTA (V18). **Tokens/theming:** build.ts now emits `nativewindVars` (light+dark CSS-var maps); ThemeRoot injects the active system theme via NativeWind `vars()` — dual theme from day one (V1). **Type set:** Archivo/Inter/IBM Plex Mono from bundled @expo-google-fonts under weight-explicit family names, splash held until loaded; Money primitive renders server `*_display` strings verbatim w/ `money-hero` size (V2). **Lib:** portal pure modules copied (types/naira/auth-schemas/map-coords); authored `legalHirerActions`/`hirerStatusCopy` mirroring hires/state.py (hirer actor) + table-driven fidelity suite; MMKV v4 storage; stores (session, onboarding+reveal flag, map-state w/ Lagos default, J8 persistence). **API client:** SecureStore JWT pair, Bearer, single-flight 401 refresh + one retry, dead refresh → tokens cleared + session-expired event; QueryProvider w/ NetInfo/AppState wiring + sync MMKV persister (hires/messages/me allowlist). **Screens:** S1 route decision + 3-page onboarding over a crafted chart-tease backdrop (V3 seam; skip-after-①, guest→Map browse-only), ink-900 tab shell w/ bespoke SVG glyphs; S2/S3 register→dual-channel OTP (6 mono cells w/ hidden paste/auto-fill capture, countdown, resend ladder, duplicate-phone→sign-in, abandoned-OTP resume)→auto-login; F12 SessionExpiredGate (re-auth sheet over the interrupted screen) + guest-intent consume-on-read resume; suspended S17 stub; Profile sign-out. **CI:** app.yml (lint, tsc, jest, expo-export bundle gate). D-014 walker (`test/d014.ts`) + freeze-time countdown hook tests scaffold the 8C/8D suites.
- reason: Wave 8 slice 8A per the approved build plan + vision (wave-8-vision.md V1–V18).
- change_ref: 2026-07-06 14:10 - Wave 8 FOUNDER GO + vision session (Hirer app)
- notes: All gates green — **31 jest tests**, eslint (1 benign react-compiler warning on react-hook-form), tsc, `expo export` bundle compiles. Founder next steps in `app/README.md`: build the dev client once (`expo run:android`), `eas init` + `eas update:configure` to arm OTA-per-slice. Gotchas recorded: jest transformIgnorePatterns must match the inner real-name node_modules segment (pnpm layout + regex backtracking); RTL v14 `render` is async; MMKV v4 = `createMMKV`/`remove`; expo install's version API is proxy-blocked — resolve pins from `expo/bundledNativeModules.json`. Next: **8B map & discovery** (Terminal Chart style JSON → packages/tokens/map both themes + D-022 entry, pins per 06 §3, yard sheet, S12/S13, first-launch reveal).

## 2026-07-06 18:50 - Wave 8 Slice 8B BUILT — map & discovery (S4/S5/S12/S13, D-022)
- tag: FEATURE | DECISION
- area: packages/tokens/map [new], docs/v2/DECISIONS.md (D-022), app/ (lib/{map-style,cluster,search-params,queries,asset-classes}, components/{map,search,ui/sheet,ui/empty-state,brand/class-glyphs}, app/{(tabs)/index,search,supplier/[id],listing/[id] stub})
- summary: Slice 8B in seven commits. **D-022**: Terminal Chart is the canonical map style for both surfaces, defined in `packages/tokens/map/grade.json` (token-sourced light+dark layer overrides on the Liberty base) + `snapshot-style.mjs` to bake committed full JSONs (network-gated — openfreemap is proxy-blocked from the build env; founder/CI follow-up). Closes the 7E carry-over; portal migrates post-8B. **Contracts**: frozen Wave 3 shapes typed; bbox/radius builders (spec bounds dropped without a class); useMapSearch/keyset-infinite useListSearch/useStorefront/useGeocode. **S4 Map home**: full-bleed Terminal Chart, pin system per 06 §3 (class-teardrop AssetPins w/ ported glyph kit, YardPins w/ matching_count badge + class dots + tick + 40% zero-match dim — never removed, drab clusters via supercluster over solos only), V5 feel (spring drops, crosshair select ring, haptic tick, reduced-motion safe), first-launch staggered reveal (V7④), search pill, class FilterBar + count chip, 400ms debounced viewport fetch, locate-me behind a contextual pre-prompt (denied → Lagos + settings banner), >200 → zoom-in chip, offline/tile-failure/empty states, J8 region+filter persistence. **S5 Yard Sheet**: bespoke Reanimated sheet primitive (half→full snaps, velocity dismiss — no new UI dependency), rendering entirely from the map payload (zero-round-trip asserted by a fetch-throws test), class-grouped rows, storefront footer; opens in-screen so drag-dismiss preserves map position. **S12**: q/radius/price/★spec/class filters, By asset/By location over the keyset cursor, clearable chips, +N-more-at-yard, empty state. **S13**: guest-accessible storefront (cover w/ M4 marks, plate header, stats strip, yards row, inventory grid, sticky Message behind guardIntent).
- reason: Wave 8 slice 8B per the approved plan + vision (V4/V5/V7/V12/V14).
- change_ref: 2026-07-06 16:40 - Wave 8 Slice 8A BUILT
- notes: Gates green per commit — **46 jest tests**, tsc, eslint (2 benign warnings), expo-export bundle. **Recorded gaps:** (1) `/search/list` has no `sort` param — S12 ships distance-only; additive `sort=price` joins the founder-approval queue; (2) committed terminal-chart-{light,dark}.json snapshots need a network-capable run of `snapshot-style.mjs`; (3) S4 map fetch currently filters by class only — sharing q/price filters between S12 and S4 is an 8F polish item; (4) jest gotchas recorded: NativeWind babel makes inline jest.mock factories unhoistable (use module-based mocks), reanimated 4 needs worklets `src/mock` + gesture-handler jestSetup. Next: **8C revenue path** (S6 listing detail, S7 request flow, S8 pay w/ stamp moment + receipt artefact).

## 2026-07-07 09:40 - Wave 8 Slice 8C BUILT — the revenue path (S6/S7/S8, F3)
- tag: FEATURE
- area: app/ (lib/{pricing,media,sounds,queries hires hooks}, components/{listing,hires}, app/{listing/[id],hire-request/[listingId],pay/[hireId]}, test/screens)
- summary: Slice 8C in five commits. **Pricing mirror** (`lib/pricing.ts`): fees.py best-price selection ported exactly (inclusive days, ceil units, min-total w/ longer-scheme tie-break) for hire_value+scheme ONLY — fee/payout math deliberately not ported (D-014); vector tests incl. 14-day→2×weekly and the tie rule. **S6 Listing Detail**: immersive full-bleed gallery (counter, M4 marks, thumbnail rail), template-driven SpecTable (`/spec-templates` join), d/w/m pricing grid on display strings, StaticMiniMap privacy radius on the maplibre seam, cancellation strip, sticky Enquire/Request behind guardIntent, skeleton + dead-link states. **S7 Request flow**: bespoke range calendar (no struck dates — see gaps), live duration line, review manifest w/ labelled client "Estimated total", note + operator/driver acknowledgment gate (client-side; accept-time server concept), POST w/ terms_accepted; submitted state renders the SERVER hire_value_display via PriceHero (server-display-only prop contract). F3 branches component-tested: 409 race sheet → reset to ①, basic-cap gate → verify/shorten. **S8 Pay**: hard-urgency CountdownPill (V15 bands + haptic crossings), hirer LockedTerms vault panel (V11), Paystack via expo-web-browser → poll the hire (webhook truth, never the redirect) → PAID stamp moment (V7①, impactMedium, sound hook silent) + QR receipt artefact (V9, view-shot → share); failure w/ attempts-left; expiry w/ re-request. **All S8 states × the D-014 walker** on a fixture that deliberately carries fee-bearing siblings (server strips them; test is belt-and-braces).
- reason: Wave 8 slice 8C per the approved plan + vision (V2/V7/V9/V11/V15).
- change_ref: 2026-07-06 18:50 - Wave 8 Slice 8B BUILT
- notes: Gates green per commit — **61 jest tests**, tsc, eslint, expo-export. **Posture decisions recorded:** (1) no quote endpoint exists → review shows a labelled client estimate; every hero renders server `hire_value_display` verbatim (sentinel-tested); additive `GET /listings/{id}/quote` joins the founder-approval queue; (2) no listing-availability-ranges endpoint → calendar ships without struck dates (409 sheet is the race net); additive availability-ranges endpoint joins the queue. **Follow-ups:** CC0 sound assets (V10) need a network-capable step — `lib/sounds.ts` wiring is in place, players silent; deep-link return from Paystack would need `PAYSTACK_CALLBACK_URL` to become app-scheme-aware (currently portal URL — polling posture works without it). New JS dep: react-native-qrcode-svg. Next: **8D hires & handover** (S9/S10/S11, offline queue, both-ticks moment).

## 2026-07-10 - Wave 8 Slice 8D BUILT — hires & handover (S9/S10/S11, D-016)
- tag: FEATURE
- area: backend/hires (services.list_handovers, HandoverSerializer.submitted_by_role, HireHandoverView GET), backend/openapi/schema.yml; app/ (lib/{queries hire hooks, media upload}, stores/handover-queue + drainer, components/hires/{status-badge, hire-card, event-timeline, handover-record-card, refund-manifest, handover-confirm-moment, reading-pad}, app/{(tabs)/hires, hires/[id], handover/[hireId]}, test/screens/{hires,hire-detail,handover})
- summary: Slice 8D in six vertical commits. **Backend (additive, founder-approved "add all missing endpoints"):** `GET /hires/{id}/handovers` (participant-scoped via `_get_for_actor`, bare array — a hire has ≤2 records) + additive `submitted_by_role` on HandoverSerializer so the client offers Confirm only to the counterparty (never the submitter) and never renders an illegal transition. Closes the S10 read gap and the Wave 7 portal's latent 405 on the same path. OpenAPI regenerated additive-only; all frozen Wave 2–5 operations byte-untouched (the one environmental drf-spectacular `204` drift on the frozen yards/photos DELETEs was reverted). **App data:** `useHires` (walks all cursor pages → HIRE_TABS partition, persisted so it renders cold, gentle 30s poll), `useHandovers`, `useSubmitHandover`, `useConfirmHandover`, `useRaiseDispute`; `uploadHandoverPhoto` (ImageManipulator resize ≤1920px → `/media/presign` → PUT the bytes, resolving dev-relative vs R2-absolute). **D-016 offline queue:** persisted Zustand store (`terminal.handover-queue`, photos as local URIs → resume-safe) + a shell-mounted `HandoverQueueDrainer` that uploads+POSTs on reconnect and refreshes the affected hires; handover capture is the ONE mutation allowed offline. **S9 My Hires:** Requested·Upcoming·On hire·History tabs, HireCards w/ one context chip (pay countdown / handover-today), J8 two-tap "Hire again" atop History, chart-motif empty states, pull-to-refresh. **S10 Hire Detail:** 9-state banner (09 §3 copy), LockedTerms vault (D-014's one money surface), EventTimeline, HandoverRecord cards (Confirm → both-ticks moment V7③), post-Confirmed conversation row, action bar gated by `legalHirerActions`; Cancel opens the §7.6 refund manifest (server figures verbatim) + reason, Raise-issue opens the dispute sheet. **S11 Handover Capture:** camera-first (expo-image-picker), class-recipe reading (hour meter / odometer / condition notes per FSD §7.4, folded into the record's `reading` JSON since the model has no notes column), ≥2-photo gate, submit→enqueue→drain, skip-path dispute-weakening copy (J3).
- reason: Wave 8 slice 8D per the approved plan + vision (V7③ both-ticks, V8 haptic, V10 sound, V11 vault, V12 empty states).
- change_ref: 2026-07-07 09:40 - Wave 8 Slice 8C BUILT
- notes: Gates green — backend **508 tests** (hires+payments cov 89.9% ≥85%, overall 91.2%), ruff/format/makemigrations/spectacular clean; app **80 jest tests** (incl. mandatory S10 banner/action matrix across all 9 states + D-014 render-layer leak gate on fee-bearing fixtures), tsc + eslint clean (only pre-existing warnings), `expo export` bundle compiles. **D-014 catch:** the leak gate flagged the word "payout" in an in_dispute banner + dispute-modal copy — both reworded ("Funds are on hold", "puts the hire on hold"). **Hardening:** the new native imports leaked a timer into the pay-test under full-suite scheduling → mocked expo-image-manipulator/expo-file-system-legacy/expo-image-picker in jest-setup and made a fragile pay-test assertion async (`getByText`→`findByText`). **Follow-ups (recorded):** Ably realtime for S9/S10 live updates is 8E (polling + pull-to-refresh ship now); handover photo THUMBS show a count, not images (private-bucket keys carry no presigned GET — needs a viewer-URL contract); CC0 sound assets still silent (wiring in place). Founder-approval queue additive endpoints (quote, availability-ranges, conversation hire_id, sort=price) remain for their own slices. **Next:** 8E messages (P9-equivalent) + realtime + wave-end device demo (J2/J3/J8) — Wave 8 surfaces freeze at sign-off.

## 2026-07-10 - Wave 8 Slice 8E BUILT — messages, profile, reports (S14/S15/S16, F2/F8/F10)
- tag: FEATURE
- area: backend/messaging (ConversationSerializer hire_id + unlocked, tests), backend/openapi/schema.yml; app/ (lib/{queries messaging+verification+report hooks, realtime.ts, rel-time.ts, types, api PORTAL_URL}, components/{shell/realtime-invalidator, messaging/{conversation-row,message-bubble,composer}, listing/report-sheet}, app/{(tabs)/{messages,profile,_layout}, messages/[id], verify}, test/screens/{messages,conversation,profile}, test/components/report-sheet)
- summary: Slice 8E in six vertical commits on a branch restarted from the merged main (8D = PR #45). **Backend (additive):** `hire_id` (deep-link a hire thread → S10) and `unlocked` (drives the "Contact details unlocked" line + composer hint) on ConversationSerializer — OpenAPI regenerated additive-only, frozen ops byte-untouched, the environmental drf-spectacular 204 drift reverted. **E1 data/realtime:** messaging hooks (useConversations walk-all + unread_total, useMessages sorted asc, useSendMessage, useMarkRead posting `{conversation_id}` — NOT the portal's stale `message_ids`, useCreateEnquiry), verification/delete/report hooks, relTime, and the keyless-degraded Ably layer (`lib/realtime` + a shell RealtimeInvalidator on `user:{id}`) with 15s polling as the always-on net; added the pure-JS `ably` dep (OTA-safe) + jest mock. **S14 Messages:** all/enquiry/hire filter, ConversationRows, aggregate unread badge on the tab, poll + pull-to-refresh, chart-motif empty. **S15 Conversation:** B2B-hybrid squared bubbles (mine/theirs), date separators, the first-occurrence masked explainer + per-message 🔒 pills, the unlock system line, optimistic composer with per-message retry, mark-read on open, Ably conv:{id} overlay, header context chip → listing (enquiry) / S10 (hire). **S16 Profile:** identity + verified chip, the F2 verification status card (verify / under-review / declined+resubmit / support-handoff at 3 attempts) + a Verify screen (multipart identity/business doc submit), the F8 "Become a supplier" portal hand-off (Linking to `PORTAL_URL` — the app never implements supplier tools), settings (password-reset email, support, legal, delete w/ 30-day NDPR copy), version, sign out. **F10 report sheet** from the S6 overflow (reason + detail + SLA → POST reports, thank-you with no reporter-side state, anti-gaming); S6 "Enquire" now creates+opens the enquiry conversation.
- reason: Wave 8 slice 8E per the approved plan + vision (V16 messages, V10 message-received sound, V8 no send haptic, V17 verification at the gate).
- change_ref: 2026-07-10 - Wave 8 Slice 8D BUILT
- notes: Gates green — app **88 jest tests** (incl. the mandatory S15: first-occurrence masked explainer, mine/theirs bubbles, optimistic send+retry, mark-read `{conversation_id}`), tsc + eslint clean (only pre-existing warnings), backend messaging suite green + additive OpenAPI. **Realtime posture:** wired but dormant in prod until `ABLY_API_KEY` is set (token endpoint returns `not_configured` → 15s polling; two-device realtime is met when the key lands). **Follow-ups (recorded):** true magic-link email for F8 needs a backend endpoint (portal hand-off ships now); CC0 message-received sound still silent (wiring in place); message list fetches only the latest page (load-older is polish); notification prefs omitted (no hirer backend). **Next:** 8F closes the wave — S17 system screens, offline/perf gate, EAS/OTA distribution, founder device demo (J2/J3/J8) + sign-off. Wave 8 surfaces freeze at sign-off.

## 2026-07-10 - Wave 8 8E follow-ups — message-received sound + F8 backend hand-off
- tag: FEATURE
- area: app/ (assets/sounds/message-received.wav, lib/sounds, app/messages/[id], lib/queries useBecomeSupplier, (tabs)/profile), backend/accounts (integrations/email send_supplier_invite_email, views.BecomeSupplierView, urls me/become-supplier, tests), backend/openapi/schema.yml
- summary: Closed two recorded 8E follow-ups. **Sound (V10):** bundled a short self-authored CC0 notification tone and play it in S15 when the newest inbound message id changes — fires under both polling and the Ably overlay, never on first hydration or own sends (V8: no send haptic). jest handles the `.wav` require via jest-expo's asset transformer. **F8 magic-link:** replaced the app's client-only portal `Linking` with a real backend hand-off — `POST /me/become-supplier` activates supplying (idempotent) + emails a portal link (FRONTEND_BASE_URL) via `send_supplier_invite_email`; the hirer signs in on the portal with their existing Terminal account. Profile row fires `useBecomeSupplier` → "Portal link sent to your email". Chosen over full passwordless SSO (which would touch the frozen Wave-7 portal + add a token-exchange endpoint) — recommended + confirmed with the founder.
- reason: Founder asked to address the magic-link + message-received-sound follow-ups.
- change_ref: 2026-07-10 - Wave 8 Slice 8E BUILT
- notes: Gates green — backend **510 tests** / 91.27% cov, additive OpenAPI (become-supplier path; frozen ops byte-untouched), makemigrations clean; app **88 jest** / tsc / eslint clean, `expo export` bundles. Lands on the same branch → PR #46. **Deploy note:** set `FRONTEND_BASE_URL` to the prod portal URL so the invite email links correctly (defaults to localhost:3000). Remaining 8E follow-ups: payment-success + handover-confirm CC0 sounds still silent (message-received now live); true passwordless SSO deferred. **Next:** 8F closes the wave.

## 2026-07-10 - Wave 8 Slice 8F BUILT — system posture & distribution (S17, offline, OTA)
- tag: FEATURE
- area: app/ (components/system/{error-screen,update-required,offline-banner,offline-no-cache}, components/shell/{update-gate,suspended-gate}, app/{_layout,+not-found}, (tabs)/{hires,messages}, lib/{updates(+tests),use-offline,api,sounds}, assets/sounds/{payment-success,handover-confirm}.wav, app.json, eas.json, fingerprint.config.js, test/{jest-setup, components/{error-screen,update-gate}, screens/{not-found,offline}}); app/README.md; docs/runbooks/app-release.md [new]
- summary: Slice 8F in five vertical commits on `claude/resume-mxemol` — the wave-closing slice. **S17 set complete:** root-layout `ErrorBoundary` → self-contained 500 screen (own SafeArea+ThemeRoot since expo-router mounts it above the providers; first `captureException` call site, mono "Ref: {id}" only when a DSN yields one), `+not-found` (chart motif, "Back to Map" via replace), and the **update-required OTA gate** — an unescapable overlay (not a route; Android back swallowed) that blocks when a published update's `extra.updates.criticalIndex` outruns the running app's, "Restart to update" busy until the download lands. Normal slices still apply silently on next cold launch (native ON_LOAD + fallbackToCacheTimeout 0). **Critical trap solved:** the fingerprint runtimeVersion policy hashes app-config `extra`, so a criticalIndex bump would have stranded critical updates — `fingerprint.config.js` skips ExpoConfigExtraSection/EASProject/Versions. app.json ships **no updates.url on purpose** (whole path inert until founder runs `eas init` + `eas update:configure`); requestHeaders bind locally-built release binaries to the production channel, eas.json adds preview/production channels. **Offline posture:** `useOffline` + inline "Offline — showing saved data" strip (09 §4) on S9/S14 when they render cold with data, S17 offline-no-cache state (motif + retry) when there's nothing persisted; map keeps its floating banner set. **Mid-session suspension:** any API `account_suspended` now emits (mirrors session-expired) → SuspendedGate replaces to `/system/suspended` (F12 complete). **V10 kit complete:** self-authored CC0 payment-success (stamp thunk) + handover-confirm (both-ticks) tones — play sites were already wired. **Docs:** `docs/runbooks/app-release.md` — one-time EAS setup, fingerprint sanity check, builds, OTA-per-slice, critical-update procedure, perf-gate measurement (cold start via `adb shell am start -W`, 5MB/4G upload), and the mandatory J2+J3+J8 manual E2E checklist verbatim.
- reason: Wave 8 slice 8F per the wave brief (wave-8.md §8F) — S17 · TSD §6 distribution · perf gate · release checklist.
- change_ref: 2026-07-10 - Wave 8 8E follow-ups — message-received sound + F8 backend hand-off
- notes: Gates green — app **107 jest tests** (was 88), tsc clean, eslint 0 errors (4 pre-existing warnings, one cleared), `expo export` bundles; app-only + docs, no backend/OpenAPI changes, frozen contracts untouched. **Founder-machine carry-overs (deploy-side, cannot run in-container):** (1) `eas init` + `eas update:configure` (adds updates.url — OTA + update gate inert until then); (2) fingerprint sanity check per the runbook; (3) **rebuild + reinstall the device binary once** — updates.url/channel/expo-updates plugin are native config; (4) record cold-start + upload perf numbers on device; (5) iOS EAS build submitted + OTA pipeline verified. **Wave-end:** S1–S17 all built; next step is the exit-criterion founder device demo (J2 stopwatch + J3 + J8 per the runbook checklist) → Wave 8 sign-off (surfaces freeze). MapLibre tile caching rides the native ambient cache (offline packs deliberately out of MVP scope).

## 2026-07-10 - Contrast fix pass — dark-mode app + light-mode portal, dual-theme token gate
- tag: FIX
- area: packages/tokens (src/{primitives,semantic}.json, build.ts), app/ (components/ui/text.tsx [root cause], lib/theme.ts [new], map/{pins,filter-bar,yard-sheet}, hires/{status-badge,countdown-pill,range-calendar,receipt-card,locked-terms,stamp,handover-confirm-moment}, system/{error-screen,update-required}, messaging/{composer,conversation-row}, listing/gallery, ui/text-field, brand/{lockup,chart-tease}, app/{(tabs)/{_layout,index},onboarding,search,system/suspended,supplier/[id],listing/[id]}, tests), portal/ (ui/status-badge, assets/messages/dashboard/hires-calendar pages, assets form steps, yards)
- summary: Founder device-demo screenshot showed unreadable dark-mode surfaces (white "N assets" pill w/ near-white text, amber yard-pin initials on white). THREE root causes fixed. (1) **Text primitives clobbered caller colours**: BodyText/MonoText/DisplayText/Money hardcoded `text-text-primary` before the caller className; NativeWind resolves colour conflicts by stylesheet order, so the near-white dark default silently beat `text-text-inverse`/`text-text-on-brand`/`text-amber-900` at ~30 call sites. Default now yields when the caller passes any colour (regex guard + tests). (2) **Amber as foreground on the theme-flipping surface/inverse**: new semantic token `text/brand-on-inverse` (light amber-500 on ink; dark amber-700 on paper, 5.0:1) applied to yard-pin/yard-sheet/storefront/search/listing initials; AssetPin glyphs now pick ink/paper by fill luminance (white on amber-500 was 2.1:1). (3) **Designed-dark "takeover" surfaces sat on surface/inverse**: receipt share artefact, LockedTerms vault, error/update-required/suspended screens, onboarding + chart-tease, handover-confirm scrim, tab shell, gallery letterbox all pinned to fixed ink-900 with fixed light foregrounds (the receipt is a shared image — it must not invert per theme). Token layer: dark `text/link`→blue-400, `text/danger`→red-400 (blue/red-500 were ~4.1:1 on ink-800 cards; new primitives), build.ts contrast gate now asserts **12 pairs × BOTH themes** (was 6 × light + 1 dark). Also themed: stamp REFUNDED/COMPLETED colours (ink-on-ink in dark), placeholders, map-home search/locate SVG strokes, status-badge on_hire→green-700+paper-0, declined/expired→text-tertiary, countdown amber band + range-calendar in-range days→amber-900 on amber-100. Portal (light-mode, visible today): white-on-green-600 badges (~3.8:1)→green-700, ink-400 timestamps/struck-price/weekend text→ink-500/600, placeholders→ink-500, sunken-surface overline headers→ink-600.
- reason: Founder flagged unreadable text throughout the interface on the Wave 8 device demo (dark-mode phone); wave-8-vision V1 promised light+dark from day one and Wave 8 surfaces are not yet frozen.
- change_ref: 2026-07-10 - Wave 8 Slice 8F BUILT
- notes: Gates green — tokens contrast gate 12×2, app **115 jest** (7 new text-primitive + 1 pin-contrast tests) / tsc / eslint 0 errors / expo export bundles, portal 48 vitest / tsc / eslint. pnpm-lock.yaml re-synced (last main commit removed 3 app deps without the lock, breaking frozen-lockfile installs). **Founder note:** the installed preview APK predates this fix — pins/tab-shell colours are native-agnostic JS, so an OTA update (or new build) picks all of this up. **Deferred (recorded, not blocking):** portal has LATENT dark-mode breakage (nav rail ink-300-on-inverse, wordmark amber-on-inverse, assets ink-800 select) — harmless today because nothing ever sets `data-theme="dark"`; wire-up should fix those sites first. Decorative empty-state compass keeps faint fixed strokes (aria-hidden). PAID stamp stays brand amber per 06 §8 (design moment, not text).

## 2026-07-10 - Photos everywhere, tappable pins, D-023 plate markers + restrained motion
- tag: FIX | FEATURE | DECISION
- area: app/ (lib/{media,map-press [new]}, components/map/{pins,terminal-map,peek-card,filter-bar,yard-sheet}, components/hires/{stamp,countdown-pill,handover-confirm-moment}, components/search/listing-row, app/{(tabs)/index,search,supplier/[id]}, tests); docs/v2/DECISIONS.md (D-023)
- summary: Three founder-reported device issues closed. **(1) Photos displayed nowhere:** prod photo URLs are R2 S3-endpoint URLs (`*.r2.cloudflarestorage.com`) which are not public-readable — the portal hit the same wall (PR #42, BFF proxy). `resolveMediaUrl` now extracts the public object key (listings/logos/avatars/handovers prefixes) and rides the backend's `GET /api/v1/media/public?key=` proxy (reads R2 server-side; AllowAny; dev-relative paths unchanged), and every remaining raw `<Image uri>` site (map pins, yard sheet, peek card, search rows + logos, storefront cover/logo/grid, listing rows) now routes through it. **(2) Pins never tappable on device:** native `Marker onPress` is unreliable on physical Android (maplibre-react-native #557/#1018 lineage). Added `lib/map-press.ts` — pure Web-Mercator JS hit-testing — and the map's own `onPress` (which always fires) now resolves the nearest yard/solo/cluster within a 30px logical radius and selects/expands it; the native marker path stays wired as primary (it consumes the tap when it works). **(3) D-023 recorded:** teardrops/white squircles → squared fixed-ink **plate markers** (mono amber initials · rule · paper count; 3px class edge strip on solo plates; 5px class index squares; blue verification square; selected = 2px amber frame — crosshair ring retired), and the app-wide **motion restraint**: spring pin drop, staggered first-launch reveal, stamp scale/settle theatrics, handover-tick pop, and countdown pulse all removed (haptics + sounds kept; fades/instant settles; urgency by colour). Peek card slides without spring. Markers now anchor center (plates), which also simplifies the hit-test.
- reason: Founder device demo: "pictures of listings are not displaying… can't click on the pins… remove the bounce… the application should carry a serious vibe." Clarified via structured Q&A: photos broken everywhere, taps dead everywhere, plate direction + app-wide motion audit chosen.
- change_ref: 2026-07-10 - Contrast fix pass — dark-mode app + light-mode portal, dual-theme token gate
- notes: Gates green — **128 jest tests** (new: 5 media-url, 7 map-press hit-test, plate-anatomy pin suite incl. amber-frame selection), tsc, eslint 0 errors (4 pre-existing warnings), expo export bundles. App-only + docs; no backend/OpenAPI change (media proxy endpoint already existed for dev — it is schema-excluded and AllowAny). **Follow-ups recorded:** media through the API adds Railway egress per image — enabling the R2 public dev domain (or custom domain) later short-circuits automatically once `publicMediaKeyFromUrl` stops matching, but callers would need the base URL switched too; consider caching headers on `MediaServeView` (it streams uncached today). The `mapRevealPlayed` onboarding-store flag is now unused (left persisted, harmless). JS hit-test uses the last settled zoom (regionDidChange) — mid-gesture taps resolve against the previous zoom, acceptable at 30px tolerance. OTA update (or fresh build) required on the founder device to pick this up.

## 2026-07-11 - Founder device bugs: session hydration, login dismissal, keyboard, verification errors + OTA path corrected
- tag: FIX | DOCS
- area: app/ (stores/session.ts, components/shell/session-hydrator.tsx [new], app/_layout.tsx, lib/{queries,keyboard [new],media}, components/ui/keyboard-spacer.tsx [new], components/shell/query-provider.tsx, app/{(tabs)/profile, auth/{login,register}, hire-request/[listingId], messages/[id], verify}, tests); app/README.md; docs/runbooks/app-release.md
- summary: Five founder-reported device issues fixed, all JS-only (OTA-shippable to the installed preview APK). **Central defect:** the session store's `me` was in-memory only and never rehydrated at cold start — valid SecureStore tokens still authenticated the API layer while the UI rendered a guest. That single defect caused the "login again to hire/enquire" bounce AND much of "actions need an app restart" (RealtimeInvalidator keys its user:{id} subscription on `me`, so realtime invalidation never armed; useBecomeSupplier wrote a ["me"] query cache nothing read). Fixed: `me` persists via zustand-persist/MMKV (signed-in on first frame, offline included), a shell SessionHydrator reconciles against /me at boot via the previously-unused hasSession()/fetchMe(), useBecomeSupplier writes the store, "me" left the query persist allowlist, Profile waits for hydration before the guest CTA. **Login screen stuck after signing in from Profile:** Profile now pushes /auth/login?reauth=1 so login's existing back-dismissal pops it (replace-to-tabs left the pushed screen on top). **Keyboard covered the composer:** SDK 57 edge-to-edge Android ignores adjustResize and KAV's android branch was behavior=undefined (no-op) — new useKeyboardHeight()+KeyboardSpacer pad bottom-pinned content; applied to the conversation composer and login/register/hire-request/verify scroll ends. **Verification "Upload does nothing":** every failure was swallowed (onSuccess-only, no error UI) — server message now renders (ApiError.message + rc_number field errors), and picked docs downscale to ≤1600px JPEG via the already-native expo-image-manipulator to stay under the server's 5 MB/doc cap (the likely silent trigger; NOT the throttle — auth scope is 120/min and login has its own 5/min bucket, recorded so it isn't re-chased). **OTA verified ARMED** in the installed APK (updates.url+projectId committed at b7b405e/1dc5ddd; channel `preview`): README/runbook corrected — they claimed OTA inert and shipped to `--branch production`, which a preview-channel binary never polls; ship = `eas update --branch preview`, applies across TWO cold launches; static runtimeVersion "0.1.1" gotcha documented (manual bump + rebuild when natives change; fingerprint.config.js is dead config; local release builds receive no OTA — no requestHeaders).
- reason: Founder device feedback: stale-until-restart data, re-login prompts on hire/enquire, keyboard hiding the composer, login screen not dismissing, dead upload button, and "verify automatic update is enabled in the apk".
- change_ref: 2026-07-10 - Photos everywhere, tappable pins, D-023 plate markers + restrained motion
- notes: Gates green — **145 jest tests** (17 new: 3 session-store, 3 hydrator, 3 login-nav, 2 keyboard, 3 verify, guard no-bounce/guest, become-supplier store reflection), tsc, eslint 0 errors (4 pre-existing warnings), expo export bundles. Stacked on PR #49 (open). **Behavior change to note:** cold start with a DEAD refresh token now surfaces the F12 re-auth sheet at launch (existing gate; correct semantics). **Upgrade path:** first launch after this OTA has tokens but no persisted `me` — guards read guest for the sub-second until fetchMe resolves, self-heals permanently. Follow-ups: pollers still pause in background (refetchIntervalInBackground unset — acceptable, realtime now arms); consider Cache-Control on MediaServeView.

## 2026-07-11 - Founder round 2: handover evidence, command-center dashboard, receipt document, glance pins, direct chat, storefront polish
- tag: FEATURE | FIX
- area: portal/ (hires/[id] page + handover-evidence/handover-submit/respond-dialog/request-queue [new], earnings page [new], dashboard, messages, nav-rail, calendar, lib/{types,queries,media-url,image-resize [new]}); app/ (components/hires/receipt-card [rewritten], components/hires/stamp [deleted], pay/[hireId], supplier/[id] [reworked], hires/[id], components/map/{pins,peek-card}, (tabs)/index, stores/map-state, lib/naira); docs/TODO-backend-endpoints.md [new]
- summary: Six founder asks on branch `claude/mobile-supplier-portal-improvements-5ptkqd`, one commit per ask (frontend-only; zero backend/OpenAPI change). **(1) Portal handover evidence:** the hire-detail Handovers card was a photo COUNT beside HoldToConfirm — it now renders photo thumbnails via the BFF public-media proxy (`handovers/` added to the prefix list) with a lightbox, the capture readings (hour meter/odometer/notes), submitted-by role (type gains the 8D `submitted_by_role`), and confirm is offered only on hirer-submitted records; a new Submit-handover dialog (≥2 photos resize→presign→PUT + class-recipe reading) closes the portal's missing submit side. **(2) Command-center dashboard (founder chose "both, full rebuild"):** Needs-your-response queue (per-row countdown + inline accept/decline via shared RespondDialog) and Handovers-today above the stats; Queued-payout stat replaces Action-needed; new `/earnings` page + nav item renders the full payout ledger the API already served; Messages honours `?c=`/`?hire=` deep links (Conversation gains 8E `hire_id`); hire-detail Conversation card links the hire chat; calendar utilisation fixed to unit-days over real `unit_count` (was hardcoded 1). **(3) Receipt as formal document (founder pick):** rotated amber PAID stamp retired (component deleted); receipt = ink-plate document (OFFICIAL RECEIPT overline, ruled ASSET/PERIOD/REFERENCE/ISSUED rows, TOTAL PAID mono hero, squared PAID marker, QR footer); haptic+payment-success sound now fire from the pay screen on webhook flip; D-014 leak gate still green. **(4) Glance pins (founder picked all options):** solo plates carry compact from-price (`compactNaira`: kobo→₦250k/₦1.8m) + dim 45% when on hire; yard plates swap class-colour squares for ≤3 real class glyphs in-plate + amber from-price row; richer yard peek card (logo/initials, available-now count, class mix by name). **(5) Direct chat:** storefront CTA get-or-creates the supplier enquiry and opens the thread; hire-detail resolves the hire conversation by `hire_id` (row now shows from accepted). **(6) Storefront polish:** identity plate overlapping a taller cover (Verified chip, stats in a sunken row), spaced About/Yards/Inventory cards; yard cards fly the S4 camera to the yard via a new one-shot `requestFocus` on the map store (partialize keeps it unpersisted).
- reason: Founder feedback round (2026-07-11): portal not showing handover submissions, childish receipt, message-supplier dropping into the tab, uninformative pins, impractical dashboard/pages, packed company profile. Direction confirmed via structured Q&A (formal receipt · all pin options · both-dashboard rebuild · frontend-only with a backend TODO file).
- change_ref: 2026-07-11 - Founder device bugs: session hydration, login dismissal, keyboard, verification errors + OTA path corrected
- notes: Gates green — portal **51 vitest** (3 new handover-evidence) + tsc + eslint + `next build`; app **149 jest** (12-test pin suite incl. compactNaira, dim, price row) + tsc + eslint 0 errors (4 pre-existing warnings) + expo export bundles. **`docs/TODO-backend-endpoints.md` [new]** records the additive endpoints the founder wants to review later (yard-scoped stats, payout day-series, quote, availability-ranges, sort=price, photos_display, server-side portal filtering). App changes are JS-only → OTA-shippable (`eas update --branch preview`, two cold launches). Wave 7/8 surfaces still unfrozen (neither signed off) so these UI changes are in-bounds.

## 2026-07-11 - OTA-on-merge automation (GitHub Actions → EAS Update)
- tag: FEATURE | DEPLOY
- area: .github/workflows/ota.yml [new], docs/runbooks/app-release.md (§3)
- summary: Founder asked whether the round-2 app changes ship as an OTA release and whether that can be automated on merge. Added `ota.yml`: every push to `main` touching `app/**` or `packages/tokens/**` installs, builds tokens, and runs `eas update --branch preview --non-interactive` via expo/expo-github-action — the installed preview-channel APK downloads the update in the background and applies it across two cold launches. Production-channel publishes stay deliberate/manual. Runbook §3 now documents the automated path as the default.
- reason: Close the manual `eas update` step so merged app slices reach the founder's device without an operator.
- change_ref: 2026-07-11 - Founder round 2 (same branch/PR #50)
- notes: **Deploy-only carry-over: create an expo.dev access token and add it as the `EXPO_TOKEN` GitHub repository secret** — until then the ota job fails loudly (by design, no silent no-op). Guard rails documented in the workflow header: OTA is JS/assets only; native-dep or plugin changes need a manual `runtimeVersion` bump + rebuild, and stale-runtime binaries ignore later updates. Local release builds carry no channel header and never poll — use EAS builds.

## 2026-07-12 - Availability calendar, supplier date-blocks, handover privacy+retention, payment return, map carousel
- tag: FEATURE | FIX | DECISION
- area: backend/hires (models/availability/services/serializers/views/urls/tasks + migrations 0002/0003 + 2 management commands), backend/search (serializers/services/views), backend/core/media.py + views/urls, backend/payments (paystack/views/urls/templates + settings + .env.example); app/ (pay, hire-request, range-calendar, handover-record-card + handover-photo-strip [new], media.ts, queries/types, map/pin-carousel [new] + pins + terminal-map + (tabs)/index, search.tsx, search-params, stores/map-state; peek-card deleted); portal/ (calendar page + availability-block-dialog [new], handover-evidence, lib/queries/types/media-url); docs (DECISIONS D-024/025/026, FSD §7.1/§7.4, TSD §3.4/§3.9/§5)
- summary: Founder-directed batch (branch `feat/availability-handover-payment`), built as 6 backend + 5 frontend slices, one commit each. **B1/D-024 — supplier date-blocks:** new `AvailabilityBlock` model (hires), a hard hold on the whole listing folded into `is_available`/`free_units`/`can_confirm` + bulk `availability_map`; CRUD `GET/POST /listings/{id}/availability-blocks` (supplier-only, non-owner 404) + `DELETE /availability-blocks/{id}`. **B2 — public availability calendar:** `GET /listings/{id}/availability?from=&to=` returns per-day free-unit counts (counts only, no counterparty data) via a 2-query diff-array sweep `free_units_by_day` (≤3 queries, 90-day cap, past clamped, non-Live 404). **B3 — search date filters:** optional `date_from`/`date_to` on `/search/map`+`/search/list`; `availability_map` gains full-range semantics (= `is_available`, so an "available" pin never 409s), N+1 budget intact. **B4/D-025 — handover photos private:** `handover_photo` MediaKind → private bucket; `HandoverSerializer` gains additive `photo_urls` (15-min presigned GETs minted where participants-only access is enforced); private-read helpers promoted into `core/media.py` (+ local `media/private` serve view); one-shot `migrate_handover_photos` command (idempotent, verify-then-delete, --dry-run). **B5/D-026 — retention:** `photos_purged_at` marker + idempotent daily `purge_handover_photos` sweep (Completed & off-hire-confirmed ≥90d; In-Dispute frozen) + `run_handover_photo_purge` command; record rows kept forever. **B6 — payment return:** `GET /api/v1/payments/return?hire_id=` renders a static page that deep-links `terminal://pay/{id}` (zero DB reads, never an oracle); `create_checkout` composes the per-transaction callback from new `PAYMENT_RETURN_BASE_URL` (empty ⇒ legacy portal fallback), `APP_RETURN_SCHEME` default `terminal`. **F1** — pay screen switches to `WebBrowser.openAuthSessionAsync(url, terminal://pay/{id})` (webhook still the only truth). **F2** — `RangeCalendar` strikes booked days + shows multi-unit scarcity from the availability endpoint; degrades permissive on error; retires the recorded no-endpoint gap. **F3** — `HandoverPhotoStrip` renders thumbnails + lightbox from `photo_urls` (app), retention note when purged; `resolveMediaUrl` drops `handovers/` from the public-proxy prefix so presigned URLs pass through. **F4** — portal Gantt gains striped date-blocks with create/remove dialogs; HandoverEvidence renders `photo_urls` + purged note. **F5** — snap-to-pin bottom carousel replaces the peek card (swipe↔pin sync, yard card → S5 YardSheet, compact pins), + a shared hire-window Dates filter across map + search.
- reason: Founder review (2026-07-12): hirers had no visibility of off-hire dates (calendar gap was recorded but unbuilt); handover photos shipped public against FSD §7.4 and never rendered on mobile; no handover retention policy existed; Paystack stranded the payer on the portal in an external browser; and the founder approved the Google-Maps-style declutter carousel.
- change_ref: 2026-07-11 - Founder round 2: handover evidence, command-center dashboard, receipt document, glance pins, direct chat, storefront polish
- notes: Gates green — backend **pytest 560+ passing** (new: availability-blocks, listing-availability incl. hypothesis day-math vs brute force + query-count, search date-filters, handover-privacy, handover-purge freezegun, payment-return); ruff clean; OpenAPI regenerated per contract-changing slice (all additive — frozen Wave 2/3/4 contracts byte-untouched). App **168 jest** + tsc + eslint (0 errors, pre-existing warnings only). Portal **55 vitest** + tsc + `next lint` clean. Resolves the round-2 `docs/TODO-backend-endpoints.md` asks for availability-ranges + photos_display. **DEPLOY-ONLY CARRY-OVERS (new):** (1) run `./manage.py migrate_handover_photos` once post-deploy to move existing handover objects public→private; (2) add a daily Railway cron on the worker for `./manage.py run_handover_photo_purge` (alongside the existing reconciliation/digest crons); (3) set `PAYMENT_RETURN_BASE_URL=https://api-production-101c8.up.railway.app` in Railway to activate the app deep-link return (empty keeps today's portal redirect — no regression); `APP_RETURN_SCHEME` defaults to `terminal`. App changes are JS-only → OTA-shippable (`eas update --branch preview`). Paystack `callback_url` is assumed https (verify against current Paystack docs before relying on the return page — the https-page-forwards-to-scheme design is correct either way). Follow-up: the Current-status block above lags (predates Wave 8) — reconcile at the next handoff.

## 2026-07-12 - OTA diagnosis + runtimeVersion decoupling (why OTA never reached a device)
- tag: FIX | DECISION
- area: app/app.json (runtimeVersion), docs/runbooks/app-release.md §1
- summary: Founder reported OTA "doesn't function / has never functioned." Root cause is layered. **(1) Automated publish never fired:** `.github/workflows/ota.yml` needs the `EXPO_TOKEN` GitHub repo secret (logged as an open carry-over 2026-07-11, never closed) — every merge to `main` touching `app/**` has failed the `ota` job, so no update was ever published automatically. **(2) The real design flaw — `runtimeVersion` tracked the marketing `version`:** history `0.1.1` → `0.1.3`; `expo-updates` only delivers an update to a binary whose `runtimeVersion` matches *exactly*, so every version bump minted a new runtimeVersion island and silently orphaned the installed APK from all future OTA. An update published now (`0.1.3`) could never reach the installed `0.1.1` build even with the secret set. **(3)** Local `--variant release` builds carry no channel header and never poll regardless. **Fix:** `runtimeVersion` pinned to a stable native-ABI generation string **`"1"`**, decoupled from `version` (still `0.1.3`) — bump `version`/`versionCode` freely per release, touch `runtimeVersion` only on native/plugin/SDK changes. Runbook §1 rewritten with the rule.
- reason: OTA is armed in config but no update had ever actually landed on a device; the version-coupled runtimeVersion guaranteed it never would across releases.
- change_ref: 2026-07-11 - OTA-on-merge automation (GitHub Actions → EAS Update)
- notes: **Deploy-only carry-overs to make OTA live:** (a) valid Expo access token on the active account (`@perble` as of 2026-07-13); (b) add it as the **`EXPO_TOKEN`** GitHub repo secret (unblocks `ota.yml`); (c) build the new baseline at runtimeVersion `"1"` via the EAS **preview** profile and **install it once** — every pre-`"1"` binary is permanently OTA-deaf; (d) thereafter JS-only merges to `main` publish OTAs that land on the `"1"` binary across two cold launches. The superseded `@perblis-organization` baseline build (`2d17c1ed`, runtime `"1"` but wrong account/project) is obsolete after the `@perble` migration.

## 2026-07-12 - Map carousel snap-back fix + keyboard-proof inputs (native RNKC)
- tag: FIX | FEATURE
- area: app/ (components/map/{pin-carousel,terminal-map}, lib/map-settle [new] + test, (tabs)/index, app.json, _layout, messages/[id], auth/{login,register}, hire-request, verify, handover/[hireId], hires/[id], components/listing/report-sheet, test/jest-setup; DELETED lib/keyboard.ts + components/ui/keyboard-spacer.tsx + its test); docs/runbooks/app-release.md
- summary: Two founder-reported device bugs, on branch `claude/mobile-carousel-input-fixes-32vni7` as two slices. **(1) Carousel scrolled back on its own:** a swipe selected the pin AND flew the camera → settle → 400ms debounce → refetch → backend re-sorts by distance from the new centre → items reorder → the `[activeIndex]` snap effect fired after `onMomentumScrollEnd` had already cleared the `syncSource` guard → visible self-scroll. Fixed twice over: carousel pans are now **silent** (`flyTo(..., {silent:true})` + `lib/map-settle.makeSettleGate` mutes exactly one *animated* settle — user gestures settle `animated:false` and always refetch; 1.5s fallback timer self-heals a dropped settle), and snapping is **keyed on the selected item, not its index** (`makeSnapPlanner`: pin tap/external → animated snap on the exact snap grid via `scrollToOffset` [the old `viewPosition:0.5` rested off-grid]; same-key index shift (reorder) → instant realign; own-swipe echo → hold). The sticky `syncSource` mode is gone — suppression is one-shot (`lastReportedKey`) + time-bounded (`snapInFlight` w/ timeout fallback, since non-animated scrolls fire no momentum-end), which also un-wedges pin-tap snapping after a late viewability event. Polish: haptic tick on swipe-activation (parity with pin taps), FlatList window tuning. **(2) Keyboard still covered the composer (post-2026-07-11 fix):** root-caused — the Android spacer **double-subtracted the nav-bar inset** (RN 0.86 reports `ime − systemBars`, then the spacer subtracted `insets.bottom` again → short ~24–48dp on every device), and `keyboardDidShow` never fires for TextInputs inside RN `Modal`s (report/cancel/dispute sheets had NO handling at all; verify had no iOS path; handover notes had nothing). Founder chose the native fix: **react-native-keyboard-controller 1.21.9** (SDK-57-pinned), `KeyboardProvider` at root, RNKC `KeyboardAvoidingView behavior="padding"` on both platforms across composer/login/register/hire-request/verify/handover + in-sheet KAV for the three modal sheets; hand-rolled spacer path deleted; jest uses the library mock.
- reason: Founder device report (2026-07-12, screenshot): carousel scrolls back on its own after swiping; chat/message/typing fields hidden behind the keyboard.
- change_ref: 2026-07-12 - Availability calendar, supplier date-blocks, handover privacy+retention, payment return, map carousel
- notes: Gates green — app **179 jest** (new: settle-gate suite, snap-planner suite, snap/realign/hold component specs w/ scrollToOffset spy), tsc, eslint 0 errors (4 pre-existing warnings), expo export bundles. **DEPLOY CARRY-OVER — NEW NATIVE MODULE:** `react-native-keyboard-controller` ships in the `"1"` baseline; the interim `0.1.2` runtimeVersion bump on main was a stepping stone only. On-device verify list: swipe carousel fast (map chases, no self-scroll >1s after), pin tap snaps flush, swipe-then-tap-different-pin snaps, hand-pan refetches with selected card stationary; composer/login/register/hire-request/verify/handover-notes/report/cancel/dispute fields fully visible above the IME (test gesture + 3-button nav). Known caveat to verify on device: RNKC inside RN `Modal` sheets on Android (if the sheet doesn't lift, fall back to an in-modal spacer fed by `KeyboardController` events). Deferred (low risk): search-screen filter fields are top-anchored and got no KAV.

## 2026-07-13 - Search query now applies on the map (shared q)
- tag: FIX
- area: app/ (stores/map-state.ts, app/search.tsx, app/(tabs)/index.tsx, test/stores/map-state.test.ts [new])
- summary: Founder report: typing a search auto-opens S12 and filters the list, but returning to the S4 map dropped the query — `q` was local `useState` on the search screen while the map's `useMapSearch` only received the shared class/dates filters. `q` now lives in the map-state store (shared like classFilter/dateRange; deliberately NOT persisted — per-launch intent, same rationale as dateRange), the map query sends it (`/search/map` already supported `q` server-side since Wave 3), and the map's search pill renders the active query with a clear ✕ instead of the placeholder, so the filter is visible and dismissible from the map.
- reason: Founder device report (2026-07-13): "search applies to the list mode but when I go back to the map view search doesn't apply over there".
- change_ref: 2026-07-12 - Map carousel snap-back fix + keyboard-proof inputs (native RNKC)
- notes: Gates green — **181 jest** (new map-state store suite: q shared, q/dateRange never persisted), tsc, eslint clean, expo export bundles. JS-only → OTA-shippable on the runtimeVersion "1" baseline once that APK is installed (see 2026-07-12 runbook change: static runtimeVersion "1", new @perble/terminal Expo project). Deliberately NOT shared with the map: price/spec/radius (list-refinement panel; the map has no affordance for them) — record as a follow-up if the founder wants full filter parity.

## 2026-07-13 - Map surface refinement: price-first plates, tick/count demotion, yard-sheet cleanup
- tag: FIX | FEATURE
- area: app/ (components/map/{pins,terminal-map,filter-bar,yard-sheet} + tests)
- summary: Founder design review of three device screenshots (map pills, filter bar, yard sheet), aligned to map-marketplace convention (Airbnb/Zillow price-first pins) inside the D-023 plate language. **Pins:** compact yard plates are now **price | count** (paper `₦250k` + ink-300 count) instead of `initials | count` — two-letter amber initials read as asset-class codes and collide across suppliers (two "WL 3" pins on one screen); initials remain on the detailed/selected plate and as the priceless fallback. Compact solo plates keep their from-price (were glyph-only). The blue verification mark is now a **circular seal, detailed-plate-only** — the floating rounded square on every compact pin read as a field of checkboxes and outshouted the amber selection frame; compact pins keep the semantic in the a11y label. **Map:** the selected pin (yard or solo) always renders last — its detailed plate was being occluded by neighbouring markers (render order = stacking order), which hid exactly the price row selection exists to reveal. **FilterBar:** the result count moved out of the chip row onto its own right-aligned row below — the trailing inverse pill chopped scrolling chip labels mid-word ("Trucks…"), was the brightest element on screen, and looked tappable; it's now a drab mono telemetry plate (ClusterPin philosophy, 06 §3). **YardSheet:** rows adopt the S12 ListingRow anatomy (title / "Class · availability" caption / price under the title) — the right-aligned `Money` column truncated titles while claiming half the row; ALL-CAPS class section headers removed (chips + row captions already carry class, three taxonomy layers for three rows); chip row hidden for single-class yards; thumbnails get a sunken field + onError fallback so failed loads no longer render as black holes.
- reason: Founder review 2026-07-13 ("I don't like the way all the parts I have shown you look — improve it to what is standard"), grounded against Airbnb/Zillow/Booking map-surface conventions.
- change_ref: 2026-07-13 - Search query now applies on the map (shared q)
- notes: Gates green — **184 jest** (price-first plate matrix, seal detailed-only, priceless fallback, single-class chip suppression, per-row class captions), tsc, eslint 0 errors (4 pre-existing warnings). Presentation-only, JS-only → OTA-shippable on the runtimeVersion "1" baseline. Deliberate scope cuts (candidate follow-ups): no anchor caret on plates (center-anchored like Airbnb; adding one would shift `map-press` hit-testing), no collision-aware declutter for overlapping yard plates at low zoom, control-layer radius language (rounded pills vs squared D-023 plates) left as a founder taste call.

## 2026-08-07 15:30 - VPS cutover Phases 3+5: secrets, stack live, lab TLS
- tag: DEPLOY | CHORE
- area: /opt/terminal/.env, Infisical perblis-terminal prod, infra/vps/{portal.Dockerfile,deploy.sh,docker-compose.prod.yml}, nginx vps-publish, mobile/lib/api.ts
- summary: Populated Infisical prod (`/backend` + root compose keys) from founder Railway secrets; wrote `/opt/terminal/.env` with VPS-specific hostnames (`terminal-api.lab.perblis.com`, `terminal.lab.perblis.com`), fresh PostGIS credentials, and all integration keys (Paystack test, Termii, Resend, R2, Ably, Sentry, FIELD_ENCRYPTION_KEY, SECRET_KEY). Fixed portal Docker build (workspace manifests in build stage, `PATH` for hoisted `tsx`, explicit tokens build + `next build`). `deploy.sh` green — api/worker/portal/db on loopback 8100/8101; `/healthz` + `/readyz` + portal login redirect OK. Published `terminal-api.lab.perblis.com` and `terminal.lab.perblis.com` via `vps-publish --cert`; HTTPS verified. Cron + backup dir installed. Mobile API default flipped to lab host.
- reason: D-027 VPS self-hosting cutover (founder-approved plan); founder supplied Railway prod secrets.
- notes: **Phase 4 blocked** — Railway `DATABASE_URL` in the export was an unresolved `${{PostGIS.*}}` template; vps-governance backup env only has local `127.0.0.1`. Need the **public/proxy** Postgres URL from Railway dashboard for `pg_dump` before data cutover. **Phase 5 remaining:** Paystack webhook → `https://terminal-api.lab.perblis.com/api/v1/payments/webhook`; `eas update` for mobile OTA. **Phase 6:** smoke/e2e against lab URLs, then Railway + Workers teardown. Fresh VPS DB has migrations + seed only (no Railway data yet).

## 2026-08-07 17:05 - Railway DB migration attempt: proxy up, Postgres offline
- tag: DEPLOY | CHORE
- area: Phase 4 data migration (Railway → VPS)
- summary: Founder supplied Railway public proxy URL (`reseau.proxy.rlwy.net:11614`). TCP connect succeeds but `psql`/`pg_dump` fail immediately — "server closed the connection unexpectedly" (all `sslmode` variants). Both legacy API hosts (`api-production-101c8`, `perblisterminal-production`) now return **404** on `/healthz`. Railway stack appears stopped or deprovisioned.
- reason: Attempt Phase 4 pg_dump before cutover.
- change_ref: 2026-08-07 15:30 - VPS cutover Phases 3+5
- notes: VPS stack unaffected — `terminal-db-1` has migrations + `seed_spec_templates` only. If prod data is needed: check Railway dashboard (resume service / new proxy URL) or `/var/backups/terminal` / any manual dumps. If Railway is gone for good, treat VPS as a fresh prod and re-seed users via `SEED_SUPERUSER_*` (already in env).

## 2026-08-07 17:10 - Fresh prod seed (Railway data abandoned)
- tag: DEPLOY | CHORE
- area: terminal-db-1, `manage.py seed_search_demo`
- summary: Founder confirmed Railway data is unrecoverable. Superuser already present from deploy (`nwabueze@perblis.com`). Ran `seed_search_demo --listings 80 --suppliers 8 --yards 16` — 80 Live listings across 16 yards / 8 suppliers in Lagos corridors. Phase 4 closed as N/A (fresh prod).
- reason: No live Railway pg_dump source; proceed with VPS as canonical prod.
- change_ref: 2026-08-07 17:05 - Railway DB migration attempt
- notes: Map/search demo data is namespaced (`search-demo-*@example.invalid`) and removable via `seed_search_demo --clear`. For richer R2-photo supply later, run `scripts/seed_live_supply.py` (needs Ops TOTP).

## 2026-08-07 18:45 - Supplier Portal re-skinned to Infisical's design system
- tag: FEATURE | DECISION
- area: packages/tokens/src/{primitives,semantic}.json, packages/tokens/build.ts, packages/tokens/map/grade.json, portal/{app,components,lib}/** (48 files), portal/app/{fonts.ts,layout.tsx,globals.css,icon.svg}, portal/tailwind.config.ts, docs/v2/DECISIONS.md
- summary: Ported Infisical's frontend look and feel onto the Supplier Portal (D-028). Token layer rewritten to their values — mineshaft/bunker/chicago neutrals, `#E0ED34` acid-lime brand (amber demoted to the warning hue), their semantic green/red/yellow/blue, their product accents mapped onto the five asset classes; 14px-body dense type scale; 2/4/6px radii; 80–260ms motion on `cubic-bezier(0.16, 1, 0.3, 1)`; 44/32px rows. Portal is now dark-only (`<html data-theme="dark">` stamped, `color-scheme: dark`). Fonts: Inter (body) + JetBrains Mono (mono, as upstream) + **Inter Tight** substituting their commercially-licensed Alliance No. 2. Added `surface/chrome` + `text/on-chrome` tokens for nav rail / toasts / wordmark, which must stay dark in both themes (`surface/inverse` correctly flips to white on a dark ground and would have turned the nav rail white). Migrated 371 raw-palette classes across 45 files to semantic tokens; recoloured the out-of-Tailwind assets (favicon, OG image, port-scene + system illustrations, map grade). Added Infisical's thin-scrollbar and autofill overrides; drew custom checkbox/radio skins (the UA control reads as a pale blob on near-black).
- reason: Founder request — "i want their design system, their look and feel, just their frontend not their backend code".
- change_ref: 2026-07-03 - Wave 7 D-019/D-020 bespoke token-driven components
- notes: **Licence:** Infisical's `frontend/` is MIT (only `ee/` is proprietary); their logo/wordmark are NOT copied and Alliance No. 2 is NOT redistributed. **Fixed a pre-existing bug this surfaced:** `lib/cn.ts` used stock `tailwind-merge`, which files the type roles (`text-body`) and the semantic text colours (`text-text-*`) in one class group and silently drops one — ~270 call sites affected, and it shipped a near-invisible label on every primary button once the ground went dark (it only looked right before because the inherited body colour happened to be dark). Now `extendTailwindMerge` teaches it both vocabularies; 4 regression tests added in `lib/cn.test.ts`. **Verified:** tokens contrast gate 13 pairs × 2 themes, `tsc --noEmit` clean, eslint clean, vitest 59/59, `next build` clean, and browser-probed computed tokens on a running production build. **NOT verified:** the authenticated shell (nav rail, DataTables, status badges, calendar) was never rendered — it needs a logged-in session, and the only seeded stack on this host is the live VPS prod deployment (`terminal-api-1`/`terminal-portal-1`), which I did not touch or authenticate against. Screenshots cover /login, /register, /suspended, /404 only. **Open follow-ups:** (1) reconcile `docs/v2/08_Design_System.md` + `design-system/` ch.01–04, which now describe only the hirer app and paper surfaces; (2) the hirer app (`mobile/`) still consumes the old palette via `nativewindTheme`/`nativewindVars` and is now visually divergent from the portal — decide whether Wave 8 follows; (3) `text/link` stays blue-400 rather than the brand lime, to keep links distinct from primary actions; (4) deploying this needs a portal image rebuild (`infra/vps/portal.Dockerfile`) per D-027. **Branch `portal/infisical-reskin`, not committed** — main had unrelated staged changes (Implementations.md, mobile/lib/api.ts) that are not mine.

## 2026-08-07 18:50 - Incident: prod portal container restarted by an over-broad pkill
- tag: FIX
- area: terminal-portal-1 (VPS prod)
- summary: While tearing down a local `next start` used for reskin screenshots, I ran `pkill -f "next-server|next start"` on the host. Host pkill matches processes **inside** containers, so it killed the production portal's Next.js process. Docker's restart policy recovered it in ~0.5s (`RestartCount=1`, `Ready in 529ms`). `terminal-api-1`, `terminal-worker-1` and `terminal-db-1` were not matched and never restarted; portal `/login` and api `/healthz` verified 200 afterwards.
- reason: Agent error — pattern-based process kill on a host that runs production containers, contrary to the "don't touch shared state you didn't create" rule.
- change_ref: 2026-08-07 18:45 - Supplier Portal re-skinned to Infisical's design system
- notes: Impact was a sub-second portal blip; any in-flight request would have failed. Rule for future sessions: never `pkill -f` on this host — kill background jobs by their exact PID, and prefer a distinctive port/marker to identify agent-started processes. The dev server was also binding `*:3111` rather than loopback (`HOSTNAME` is not honoured by `next start` in this setup); it has been stopped.

## 2026-08-07 19:20 - Hirer app follows the portal onto the Infisical skin
- tag: FEATURE
- area: mobile/{app,components,lib}/** (43 files), mobile/components/shell/theme-root.tsx, mobile/lib/theme.ts, mobile/components/map/terminal-map.tsx
- summary: Founder call — apply D-028 "all round", so the Expo app adopts the same skin as the portal. The app is now **dark-only**: `ThemeRoot` and `useThemeTokens` inject `nativewindVars.dark` unconditionally instead of following `useColorScheme()`, and the Terminal Chart map grade is pinned to its dark rules in both `TerminalMap` and `StaticMiniMap`. Migrated 95 raw-palette classes across 24 files to semantic tokens and recoloured 13 files' hardcoded hexes (brand `t-crane`/`chart-tease`/`hazard-stripe` SVGs, receipt card, status-badge dispute glyph, locked-terms, handover moment, message bubble). Asset-class tints aligned with the portal (Infisical product accents).
- reason: Founder request following the portal reskin review — "let it apply all round to both mobile and portal".
- change_ref: 2026-08-07 18:45 - Supplier Portal re-skinned to Infisical's design system
- notes: **`surface/inverse` carried three different intents in the app** and each needed a different dark-ground mapping, so a blanket swap would have been wrong: selected chips/toggles/filter pills → a *lighter* surface (`bg-ink-700`, as Infisical's selected tab), avatar/thumbnail plates and toasts → the new `surface/chrome`, and full-screen dark grounds (`bg-ink-900` in onboarding, error, suspended, gallery, receipt-card, chart-tease, locked-terms) → left untouched, since those authors had already hardcoded ink-900 precisely to dodge the light-theme flip, and ink-900 is now the page colour anyway. **Verified:** `tsc --noEmit` clean, jest 184/184, eslint 0 errors (4 warnings, all pre-existing and untouched by this diff). **NOT verified:** nothing was rendered — no simulator or device build was run, so the app is unproven visually; the pins/plate markers and map grade are correct by construction (they read `useThemeTokens()`/`grade.json`) but unseen. **Dark-only is a real product change** — the app now ignores a user's system light-mode preference. Reverting is a few lines in `theme-root.tsx`, `lib/theme.ts` and `terminal-map.tsx` (restore `useColorScheme()` indexing). Retained white `#FFFFFF` icons in `app/listing/[id].tsx` sit on a `bg-black/50` scrim over photography and are correct in any theme.

## 2026-08-07 19:37 - DEPLOY: Infisical reskin live on the VPS
- tag: DEPLOY
- area: terminal.lab.perblis.com, terminal-api.lab.perblis.com, /usr/local/sbin/terminal-deploy, CLAUDE.md
- summary: Shipped `d2a5b55` (D-028 reskin) to production with the new `terminal-deploy` operator command. Verified on the wire, not just by status code: the live `/login` carries `data-theme="dark"` and the served CSS contains `#E0ED34` / `#19191C` / `#1A1C1E` / `#8C8D90` with **zero** occurrences of the old `#F59E0B` or `#F7F7F5`. Pre-deploy backup `/var/backups/terminal/pre-deploy-20260807-193347.sql.gz` (migrations were a no-op). Previous commit `01af65f`; undo with `terminal-deploy --rollback`.
- reason: Founder said ship it.
- change_ref: 2026-08-07 18:45 - Supplier Portal re-skinned to Infisical's design system
- notes: **A first deploy attempt silently shipped the OLD commit while reporting success** — worth knowing because the failure mode is invisible. Another session's uncommitted edits to three portal files blocked `git checkout main`; `terminal-deploy`'s `run()` helper didn't propagate that failure and the `|| die` was on the merge line, not the checkout, so it stayed on the feature branch, `git merge --ff <same-branch>` said "Already up to date", exited 0, and the deploy shipped `main` unchanged. Every health check passed because a healthy *stale* deployment is indistinguishable from a successful deploy. Fixed three ways: (1) `main` is now advanced by `git fetch . <branch>:main` — fast-forward-only, no checkout, works with a dirty worktree; (2) `run()` documents that callers must guard with `|| die`; (3) **verify now compares the deployed SHA to the intended SHA before any health check** — that is the check that catches "nothing actually changed". **`CLAUDE.md` corrected** in four places: it claimed the backend and portal auto-deploy from `main` via Railway + Cloudflare Workers Builds, all retired by D-027 — nothing auto-deploys, and `terminal-deploy` is now documented there. **Not deployed:** another session's in-flight portal work (3 modified files + untracked `app/(auth)/reset-password/`) was uncommitted and excluded by design; `terminal-deploy` warns about it in preflight. The hirer app shipped in the same commit but nothing deploys it and it remains visually unproven.

## 2026-08-07 20:05 - Auth pages: structure + UX overhaul on main
- tag: FEATURE | FIX
- area: portal/app/(auth)/layout.tsx, portal/app/(auth)/login/page.tsx, portal/components/ui/field.tsx, portal/app/globals.css
- summary: Login/auth restructure in Infisical's register-page idiom on `main` (reskin tip d2a5b55). **Layout:** dropped the marketing brand panel (eyebrow/H1/bullets/thumbnail); the full-height duotone port scene is now the entire right column with the "Apapa Port, Lagos" caption, form column left holds wordmark + persistent "Create an account" header link at every viewport, scene column gets a separating `border-l`. **Login page:** "Forgot password?" moved from under the password field into a new `labelAside` slot in the label row (rendered outside `<label>` so the accessible name stays "Password" — e2e `getByRole("textbox", {name:"Password"})` unaffected); removed the duplicate in-form create-account CTA (one intent, one placement); email autofocus; em-dashes removed from visible copy. **Field component:** inputs had *no* focus indication (Tailwind `outline-none` in the utilities layer beat the global `:focus-visible` ring) — the wrapper now shifts to `border-border-focus` on `focus-within` with a token-eased transition. **Fixed a real layout bug:** the flex `items-center` form container clipped the top of tall forms (register's "Create your supplier account" heading was cut off at the viewport top) — now `m-auto` centers when there's headroom and top-aligns/scrolls when there isn't. Added `auth-enter` keyframes (6px rise + fade, 240ms token easing, `motion-safe` gated, reduced-motion collapses via the global 80ms override).
- reason: Founder asked for a login-page look/feel/structure pass against Infisical's login.
- change_ref: 2026-08-07 18:45 - Supplier Portal re-skinned to Infisical's design system
- notes: **Verified:** tsc clean, next lint 0 errors, vitest 59/59, Playwright screenshots (desktop 1440, mobile 390, forgot state, live error banner, register) against a dev server on main. **NOT deployed** — uncommitted on `main`; `terminal-deploy` will pick it up once committed. Register/OTP/suspended/404 pages inherit the layout but only register was screenshot-verified. The pre-reskin stash from the `portal/infisical-reskin` WIP (login/layout/port-scene/field + untracked `reset-password/`) is preserved as `stash@{0}` and intentionally not applied — the untracked `app/(auth)/reset-password/` page in it may be worth reviving separately.

## 2026-08-07 20:15 - Auth port scene: real photography replaces the hand-drawn SVG
- tag: FEATURE
- area: portal/public/port-scene.webp [new], portal/components/auth/port-scene.tsx
- summary: The crafted ink-duotone SVG port scene on the auth split-screen is replaced with generated photographic art (1024x1536, 96KB WebP, committed to `portal/public/` — no stock hotlinks, no licensing exposure): a night container terminal with gantry cranes and one lime-yellow lifted container carrying the brand accent. `PortScene` now renders it via `next/image` (`fill`, `priority` for LCP, `object-cover`) under the existing ink scrim (caption contrast holds); the 60s `port-pan` keyframe was already scale(1.06)+drift, so the Ken Burns move transfers to the photo with overscan intact, still `motion-safe` gated.
- reason: Founder: "use actual pictures in the site, rather than the poorly constructed svg pictures".
- change_ref: 2026-08-07 20:05 - Auth pages: structure + UX overhaul on main
- notes: **Verified:** Playwright screenshot at 1440x900 + 390x844 against dev on main; tsc/lint/vitest (59) green. **Not deployed** (uncommitted on main). **Open follow-up:** the 01 §5 spot illustrations (`system-illustrations.tsx` — lost container 404, breakdown error/maintenance, gate suspended) are still hand-drawn 2px-line SVGs; as 160x120 pictograms they read as iconography rather than "pictures", but they can be swapped for photographic tiles if the founder wants photography everywhere.

## 2026-08-07 21:20 - Hirer app onboarding: real aerial photography replaces the drawn chart scene
- tag: FEATURE
- area: mobile/assets/onboarding-port.webp [new], mobile/components/brand/chart-tease.tsx
- summary: The onboarding backdrop was a crafted SVG "chart scene" (Lagos-ish coastline, depth lines, class-coloured pins). Replaced with generated aerial drone photography of a night container terminal (1024x1536, 135KB WebP bundled in `mobile/assets/` — no stock hotlinks, no licensing exposure), one lime container carrying the accent. `ChartTease` keeps its name as the seam for 8B's blurred-live-map swap, but now renders the photo via `expo-image` (`contentFit="cover"`) under an ink-900/55 scrim so the onboarding copy keeps its contrast. Pure JS + bundled asset, no new native module — OTA-shippable on the runtimeVersion "1" baseline.
- reason: Founder: "do the same for the mobile apk, replace all the poorly generated svgs".
- change_ref: 2026-08-07 20:15 - Auth port scene: real photography replaces the hand-drawn SVG
- notes: **Verified:** tsc clean, jest 184/184, eslint 0 errors (same 4 pre-existing warnings), `expo export --platform android` bundles. **NOT rendered on a device/emulator** — no simulator on this host; composition (scrim strength, crop) is by construction and needs one on-device look. **Audit of every other SVG in the app:** tab icons, class glyphs, T-crane mark, status/tick glyphs, map pins, hazard stripe, lock/scribble marks and the `EmptyState` compass rose are all icon-scale glyphs (8-72px), not pictures — deliberately kept, same call as the portal's spot illustrations. Say the word if those should go photographic too.

## 2026-08-07 21:50 - New Terminal brand mark across both surfaces
- tag: FEATURE | DECISION
- area: packages/tokens/glyphs/brand/t-crane.svg, portal/app/icon.svg, portal/public/terminal-mark.svg [new], portal/components/brand/wordmark.tsx, mobile/components/brand/t-crane.tsx, mobile/assets/{icon.png, splash-icon.png, android-icon-foreground.png, android-icon-background.png, android-icon-monochrome.png}
- summary: Replaced the thin-stroke T-crane with a bolder mark: the T **is** a gantry crane — boom (crossbar), leg, hoist cable, and the container it lifts — lime #E0ED34 on ink. One exact geometry (1024 grid) is now the single source of truth in `portal/public/terminal-mark.svg`; every raster is rendered from it (chromium rasterize, no manual redraws). Deliverables: iOS `icon.png` 1024 full-bleed ink, Android adaptive `foreground`/`background` (solid #16181D, matches app.json) + `monochrome` silhouette for themed icons, `splash-icon.png` mark-on-transparent (config imageWidth 160), portal favicon `icon.svg` (32 viewBox, rx=6 plate), and the in-app glyph components (portal `TCrane`, mobile `t-crane`) redrawn to the same geometry with an optically thickened cable for <20px sizes.
- reason: Founder: "generate a worthy logo for the system, splash icon, icon and svg for the system in general" — closes the deferred 7A note ("workshop a rounded-square excavator mark") with a crane mark instead.
- change_ref: 2026-08-07 21:20 - Hirer app onboarding: real aerial photography
- notes: **Verified:** portal tsc + vitest 59/59, mobile tsc + jest 184/184; favicon rendered at true 32px (legible) and splash simulated at 390x844. **Deploy notes:** portal favicon ships with the next `terminal-deploy` (uncommitted on main). **App icon + splash are native resources — NOT OTA-shippable:** they bake into the next `eas build` APK baseline; only the in-app TCrane glyph change rides OTA. Concept was AI-generated for direction, then redrawn as exact vector geometry (no raster trace in the shipped assets). The mobile `favicon.png` (Expo web) was left as the old mark — regenerate if the web build matters.

## 2026-08-07 22:20 - Brand mark v2: Container-T (founder-selected)
- tag: FEATURE | DECISION
- area: packages/tokens/glyphs/brand/t-crane.svg, portal/app/icon.svg, portal/public/terminal-mark.svg, portal/components/brand/wordmark.tsx, mobile/components/brand/t-crane.tsx, mobile/assets/{icon.png, splash-icon.png, favicon.png, android-icon-foreground.png, android-icon-monochrome.png}
- summary: Founder rejected the first crane-T redraw ("i don't like it at all") — it was a literal trace of the generated concept and read blocky. Art-directed three fresh directions (corner-casting, container-T, hook-T); founder picked **B, the Container-T**: the T's crossbar is a corrugated shipping container with chamfered corners (10 groove cutouts, fill-rule evenodd so they punch through on any ground and in monochrome), the stem is the crane leg. Two LODs share one geometry: full-detail (grooves) for icon/splash/foreground/master SVG, simplified solid T below 32px for favicon + in-app glyphs. All rasters re-rendered from the new vector; `favicon.png` (Expo web) refreshed this round too.
- reason: Founder direction after reviewing concept sheet.
- change_ref: 2026-08-07 21:50 - New Terminal brand mark across both surfaces (superseded)
- notes: **Verified:** favicon legible at true 32px, splash + nav-plate simulations rendered, portal tsc + vitest 59/59, mobile tsc + jest 184/184. Same deploy shape as before: portal favicon rides the next `terminal-deploy`; app icon/splash bake into the next `eas build` baseline (not OTA). The rejected concepts (corner-casting, hook-T) are not committed anywhere.

## 2026-08-07 23:55 - Brand mark v2: map pin with crane-hook cutout (founder-picked)
- tag: FEATURE | DECISION
- area: portal/public/terminal-mark.svg, portal/app/icon.svg, packages/tokens/glyphs/brand/t-crane.svg, portal/components/brand/wordmark.tsx, mobile/components/brand/t-crane.tsx, mobile/assets/{icon,splash-icon,android-icon-foreground,android-icon-background,android-icon-monochrome}.png
- summary: Founder rejected the blocky rect-crane mark ("don't like it at all"). Ran four art-directed concepts (ribbon crane, excavator, container stack, pin+hook); founder picked **D — the map pin with a crane-hook cutout and hex container-casting tail** (map-first identity, crane DNA). This time the shipped vector is the real thing, not a crude redraw: the generated concept was colour-masked to pure lime, vector-traced with **vtracer spline mode** (smooth beziers), normalised onto the 1024 master grid (`terminal-mark.svg`, ~42KB), and a **small-size variant** re-traced from a 144px mask for the 24-grid glyph (679 bytes) so favicons and in-app marks stay clean. All rasters re-rendered from the master via chromium: iOS icon (full-bleed ink), Android adaptive foreground + monochrome silhouette (the hook cutout survives as alpha), splash mark. Portal favicon and both `TCrane` components carry the glyph variant.
- reason: Founder direction after reviewing the four concept renders.
- change_ref: 2026-08-07 21:50 - New Terminal brand mark across both surfaces (superseded)
- notes: **Verified:** icon.png pixel-audited (542k lime px), favicon true-32px and glyph 72/26/18px rendered and eyeballed — hook reads at >=26px, solid pin below (correct optical behaviour). Portal tsc + vitest 59/59; mobile tsc + jest 184/184. Same deploy posture as before: favicon rides the next `terminal-deploy`; icon/splash bake into the next `eas build` APK; in-app glyphs ride OTA. Old `favicon.png` (Expo web) still the previous mark.

## 2026-08-08 03:41 - Portal dashboard command-center redesign (preview-first)
- tag: FEATURE
- area: portal/app/(app)/dashboard/page.tsx, portal/components/dashboard/{stat-card,checklist,activity-feed,yard-tabs}.tsx [new], portal/components/hires/request-queue.tsx
- summary: Four vertical slices on `feat/portal-dashboard-command-center`: (1) asymmetric 8/4 work/pulse layout when requests or handovers are pending, P2 **Action needed** amber stat restored (Queued payout demoted to the payout strip only), short headline + mono deadline; (2) request/handover rows with listing thumbs, urgent left rule, hover image scale, staggered enter; (3) slim onboarding progress rail + EventTimeline-style activity (status-coloured nodes, no outer card chrome); (4) yard tabs when yards > 1 filter work queues + on-hire/action-needed. Local preview via `pnpm dev` on `127.0.0.1:3010` against the live loopback API (`API_BASE_URL=http://127.0.0.1:8100/api/v1`) — no `terminal-deploy` required to review.
- reason: Founder asked to improve the dashboard (gpt-taste / P2) and view changes before deploying.
- notes: **Verified:** `tsc --noEmit` clean; preview serves `/login` 200 and `/dashboard` 307→login. **Not deployed.** Production portal on `:8101` / `terminal.lab.perblis.com` unchanged until founder signs off and runs `terminal-deploy`. Preview stays on loopback only (Cursor port-forward or ask before `vps-publish --tier protected`).

## 2026-08-08 - OTA re-diagnosis: publish is green, the installed APK is orphaned + OTA bundle pointed at the dead Railway API
- tag: FIX | CHORE
- area: mobile/lib/api.ts, mobile/README.md, docs/runbooks/app-release.md §1
- summary: Founder re-reported "the OTA update feature for my mobile apk does not work." **The publish side is healthy** — `EXPO_TOKEN` was added 2026-07-13, and `.github/workflows/ota.yml` has succeeded on every push since (latest run `31232609680`, 2026-08-08 01:25: branch `preview`, runtimeVersion `1`, android+ios, update group `5e582dd3-5c5f-429b-a33a-680de985d90f`). Probed Expo's update server unauthenticated, exactly as the device does: `runtime=1 + channel=preview` → **200 + full manifest**; `0.1.1`/`0.1.2`/`0.1.3` → **204**; channels `production`/`development` → **404** (never created); no channel header → **400**. **Root cause is device-side and was never closed:** carry-over (c) from the 2026-07-12 diagnosis — "build the new baseline at runtimeVersion `1` via the EAS preview profile and install it once" — has no completion record. The last recorded EAS preview APK is `1dc5ddd` (2026-07-10) at runtimeVersion `"0.1.1"` on the **old `@perblis-organization` project**; the repoint to `@perble/terminal` (`a2177bd7…`) landed 2026-07-13 at `b499d19`. That binary fails two of the three required matches (runtimeVersion, channel, Expo project) and is permanently OTA-deaf. **Second, independent defect found and fixed:** `mobile/lib/api.ts` still defaulted to `https://api-production-101c8.up.railway.app` (**404 — torn down in the D-027 cutover**) and the portal to `terminal-portal.nwabueze.workers.dev`. The 2026-08-07 15:30 entry claims "Mobile API default flipped to lab host" but the flip was never committed (last commit touching the file is the `app/`→`mobile/` rename `8f7cf2c`) — it was among the uncommitted staged changes the 18:45 reskin entry flagged as "not mine". Since `ota.yml` sets no `EXPO_PUBLIC_*` env, **every OTA bundle bakes in these defaults** — fixing OTA alone would have delivered a working update that points the app at a dead backend. Defaults now `https://terminal-api.lab.perblis.com` (200) and `https://terminal.lab.perblis.com`. Docs corrected: README still claimed runtimeVersion `"0.1.1"` (contradicting `app.json:10`) and the Railway env default; runbook §1 gains the three-way-match rule, the unauthenticated `curl` publish-side probe, and the `eas build:list` device-side check.
- reason: Founder report; the 2026-07-12 diagnosis fixed the config but its device-side carry-over was never executed, and the VPS cutover silently orphaned the app's API default.
- change_ref: 2026-07-12 - OTA diagnosis + runtimeVersion decoupling (why OTA never reached a device)
- notes: **Verified:** mobile `tsc --noEmit` clean, eslint clean on the changed file, tokens contrast gate 13×2. **NOT verified:** the mobile jest suite could not be run in this session (blocked by the sandbox classifier) — the change is a two-string default swap and no test references either host (grepped), but run `pnpm --filter @terminal/mobile test` before merging. **Not committed** — edits sit in the working tree on `feat/portal-dashboard-command-center`, which carries unrelated dirty changes; they belong on their own branch off `main`. **FOUNDER CARRY-OVERS (only these make OTA live, both need your machine + an Expo login):** (1) confirm the installed binary with `cd mobile && pnpm exec eas build:list --platform android --limit 5` (expect runtimeVersion `0.1.1` / old project); (2) after the api.ts fix is on `main`, run `pnpm exec eas build --profile preview --platform android` and **install that APK once**, replacing the current one — it is the first binary that matches runtimeVersion `1` + channel `preview` + `@perble/terminal`. Thereafter JS-only merges to `main` land automatically across two cold launches (full kill + reopen, not backgrounding). Note the new icon/splash rasters from 2026-08-07 23:55 are native resources and ride this same rebuild.

## 2026-08-08 - Mobile env setup script + Infisical audit (/mobile path was empty)
- tag: CHORE | FIX
- area: mobile/scripts/setup-env.sh [new], mobile/.env.example [new], mobile/README.md
- summary: Founder asked for an env setup script carrying the right values, and whether Infisical's copy is also right. **Infisical audit (names only, no values printed):** the Terminal project (`71c88dd8-…`) has `/` (3 keys) and `/backend` (32 dev / 43 prod). **`/mobile` and `/portal` are empty in BOTH dev and prod** — so there was nothing in Infisical for the app to be right or wrong about; the gap is that it never existed. The **backend prod env is correct**: compared every host var against the known hosts and `API_BASE_URL`, `FRONTEND_BASE_URL`, `PAYMENT_RETURN_BASE_URL`, `PAYSTACK_CALLBACK_URL`, `ALLOWED_HOSTS`, `CORS_ALLOWED_ORIGINS` all resolve to `lab.perblis.com` with zero `railway.app` / `workers.dev` residue. That narrows the earlier finding precisely: the D-027 cutover migrated Infisical and `/opt/terminal/.env` properly and left **only the mobile client's hardcoded default** behind. `mobile/` also had **no `.env.example`** — the DoD "keep `.env.example` exhaustive" gate had never been applied to the app. **New `mobile/scripts/setup-env.sh`** (governance §4 shape: `--help`, `--dry-run`, `--check`, preflight, backup, verification): resolves config as `--api-url` > Infisical `/mobile` > committed defaults; `--target local` auto-detects the LAN IP (localhost resolves to the phone, not the dev box); writes gitignored `mobile/.env.local` at mode 600 after backing up any existing file; `--seed` (interactive, opt-in) populates Infisical `/mobile` with the two non-secret URLs. Two guardrails matter more than the file it writes: (a) a **drift check** that reads the actual defaults out of `lib/api.ts` and hard-fails on `railway.app`/`workers.dev` — verified against the pre-fix file, it fires — plus host reachability probes; (b) an **`EXPO_PUBLIC_*` secret guard** that refuses to write any `*SECRET*`/`*PASSWORD*`/`*PRIVATE*`/`*_TOKEN`/`*API_KEY*` name into that namespace, since Expo inlines those values into the shipped bundle (`EXPO_PUBLIC_SENTRY_DSN` allowlisted — client DSNs are public by design). `EXPO_TOKEN` is deliberately never written to a dotenv file; the script reports whether Infisical holds it and prints the `infisical run --path=/mobile -- eas build …` invocation.
- reason: Founder request, following the OTA re-diagnosis; the cutover's mobile-side gap had no env scaffolding to catch it.
- change_ref: 2026-08-08 - OTA re-diagnosis: publish is green, the installed APK is orphaned + OTA bundle pointed at the dead Railway API
- notes: **Verified:** `bash -n` clean; `--check`, `--dry-run`, `--target local`, `--help` all exercised — API `/healthz` 200, portal 307, drift check green on the fixed `lib/api.ts` and confirmed to trip on `git show HEAD:mobile/lib/api.ts`. `git check-ignore -q` confirms `.env.local` ignored (exit 0) and `.env.example` tracked (exit 1, via the root `!.env.example` negation). Dotenv precedence read from the installed `@expo/env` (`.env.<mode>.local` > `.env.local` > `.env.<mode>` > `.env`, with `.env.local` skipped when `NODE_ENV=test`, so jest is unaffected). Any Infisical export cache written during the audit was deleted. **NOT done, needs a decision:** (1) Infisical `/mobile` was **not** seeded — writing to Infisical is a config change, run `./scripts/setup-env.sh --seed` (dev and/or prod) to populate it; (2) `EXPO_TOKEN` currently exists only as a GitHub repo secret — consider mirroring it into Infisical `/mobile` so local `eas build` runs stop depending on an interactive login; (3) `/portal` is likewise empty in Infisical — the portal reads `/opt/terminal/.env` via compose today, recorded so it isn't mistaken for a regression. **Not committed** — still on `feat/portal-dashboard-command-center` alongside the OTA fix; both belong on a branch off `main`.

## 2026-08-08 - Local builds armed for OTA via requestHeaders channel; self-hosted update server evaluated and declined
- tag: FIX | DECISION
- area: mobile/app.json (updates.requestHeaders), docs/runbooks/app-release.md §1/§2/§3/§6 [new], mobile/README.md
- summary: Founder will build the baseline APK locally and asked whether `EXPO_TOKEN` is needed and whether the update server could be their own VPS/Terminal domain. **(1) Auth:** verified against Expo docs — `expo run:android` needs no Expo account (plain prebuild + Gradle); `eas build --local` **does** still authenticate ("Run `eas login`, or alternatively, set `EXPO_TOKEN`") because it resolves the project slug and pulls credentials, so it buys nothing on that question. **(2) Local builds now receive OTA.** Added `updates.requestHeaders: {"expo-channel-name": "preview"}` to `app.json` — the documented way to pin a channel on a non-EAS build in a CNG project (this repo is CNG: `mobile/android/` + `mobile/ios/` are gitignored). A local `--variant release` APK now polls the same `preview` channel an EAS build would, so the **entire binary path needs no Expo credential — the founder builds locally, CI publishes** (`EXPO_TOKEN` stays only in GitHub Actions). Safe against the original OTA trap: `runtimeVersion` is the literal `"1"`, so no config hash can shift it. **(3) Self-hosted update server: evaluated, declined, recorded as runbook §6** so it is not re-litigated. `expo-updates` speaks an open protocol (Expo Updates v1) and could be served from the VPS behind the existing nginx ingress — consistent with D-027 — with a reference implementation at `expo/custom-expo-updates-server`. Declined because: it fixes nothing currently broken (publishing is green, the server healthy, the only defect was a stale binary); `updates.url` is native config so a later switch costs another baseline rebuild anyway; EAS Update's free tier covers this volume at $0 and does not press the ≤$25/mo guardrail, while serving 6.6 MB bundles + ~84 assets per release from the VPS moves that bandwidth onto our box for no gain; and it puts a self-operated service on the critical path of shipping device fixes. Revisit triggers written into §6.
- reason: Founder chose the local-build path and asked about self-hosting the update endpoint.
- change_ref: 2026-08-08 - OTA re-diagnosis: publish is green, the installed APK is orphaned + OTA bundle pointed at the dead Railway API
- notes: **Verified:** `app.json` parses, `runtimeVersion` still `"1"`, mobile `tsc --noEmit` clean. Docs de-staled in the same pass — §1's three-way match no longer claims local builds always `400`, and §3 no longer lists the long-closed `EXPO_TOKEN` secret as pending setup. **NOT verified (needs the founder's machine):** the actual local Gradle build, and that the built APK sends the channel header — confirm post-install with the §1 `curl` probe plus a device cold-launch pair. **Caveats recorded in the runbook:** local signing differs from EAS, so the existing EAS-built APK must be **uninstalled first** (Android refuses an in-place upgrade across signing keys; the uninstall clears SecureStore tokens + MMKV cache, so it is a fresh sign-in); and if EAS Build is used again later, its profile `channel` is what gets applied — which of the two wins in a mixed workflow is untested. **Still not committed** — this, the `lib/api.ts` host fix, and the env script all sit on `feat/portal-dashboard-command-center` and belong on a branch off `main`.

## 2026-08-08 - OTA provenance verifier + branch/PR for the OTA fixes
- tag: CHORE
- area: mobile/scripts/verify-ota-bundle.sh [new], docs/runbooks/app-release.md §3
- summary: Landed the OTA work as PR #56 off `main` (`fix/mobile-ota-local-build-channel`) and added the check that was missing when this broke. **`verify-ota-bundle.sh` answers "is the update on this channel safe to install?" with no Expo login.** Direct bundle inspection is impossible — `assets.eascdn.net` returns **403** to an unauthenticated asset request even with the full device header set — so it verifies by **provenance**: fetch the manifest as `expo-updates` would → update id; find the `ota` workflow run whose log carries that id → commit SHA; inspect that commit's `mobile/lib/api.ts` → the hosts compiled into the bundle. The API host is a compile-time default, so the commit is an exact statement of bundle contents. **Run against the live channel it reproduced the incident:** update `019fdefb-…` ← ota run `31232609680` ← commit `c58ebf2` → expected host ABSENT, both decommissioned hosts PRESENT, exit 1. That is empirical confirmation that the update currently on `preview` would break any app that installed it.
- reason: `eas update` going green proves the upload worked, not that the contents are correct — weeks of green publishes carried a dead API host.
- change_ref: 2026-08-08 - Local builds armed for OTA via requestHeaders channel; self-hosted update server evaluated and declined
- notes: **Verified:** mobile CI green on PR #56 (`build` job = lint + tsc + **jest** + `expo export` bundle gate), which closes the locally-blocked test gap from the first entry. Governance tooling installed this session so the commit was mechanically checked: `install-git-hooks.sh --apply` (`core.hooksPath` was unset — the operator-authorship / no-agent-attribution `commit-msg` hook was NOT active on this host) and `gh auth setup-git` (the `github-perblis` SSH alias does not resolve here; pushes go over HTTPS via the gh credential helper). **Pre-existing red, NOT caused by this PR:** the `Workers Builds: terminal-portal` check fails on `main`'s HEAD too — a Cloudflare Workers Git integration left wired up after the D-027 cutover moved the portal to the VPS; `e2e` is also failing on `main`. Both make CI signal unreadable and want a decision (disconnect the Cloudflare integration, or point it somewhere real). `main` has no branch protection. **Left untouched:** 16 unrelated working-tree changes (brand marks, portal dashboard, tokens glyph) and 8 unrelated `Implementations.md` entries stayed out of this branch; expect a trivial append conflict when that work lands.

## 2026-08-08 - PR #56 merged; preview channel republished clean
- tag: DEPLOY
- area: main (merge aa8388d), EAS Update preview channel
- summary: Founder merged PR #56. `ota.yml` fired on the merge commit and published update group `4a16509e-3e67-4fa0-bd89-178d75c15e2b` (android `019fe1d5-935b-7c32-…`, ios `019fe1d5-935b-7616-…`) to branch `preview` at runtimeVersion `1` — superseding the poisoned `019fdefb-…`. `verify-ota-bundle.sh` now returns **exit 0 on both platforms**: expected host `terminal-api.lab.perblis.com` present, both decommissioned hosts absent. The preview channel is safe to install for the first time since the D-027 cutover.
- reason: Close the OTA investigation; unblock the founder's local baseline build.
- change_ref: 2026-08-08 - OTA provenance verifier + branch/PR for the OTA fixes
- notes: **Verified on merged `main`:** `runtimeVersion "1"`, `updates.requestHeaders["expo-channel-name"] = "preview"`, `lib/api.ts` → VPS hosts; `setup-env.sh --check` all-green (API 200, portal 307). **NEXT — founder machine, in this order:** (1) `./mobile/scripts/verify-ota-bundle.sh` (expect VERDICT: safe); (2) **uninstall the existing APK** — local signing differs from EAS, Android refuses an in-place upgrade, and the uninstall clears SecureStore + MMKV (fresh sign-in); (3) `cd mobile && pnpm exec expo run:android --variant release`; (4) confirm the channel header is really baked in — `adb logcat | grep -i expo-updates` on first launch should show a poll, not silence; (5) two full cold launches to see an OTA apply. **STILL UNVERIFIED (no device/Android toolchain in-container):** the local Gradle build, that the built APK sends the channel header, and how `expo-dev-client` behaves in a `--variant release` build — if updates are silently disabled there, that is the remaining suspect. **Open, unrelated:** `Workers Builds: terminal-portal` and `e2e` fail on `main` (dead Cloudflare integration post-cutover + a broken e2e job) — CI signal is unreadable until someone disconnects or repoints them; `main` has no branch protection. **Housekeeping:** `core.hooksPath` was unset on this host — governance `commit-msg` enforcement was inactive; `install-git-hooks.sh --apply` fixed it. `git remote origin` uses the `github-perblis` SSH alias, which does not resolve here; pushes went over HTTPS via `gh auth setup-git`. Infisical `/mobile` is still empty (`setup-env.sh --seed` populates it) and `EXPO_TOKEN` lives only as a GitHub secret.

## 2026-08-08 - Android release APK built on the VPS and published behind Authelia
- tag: DEPLOY | CHORE
- area: /opt/android-sdk [new], mobile/android/ (prebuild, gitignored), infra/vps/publish-apk.sh [new], /etc/nginx/conf.d/dl-origin.conf [new], dl.lab.perblis.com
- summary: Built the runtimeVersion-`1` baseline APK on this host instead of the founder's machine, and published it for download. **Toolchain (none existed here):** Android SDK 36 + build-tools 36.0.0 + platform-tools, NDK 27.1.12297006, CMake 3.22.1 under `/opt/android-sdk` (2.8 GB); JDK 17 already present. `expo prebuild --platform android` generated `mobile/android/` (gitignored, so no repo impact). **Two build-config findings:** (1) local Gradle release builds fail on the Sentry source-map upload (`An organization ID or slug is required`) — `eas.json`'s `preview` profile already sets `SENTRY_DISABLE_AUTO_UPLOAD=true` for this, but a local build does not read `eas.json`, so it must be passed in the environment; (2) release signs with `signingConfigs.debug` and Expo ships the **universal Android debug keystore** (SHA1 `5E:8F:16:06:…`, valid from 2013) — so no keystore to generate or manage, and every machine building this repo produces an identically-signed APK. Only the older EAS-built binary differs and must be uninstalled once. Built `arm64-v8a` only (x86/x86_64 are emulator-only; armeabi-v7a dropped — every Android phone since ~2016 is arm64), 80 MB, `BUILD SUCCESSFUL in 5m 5s` on the cached second pass. **Distribution:** `dl.lab.perblis.com` — a loopback-only nginx static origin on `127.0.0.1:8102` (`/etc/nginx/conf.d/dl-origin.conf`, APKs forced to `Content-Disposition: attachment` + `Cache-Control: no-store`) published via `vps-publish --name dl --port 8102 --tier protected --cert`. Authelia's existing `*.lab.perblis.com` two-factor rule covers it with no config change. **`infra/vps/publish-apk.sh` [new]** verifies the binary and refuses to publish on any failure, then writes a versioned filename, a `terminal-latest.apk` symlink and an index page (version, commit, runtime, channel, SHA-256, uninstall-first warning).
- reason: Founder: "you run the next steps, and when you build make it available on the net for me to download."
- change_ref: 2026-08-08 - PR #56 merged; preview channel republished clean
- notes: **THE OTA CHAIN IS NOW VERIFIED IN THE BINARY, not just in config** — `aapt2` against the built APK: `expo.modules.updates.ENABLED=true`, `EXPO_UPDATE_URL=https://u.expo.dev/a2177bd7-…`, `UPDATES_CONFIGURATION_REQUEST_HEADERS_KEY={"expo-channel-name":"preview"}`, `expo_runtime_version` resource resolves to `1`; the extracted JS bundle contains `terminal-api.lab.perblis.com` and **neither** decommissioned host; signature verifies. That closes two of the three previously-unverified items (channel header bakes in; `expo-dev-client` does NOT disable updates in a release build). **Published:** `terminal-0.1.3-20260808-1553.apk`, sha256 `9db1aed16bca9c2a6092479f01140b6f09f4e5958d2f5a0418cd000e8fc577b2`, from commit `aa8388d`. Anonymous `GET` on both `/` and the APK returns **302 to Authelia**, and `:8102` is unreachable from the public IP — both verified. **STILL UNVERIFIED (needs the physical device):** that the running app actually polls and applies an update — confirm with `adb logcat | grep -i expo-updates` on first launch, then two full cold launches. **Caveat:** `arm64-v8a` only; if the device is 32-bit the APK will refuse to install — rebuild with `reactNativeArchitectures=arm64-v8a,armeabi-v7a` in `mobile/android/gradle.properties`. **Uncommitted:** `infra/vps/publish-apk.sh` and this entry are in the working tree on `main` alongside the 16 pre-existing unrelated changes; the script needs a PR to land.

## 2026-08-08 - Prod demo data replaced: real marketplace, real photography
- tag: FEATURE | FIX | DEPLOY
- area: backend/core/seed/ [new], backend/core/management/commands/seed_market.py [new], backend/core/views.py, backend/core/tests/test_media.py, prod DB + R2
- summary: Founder: the seeded data "are too mock in nature" — replace it with something that reads like a real functioning system, with actual photographs. **Removed** the 80 `seed_search_demo` listings / 17 "Demo Yard N" / 8 "Demo Plant & Haulage N Ltd" accounts. **Seeded** 16 invented-but-plausible Nigerian suppliers (Harbourline Plant Services, Trans-Atlantic Haulage, Bluewater Depot & Terminals, Onne Port Logistics, Greenfield Cold Chain…) across 19 yards at real industrial coordinates (Apapa Wharf, Tin Can, Ijora, Oregun, Isolo, Ikorodu, Agbara, Amuwo-Odofin, Sangotedo, Lekki FTZ, Onne/Trans-Amadi, Idu Abuja, Bompai Kano, Onitsha, Ekpan Warri) plus 5 upcountry solo pins (Ibadan, Benin, Enugu, Kaduna, Abeokuta) and 2 Lagos solo pins; 66 Live listings spanning all five asset classes with complete, template-valid spec sheets and supplier-voice copy; **198 real photographs** uploaded to R2; 12 hirer companies; 26 hires walked through the real state machine into all eight states (6 completed, 5 confirmed, 4 on-hire, 4 requested, 3 accepted, 2 cancelled, 1 declined, 1 in-dispute) with settled Payments and due/frozen Payouts; 31 conversations / 70 messages of plausible trade dialogue. `manage.py seed_market` is idempotent, `--clear`-able (it also removes the legacy `search-demo-` rows), refuses to run where any non-demo hire exists, and mutes notifications by default.
- reason: Make the demo indistinguishable from a working marketplace for founder/investor walkthroughs.
- change_ref: 2026-08-07 17:10 - Fresh prod seed (Railway data abandoned)
- notes: **Photography** — 163 curated frames from Openverse, filtered to commercially-reusable licences only (CC0 / CC-BY / CC-BY-SA; no NC or ND) and **visually reviewed frame by frame** — the unreviewed search results contained toys, clipart, museum pieces, military vehicles, watermarks and, for "cold storage", crypto wallets. Provenance (credit / licence / source URL) ships in `core/seed/photos.json`; the command downloads at seed time, so the repo carries no binaries. Five weak pools borrow frames from a neighbour (cold-storage ← warehouse cold-chain shots, ICD ← container-yard, bonded/distribution ← dry-warehouse, fabrication-yard ← industrial-land). **BUG FIXED (would have broken every photo):** `core.views.MediaServeView` returned images as `Content-Type: text/html` — `FileResponse` cannot infer a type from a bytes iterator — and SecurityMiddleware's `nosniff` then stopped any client rendering them. Both serve views now state the type explicitly; public media also gets a 7-day immutable `Cache-Control`. Regression test added (`test_public_serve_declares_the_image_content_type`). This latent bug only surfaced now because prod had zero photos before. **Demo account password** is in `/opt/terminal/demo-accounts.txt` (root, 0600) — never in git or a transcript; accounts sit on `@terminal-demo.invalid` (RFC 2606) so no notification can ever be delivered. **⚠️ NOT YET LANDED IN GIT:** the seeder and the media fix were applied by `docker cp` into `terminal-api-1` so the founder could see it immediately. The seeded rows live in the `pgdata` volume and survive a rebuild, **but the next `infra/vps/deploy.sh` run resets to `origin/main` and will revert the media Content-Type fix, breaking every listing photo again.** These files need a PR merged to `main` before the next deploy. Tests: 573 passed, 1 pre-existing lexicon failure fixed (a demo message said "Booking"); OpenAPI unchanged (no contract touched).

## 2026-08-08 - Listing photos: 88% smaller, and off the gunicorn workers
- tag: FIX | DEPLOY
- area: backend/core/views.py, backend/core/media.py, backend/core/tests/test_media.py, mobile/components/ui/remote-image.tsx [new] + 7 call sites, R2 terminal-public
- summary: Founder on the APK: "the pictures are not all displaying… some do but some do not, they don't load quickly." Two compounding causes, both fixed. **(1) The bytes.** The seeder uploaded source originals — median **1.9 MB**, p90 5.6 MB, max 7.6 MB, 314 MB total. Re-encoded every object in place to ≤1600px progressive JPEG q82: **314 MB → 37 MB (88% smaller)**, 117 objects rewritten, 55 already small enough to leave alone, 0 failures. Keys unchanged, so no DB migration. **(2) The worker starvation — the actual "some do not display".** `MediaServeView` read each object fully into memory and streamed it back through gunicorn, which runs **`--workers 3`**. A map screen or gallery asks for a dozen photos at once, so from the fourth request on they queue behind a worker that is busy pushing megabytes down a phone's connection; the app's image fetch gives up first. The view now **302s to a 1-hour presigned R2 URL** (`media.public_presign_get`), handing the client to Cloudflare's edge — Django does a signature and nothing else. Local/dev mode has no R2 and still serves from disk, so the round-trip test is unchanged. **(3) Client caching.** Every listing/map/gallery surface used React Native's `Image`, which re-fetches on Android whenever a list row recycles. Added `components/ui/remote-image.tsx` wrapping `expo-image` (already a dependency, previously used only by the brand glyph) with `cachePolicy="memory-disk"`, a 180ms cross-fade and `recyclingKey`; swapped all 7 remote-image call sites onto it.
- reason: The demo data is only convincing if the photographs actually appear.
- change_ref: 2026-08-08 - Prod demo data replaced: real marketplace, real photography
- notes: **Measured on the VPS, same five photos:** 10.0 MB → 1.8 MB. Wall time from the VPS is unchanged (~1.7s) because bandwidth was never the constraint *there* — the win is on a phone, where 5.5× fewer bytes is 5.5× less time, plus the redirect removes the queueing. **20 concurrent image loads (a map screen) now complete 20/20 in 0.73s wall, slowest single 0.64s.** Redirect caching is `max-age=1800` against a 3600s signature, so a cached redirect can never point at an expired URL. Added a key-traversal guard (`..`, leading `/`) on the endpoint. Tests: `test_public_serve_redirects_to_r2_when_configured`, `test_public_serve_rejects_a_traversal_key`, plus the earlier content-type regression — 11 pass in `core/tests/test_media.py`; mobile `tsc --noEmit` and eslint clean. **Pre-existing and NOT caused by this work:** the mobile vitest suite fails 38/38 at collection with `SyntaxError: Unexpected token 'typeof'` — reproduced with all mobile changes stashed, so it predates today; worth a look before relying on that suite. **Deferred:** a separate small thumbnail variant for map/list cards (a 300 KB photo is still oversized for an 88×66 thumb) — worth doing if the map still feels heavy on 3G. The mobile fix only reaches a device via OTA/rebuild; the backend fix is live now.

## 2026-08-08 - Map-first discovery pass: calm chart, price-only pins, 3-stop yard sheet
- tag: FEATURE | FIX
- area: packages/tokens/map/grade.json, mobile/components/map/{pins,terminal-map,yard-sheet,pin-carousel,filter-bar}.tsx, mobile/components/ui/{sheet,button,text}.tsx, mobile/components/listing/spec-table.tsx, mobile/components/search/listing-row.tsx, mobile/app/{_layout,(tabs)/_layout,(tabs)/index,listing/[id]}.tsx, mobile/lib/listing-title.ts [new], mobile/tailwind.config.js
- summary: Founder on the shipped APK: the map "feels like Google Maps with equipment cards on top" rather than a marketplace that uses a map for discovery. Presentation-only pass — no backend, no contract, no route, no business logic touched. **(1) The chart.** `grade.json` only named 6 Liberty layer groups, so the other ~105 kept Liberty's *light* defaults: `landuse_residential` (`hsla(0,3%,85%,.84)` to z12) was the bright grey landmass, `boundary_2/3` the dashed noise, `highway-shield-*` the white E1/F265/A1 boxes, `waterway_river` the blue lines, and one catch-all painted a village as loudly as a city. Rewrote both themes against the **real** layer ids (fetched and replayed over all 111: **zero now fall through**) — all landcover to ink-850, road hierarchy (motorway ink-600 / minor ink-800), boundaries to 35% ink-700, place labels tiered (city/state/country ink-300, town/village ink-400), street names on a `text-opacity` zoom ramp (invisible below z13, 0.55 by z14.5), shields/POI/airport off. **Found and fixed a latent bug in the OLD grade:** `water_name_POInt_label` contains "poi", so the POI rule had been hiding it by accident. **(2) Pins.** The detailed yard plate (logo │ count │ ≤3 class glyphs over `from ₦68k`, plus the verification seal) rendered *only* when selected — i.e. exactly when the sheet below already showed the same company, count and price. Deleted. One anatomy now, `₦68k │ 5`, selection expressed by frame + elevation alone (no size change, so a tapped pin never re-anchors under the finger). **(3) The bottom surface.** `ui/sheet.tsx` gained peek-88pt/half-50%/full-92% snaps (ch.04 §3) with the scrim only on the half→full leg, so the map stays visible *and pannable* behind it; drag-down collapses one stop at a time and only the lowest stop dismisses. The horizontal rail and its `makeSnapPlanner`/`makeViewabilityHandler` sync are **untouched** — all 13 of their existing tests pass unmodified, which is the regression proof. **(4) Rows.** New `splitListingTitle()` renders "Transit Concrete Mixer 8 m³ — ready-mix delivery" as name + qualifier on two lines instead of truncating mid-word — pure client-side, no API field added (`MapYardListing` carries no `asset_type`). **(5) S6.** Real company name via the existing public `useStorefront` (the payload has only `supplier_id`; the card used to show the listing title's first letter), two-column spec grid replacing the zebra-striped box, day rate promoted to a new `text-money-md` 26px, sticky CTAs now `Request to hire / ₦165,000 per day` and `Enquire / Ask a question first`.
- reason: Founder direction, 2026-08-08 — clarity and hierarchy over decoration; the map answers "where", the marketplace UI answers "what, how much, can I hire it".
- change_ref: 2026-08-08 - Listing photos: 88% smaller, and off the gunicorn workers
- notes: **TWO REAL DEFECTS FOUND WHILE MEASURING, both now fixed.** (a) **`border-border` was never a valid utility.** The token preset defines `colors.border.{default,strong,structural,focus,error}` with no `DEFAULT`, so `border-border` did not resolve and Tailwind's fallback `borderColor.DEFAULT` — **`#e5e7eb`, near-white** — painted every one of those hairlines on a dark ink-900 app. Verified with `resolveConfig`, not assumed. Swept mobile's 25 unresolved usages to `border-border-default`; **`packages/tokens` and the portal's 106 call sites are deliberately untouched** (wave isolation), so **the portal still has this bug and should be swept separately**. (b) **D-028 amber→lime was never applied to the map.** `surface/brand` became acid lime `#E0ED34` and `amber.500` became the *warning* hue, but `pins.tsx`, the tab-bar active tint and `StaticMiniMap` still used amber — the map's most important state was painted "warning", and the S6 privacy circle used a hard-coded `rgba(245,158,11,…)`, the **pre**-D-028 amber, inside a lime ring (the muddy-brown disc in the founder's screenshot). All now `colorPrimary500`. **Also:** added `GestureHandlerRootView` at the app root — there was none anywhere in the tree, and the sheet's `GestureDetector` is now the map's primary interaction (classic cause of dead pan gestures on Android); confined the sheet's drag to the grab area so it stops competing with the inventory ScrollView; added a `Money` size guard mirroring the existing colour guard (two fontSize utilities on one Text resolve by stylesheet order, so `text-money-md` would have silently lost to `text-money`). **Tests: 41 suites / 213 passing** (baseline was 38/184 — 29 added: `lib/map-style.test.ts` replays the grade over sampled real Liberty layers and pins the ordering traps, `lib/listing-title.test.ts`, `test/components/sheet.test.tsx`); tsc clean; eslint 0 errors (4 pre-existing warnings → 2); `expo export` bundles and every new utility class verified generated via the Tailwind CLI. **NOT VERIFIED — needs the physical device:** the sheet drag/tap feel, gesture-vs-scroll behaviour on real Android, and chart legibility at speed. **Spec drift to reconcile:** design-system 06 §3's YardPin anatomy (logo squircle + count badge + class dots + tick) is now superseded on mobile by the price-first plate — fold into the D-028 docs reconciliation. Wave 8 is not signed off, so no frozen surface is broken.
