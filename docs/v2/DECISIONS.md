# Terminal v2 — Decision Log

Running log of founder-approved decisions. Each entry is binding on downstream documents (FSD, TSD, design system) unless explicitly superseded here.

| # | Date | Decision | Detail |
|---|---|---|---|
| D-001 | 2026-06-11 | **Naming: Supplier / Hirer** | Replaces Owner/Renter platform-wide. "Hire" is the product word for a booking. See `01_Product_Definition.md` §1. |
| D-002 | 2026-06-11 | **Service fee = Model A: per-class rates + minimum-fee floor** | Keep the FSD per-class/per-duration rate table (Plant & Machinery 12/10/8 · Trucks & Haulage 11/9/7 · Warehousing, Terminals, Land 10/8/6). Replace the flat-fee-below-₦25,000 rule with: `service_fee = max(rate × hire_value, ₦2,500)`. Eliminates both fee cliffs (the ₦24,999→₦25,000 jump and the reverse cliff where the flat fee exceeded low monthly percentages). Display as "Service fee: 12% (min ₦2,500)". |
| D-003 | 2026-06-11 | **Big-ticket fee taper deferred to Phase 2** | Marginal rate bands / fee cap for high-value hires (anti-leakage economics) to be decided when real payments launch and big-ticket behaviour is observable. Pre-committed as a Phase 2 agenda item, not an open question. |
| D-004 | 2026-06-11 | **No minimum hire value** | The Policy Bible's ₦25,000 minimum hire value is NOT adopted. The ₦2,500 fee floor keeps small hires profitable; long-tail supply (generators, small storage) stays on-platform. |
| D-005 | 2026-06-11 | **Split fees and hirer-side fees rejected for MVP** | Hirer pays the listed price only (supplier bears the service fee), consistent with Policy Bible §4.2. Revisit only at Phase 2 alongside real payment protection. |

| D-006 | 2026-06-11 | **MVP payments: collect-only via Paystack** | Real checkout, 4h payment window, webhook-driven On Hire transition, refunds via API. Supplier payouts manual via Ops Console payout queue (weekly). Deposits deferred to Phase 2. "Simulate integrations, never simulate trust" — OTP real (Termii), ID verification real-but-manual. |
| D-007 | 2026-06-11 | **Launch: both Lagos corridors, class-anchored** | Lekki–Ajah/Mainland anchored by Plant & Machinery + Trucks & Haulage; Apapa–Tin Can anchored by Terminals/Warehousing/Land. Per-corridor supply targets in 03 §8. (Founder chose dual-corridor over single-corridor recommendation.) |
| D-008 | 2026-06-11 | **Duration pricing: best-price rule** | `hire_value = min(daily×d, weekly×ceil(d/7), monthly×ceil(d/30))` over supplier-set rates; winning scheme sets the service-fee tier. Replaces fixed boundary bands; kills the "8 days = 2 weekly" unfairness. |

| D-009 | 2026-06-11 | **API framework: DRF 3.17** (overrides locked Django Ninja) | Decisive: rest_framework_gis for the map-first core; multi-maintainer ecosystem; agent code accuracy. Stack: Django 6.0.x + DRF + simplejwt + drf-spectacular + rest_framework_gis. |
| D-010 | 2026-06-11 | **Tasks: Django 6 native Tasks + django-tasks** (overrides locked Celery+Redis) | Postgres as broker; no Redis anywhere in the stack. Celery reconsidered post-MVP only for heavy fan-out. |
| D-011 | 2026-06-11 | **Realtime: Ably free tier** (overrides locked Pusher) | 200 connections / 6M msgs free; $29 paid cliff vs Pusher's $49 at 100 connections. |
| D-012 | 2026-06-11 | **Hosting: Railway Hobby (~$10–15/mo)** + Cloudflare Workers for the Supplier Portal (Vercel Hobby forbids commercial use) + R2 media + local-Android/EAS-free-iOS builds | Fits the $25/mo cap with ~$10 headroom. Usage cap to be set on Railway. |
| D-013 | 2026-06-11 | **Maps: MapLibre GL + OpenFreeMap tiles + LocationIQ geocoding** (pin-drop-first UX) | Decided by ToS, not price: Mapbox free tier forbids storing temporary-geocoding results (our core write path) and Vercel-style traps. Protomaps-on-R2 documented as tile fallback. Stands per 04 §5 unless founder objects. |

| D-014 | 2026-06-11 | **Service fee is supplier-confidential** | Hirers see only the total they pay — no service-fee or payout figures anywhere on hirer surfaces (request preview, hire detail, receipt, emails). Fee breakdown (Hire Value / Service Fee / You Receive) renders on supplier and Ops surfaces only. ToS discloses the fee model in legal terms. Refines D-005. |

| D-015 | 2026-06-11 | **Cancellation withholdings ARE payouts (FSD §7.6 wins over §3.2)** | Resolves the conflict between FSD §3.2 ("a payout is never created for a hire that did not complete") and §7.6 (hirer cancels ≤72h before start → the withheld day "becomes a supplier payout (due)"). The §7.6 rule stands: a cancellation-withholding payout is the one exception to the completion rule — the supplier is compensated for late cancellation. §3.2 amended to carry the exception. |
| D-016 | 2026-06-11 | **Handover capture works offline (ux/02 S11 wins over TSD §6)** | Resolves the conflict between TSD §6 ("mutations require connectivity") and ux/02 S11 (offline capture, photos queue, record submits on reconnect). Handover is the one allowed offline mutation — it happens in the field at sites with poor signal, and a lost capture damages the dispute-evidence story. TSD §6 amended. |

| D-017 | 2026-06-15 | **MVP payment provider = Bachs.io (collect-only), superseding Paystack in D-006** | Provider changes; the rest of D-006 stands — collect-only, 4h payment window, webhook-driven On-Hire transition, refunds via API, **manual weekly payouts via the Ops queue** (Bachs Payouts API is Limited-Access and pays only to our own destinations — no split/escrow, so manual payout is retained, not a regression). **Integration contract (Wave 4):** base `https://api.bachs.io/v1` / sandbox `https://sandbox-api.bachs.io/v1`; auth `Authorization: Bearer sk_{live,sandbox}_…`; collect via `POST /v1/checkouts` (ad-hoc NGN); **verify-before-transition** via `GET /v1/payments/charges/{charge_id}` (require `status=SUCCEEDED` + matching amount + currency); webhook `collection.succeeded` signed **HMAC-SHA256 hex** over `"{X-Bachs-Timestamp}.{raw_body}"` with a **per-endpoint** secret (≠ API key), 5-min tolerance, dedup on envelope `id` (at-least-once delivery). **Money boundary:** Bachs uses decimal-naira strings (`"75000.00"`); domain stays integer kobo (commandment 2) — convert only inside the Bachs adapter. No monthly fee (per-transaction only) → within the $25/mo cap (D-012). Supersedes the "via Paystack" provider choice in D-006 and the Paystack references in design.md §3/§9 and TSD §6 (amend when Wave 4 builds payments). **Confirm with Bachs before Wave 4 launch:** marketplace settlement model (collect-and-payout vs Connected Accounts), Payouts Limited-Access enablement, exact NGN bank-transfer fee, production rate limit, refund-completion webhook event name, and the one-refund-per-charge constraint vs our refund model. Founder noted Bachs is early-stage (some Beta/"coming-soon" features) — accepted. |

| D-018 | 2026-06-19 | **MVP payment provider switched back to Paystack (collect-only), superseding D-017's Bachs.io** | Founder call: Paystack is the active MVP provider. The rest of D-006/D-017 stands — collect-only, 4h payment window, webhook-driven On-Hire transition, refunds via API, manual weekly payouts via the Ops queue. **Provider is now pluggable** behind `payments.gateway` (`settings.PAYMENT_PROVIDER = paystack \| bachs`); the Bachs adapter is retained, not deleted, so the choice can flip again without code change. **Paystack integration contract:** base `https://api.paystack.co`; auth `Authorization: Bearer sk_{test,live}_…`; collect via `POST /transaction/initialize` (amount in **integer kobo**, `email` required, our `reference`); **verify-before-transition** via `GET /transaction/verify/{reference}` (require `data.status=success` + matching amount + currency); webhook `charge.success` signed **HMAC-SHA512 hex of the raw body with the secret key** (header `x-paystack-signature`, no timestamp), deduped on `(event, reference)`. **Money boundary:** Paystack speaks integer kobo natively — no decimal conversion (simpler than Bachs). Supersedes D-017's provider choice; the kobo/state-machine/§7.6/D-014 domain is unchanged. |

| D-019 | 2026-06-22 | **Supplier Portal UI: bespoke token-driven components (overrides "shadcn/ui restyled" in TSD §5 / CLAUDE.md / wave-7.md)** | Founder call at the Wave 7 start: hand-build the portal component library directly from the design-system chapters (05/06/07), styled **only** from `@terminal/tokens` (no literal hex/px — design.md §5), rather than restyling shadcn/ui. Rationale: maximum fidelity to the "Heavy Duty" language (manifest blocks, stamps, hazard stripes, the supplier/hirer `LockedTerms` split, density-aware DataTable, the CSS-grid CalendarGantt) and a visually distinctive result. Headless a11y primitives (focus-trap, listbox) are hand-rolled by default; an individual unstyled `@radix-ui/react-*` may be pulled in only where a specific behaviour is too costly to hand-build. **No contract impact** (presentation only; frozen API contracts untouched). Supersedes the "shadcn/ui" wording in TSD §5, CLAUDE.md (stack), and `docs/waves/wave-7.md` §7A — amend those when convenient; this entry is binding. |

| D-020 | 2026-07-03 | **Portal headless-primitive allowlist (refines D-019)** | Ratified with the Wave 7 build-plan approval. D-019's "hand-roll by default" stands; the following **unstyled/headless** packages are pre-approved where hand-building the behaviour is disproportionate: `@radix-ui/react-dialog` (focus-trap + scroll-lock + aria for modals), `@radix-ui/react-popover`, `@radix-ui/react-tooltip`, `@radix-ui/react-slot` (composition primitive), `@tanstack/react-table` (headless table state for the P3/P6 DataTables), `@tanstack/react-virtual` (row virtualisation for large fleets). All rendering/styling stays bespoke from `@terminal/tokens` (design.md §5); anything outside this list still needs a new decision. No contract impact. |

| D-021 | 2026-07-03 | **Corner-bracket panel motif (extends design-system 01 §2 M4)** | Founder vision call (wave-7-vision.md): the thin L-shaped corner registration marks — previously **hero imagery only** — may additionally frame **key money/trust panels** in the portal: dashboard stat cards, the P7 LockedTerms panel, and the P11 bank-details vault. Rendered as 1.5px, 12px-arm marks in `ink-200` on light fields (`ink-900 @ 60%` on photography, unchanged). The one-motif-per-screen restraint rule (01 §2) still binds — a screen using corner-bracket panels uses no hazard stripe/stamp/manifest motif except where 01 §2 already allows pairing (receipts). Amends 01 §2 M4's "hero imagery only" scope; the design-system chapters otherwise stand. |

| D-022 | 2026-07-06 | **"Terminal Chart" map style is canonical for both surfaces, defined in `packages/tokens/map`** | Ratified with the Wave 8 vision (wave-8-vision.md V4). The map style both surfaces render is the OpenFreeMap Liberty base (D-013 stands) graded into the Terminal Chart look — paper-50 land / desaturated water / POIs off (light) and ink-900 land / ink-800 water / ink-700 roads / paper labels (dark). The grade is **defined once** in `packages/tokens/map/grade.json` (token-sourced values, both themes); `snapshot-style.mjs` bakes it into full committed `terminal-chart-{light,dark}.json` artifacts when run with network access (tiles.openfreemap.org is blocked from the agent build env — founder/CI step, tracked as a follow-up). The hirer app (Wave 8B) applies the grade at runtime over the fetched Liberty style; the portal migrates from its hard-coded `map-view.tsx` grade to this shared definition post-8B. Closes the Wave 7E "style JSON lands in packages/tokens/map" carry-over. No contract impact (presentation only). |

| D-023 | 2026-07-10 | **Serious instrument aesthetic: plate map markers + restrained motion (amends 06 §3 pin anatomy and the V5/V7 map-feel outcomes)** | Founder call from the Wave 8 device demo: the app must carry a serious, professional tone — "we are not dealing with small children." **Pins:** the class-coloured teardrops and white yard squircles are replaced by squared **industrial plate markers** — fixed ink-900 equipment tags (mono type, amber initials/accents, class hue reduced to a 3px edge strip / 5px index squares, blue verification square, hairline ink-400 border; selected = 2px amber frame, crosshair ring retired). Plates are theme-fixed like the tab shell and PlateLockup. Badge/dim/tick/count semantics from 06 §3 are unchanged. **Motion:** the spring pin drop, first-launch staggered map reveal (V7④ as animation), stamp scale/settle theatrics, handover-tick scale pop, and countdown pulse are removed app-wide — moments keep their haptic + sound and settle by fade/instant; urgency is carried by colour, not motion. Functional transitions (sheet gestures, camera easing) stay. Amends 06 §3 (pin anatomy) and the wave-8-vision V5 "springy chart-instrument pins" / V7① / V15 pulse outcomes; the design-system chapters otherwise stand. No contract impact (presentation only). |

| D-024 | 2026-07-12 | **Supplier manual date-blocks are hard holds on the whole listing** | Founder call (availability/handover/payment batch): suppliers can block date ranges on a listing (maintenance, off-platform commitments) via a new `AvailabilityBlock` model (`hires`, `listing`+`start_date`+`end_date`+`reason`+`created_by`, indexed like the hire overlap hot path). A block behaves as a **hard hold on every unit** for its range — it zeroes public availability and gates `can_confirm`, so request/accept/pay all reject into a blocked range through the existing `state.apply` row-lock re-check (zero new state-machine edges). Blocks never touch existing hires (creating one over a Confirmed hire is allowed; it cannot retro-cancel) and are **hard-deleted** on removal (supplier config, not a transaction record — no audit mandate). MVP is whole-listing; per-unit blocking is a Phase-2 extension. Endpoints (additive): `GET/POST /api/v1/listings/{id}/availability-blocks` (supplier-of-listing only, non-owner → 404; validates ordered dates, ≤365 days, start not in the past → `block_invalid_range`; overlapping own blocks allowed), `DELETE /api/v1/availability-blocks/{id}`. Surfaced on the portal CalendarGantt (striped spans). No frozen-contract impact (new paths only). |
| D-025 | 2026-07-12 | **Handover photos are private-bucket (spec wins over shipped code)** | The Wave-4 code shipped `handover_photo` to the **public** bucket, contradicting FSD §7.4 ("private bucket; visible only to the two parties and Ops"). Founder call: the **spec is binding** — handover evidence (plates, sites, meter readings) is commercially sensitive. The media kind moves to the private bucket; reads are **short-lived (15-min) presigned GETs embedded in `HandoverSerializer.photo_urls`** (same order as the raw `photos` keys), minted only where participants-only access is already enforced (the record 404s a non-party). Reuses the verification-doc presign pattern — no byte-streaming proxy view, no key→ownership oracle endpoint. Both surfaces render `photo_urls` directly (portal local-dev URLs ride the BFF; R2 presigned URLs pass through untouched). A one-shot `migrate_handover_photos` management command moves existing objects public→private (idempotent, verify-then-delete, `--dry-run`). Amends the `core/media.py` bucket and confirms FSD §7.4 / TSD §3.9 wording. No frozen-contract impact (`photo_urls` is an additive response field). |
| D-026 | 2026-07-12 | **Handover photos purge 90 days after off-hire confirmation; record rows kept forever** | No retention policy existed (the TSD's "orphan sweep >7 days" targets only *unattached* keys and was an unimplemented stub). Founder call: handover **photo objects** are deleted from the private store **90 days after the off-hire handover is confirmed**; the `HandoverRecord` rows (kind, reading, timestamps, submitter/confirmer roles) are retained **indefinitely** as the transaction/dispute history. Eligibility: hire `status == completed` (an `in_dispute` hire freezes its evidence) **and** a confirmed off-hire handover ≥90 days old; all of that hire's handover records purge together. Implemented as an idempotent daily django-tasks sweep (`purge_handover_photos` / `run_handover_photo_purge`, marker field `HandoverRecord.photos_purged_at`), per-record failure-isolated. Financial records keep their NDPR 7-year carve-out regardless. Extends the NDPR retention carve-outs (TSD §7 / FSD §11) with a handover-photo window. No frozen-contract impact (`photos_purged_at` is an additive response field). |
| D-027 | 2026-08-07 | **Self-hosting cutover: production moves from Railway + Cloudflare Workers onto the founder's VPS (supersedes the hosting choice in D-012)** | The Django api (gunicorn), django-tasks worker, PostGIS 17-3.5, and the Supplier Portal (Next.js `standalone` on Node 22 — `wrangler.toml`/`@opennextjs/cloudflare` removed) run as one Docker Compose project (`infra/vps/`) bound to loopback behind the host's existing nginx ingress with Let's Encrypt TLS; no container publishes a public port. Production data migrates from Railway via `pg_dump` restore; Railway + the Cloudflare Worker are decommissioned after verification. Periodic jobs (hire sweeps, daily reconciliation, handover-photo purge D-026, weekly digest, NDPR purge) move to `/etc/cron.d/terminal` via `docker compose exec`; nightly `pg_dump` to `/var/backups/terminal` (14-day retention) replaces Railway's implicit backups. **Unchanged:** D-010 (django-tasks, no Redis), D-011 (Ably), D-013 (maps), R2 media, Termii/Resend, and D-018's Paystack collect-only (the webhook URL moves to the VPS host). D-012's $25/mo budget guardrail still binds and total spend drops — Railway/Cloudflare off, VPS is sunk cost. Launch hosts are `terminal-api.lab.perblis.com` + `terminal.lab.perblis.com`; product domains are a documented one-line-per-env switch (infra/vps/README.md). |

| D-028 | 2026-08-07 | **Supplier Portal adopts Infisical's design system — dark-only, acid-lime brand (supersedes the "Heavy Duty" light palette, type scale and density for `portal/`)** | Founder call: the portal's look and feel is ported from Infisical's frontend (`github.com/Infisical/infisical`, `frontend/src/index.css` — **MIT**, Copyright (c) 2022 Infisical Inc.; only their `ee/` tree is proprietary and none of it is used). **What changed:** `packages/tokens/src/primitives.json` values are Infisical's — the `mineshaft`/`bunker`/`chicago` neutrals, the `#E0ED34` acid-lime brand (replacing amber-500, which is demoted to the warning hue), their semantic green/red/yellow/blue, their product accents mapped onto the five asset classes (PKI orange→plant, org blue→trucks, KMS teal→warehouse, secret-sharing violet→terminals, PAM pink→land); a markedly denser type scale (14px body, headings topping out at 20px); tight 2/4/6px radii; 80–260ms timings on their `cubic-bezier(0.16, 1, 0.3, 1)`; 44/32px table rows. **The portal is now dark-only** — `<html data-theme="dark">` is stamped, not toggled. **Token names did not change** — semantic.json never referenced a hue, so this was a value swap, which is exactly what that indirection was built for. **Two deliberate deviations from upstream:** their display face **Alliance No. 2 is a commercial typeface we may not redistribute** — Inter Tight substitutes (body stays Inter, mono becomes JetBrains Mono as upstream); and `text/tertiary` uses an added `ink.350` stop because Infisical's `#707174` muted fails WCAG AA at 3.35:1 on a card, which our contrast gate (13 pairs × 2 themes) refuses. Their logo, wordmark and product naming are **not** copied. **The light token map is retained** (emails, print receipts, the Ops admin layer, and the gate's both-themes assertion) — it is simply never selected by the portal. **New tokens:** `surface/chrome` + `text/on-chrome` for persistent app furniture (nav rail, toasts, wordmark plate), which must stay dark in *both* themes and therefore cannot be `surface/inverse` — inverse legitimately flips to white on a dark ground. **Consequence for the docs:** `docs/v2/08_Design_System.md` and `docs/v2/design-system/` ch.01–04 (brand foundations, colour, typography, layout/density) now describe the *hirer app and paper surfaces* only, and no longer describe the portal; D-021's corner-bracket stroke colours and D-022's map grade are re-pointed at the new ramps. **Reconciling those chapters is an open follow-up, not done here.** No contract impact (presentation only; frozen API contracts untouched). |

| D-029 | 2026-08-10 | **Storefront display copy may call a place a "facility" — a class-aware noun, narrowing 02 §2's rejection of "Facilities"** | Founder call on the S13 restructure: the storefront must read as a marketplace page for hireable places, and a hirer searching for cold storage does not search for "assets". `docs/v2/02_System_Lexicon.md:35` rejected **Facilities** as a *category name* ("Facilities is vague — a warehouse is also a facility") and reassigned that class to **Land & Staging**; that ruling stands, and **Asset remains the umbrella term** (02 §2). This decision narrows the rejection to display copy only, and only where the noun is accurate. **The rule is derived from the classes actually on the page, never assumed** (`mobile/lib/asset-noun.ts`): all of `warehousing` / `terminals_yards` / `land_staging` → "facility · facilities"; all of `plant_machinery` / `trucks_haulage` → "asset · assets"; **mixed, or nothing listed → "listing · listings"**, the Lexicon's own word for a published offer, which is true of every class. The mixed rule is load-bearing, not a corner case: 8 of the 16 seeded storefronts carry more than one class and 5 straddle this split (a laydown yard and a tipper truck on one page cannot share a noun). A card follows the thing it opens rather than the page it sits on, so a cold-store card on a mixed storefront still says "View facility →". **Identifiers, API fields, DB columns, serializer keys and the Lexicon's own tables are unaffected** — this is rendered copy only, and "Supplier"/"Hirer"/"Listing"/"Yard"/"Hire" are untouched. "Seller" is **not** adopted anywhere: it appears nowhere in the repo and Supplier remains the only approved term for that role. **Known inconsistency, deliberately not fixed here:** S4/S5/S12 still render "{n} assets" for every class, so the same three cold rooms read "facilities" on S13 and "assets" on the map — follow through on those surfaces or revisit. No contract impact (presentation only). |

| D-030 | 2026-08-10 | **Storefront listings carry `specs`, `tier` and `available`; the endpoint gains a deterministic order** | A storefront card must answer "is this suitable, is it trustworthy, can I actually have it" without the hirer opening every asset, and none of that was on `GET /api/v1/storefronts/{id}` — which returned seven fields per listing. The app had been reading `GET /listings/{id}` **once per card** to get it (PR #66). Those three keys are now on the storefront payload, and the per-card fan-out becomes a **self-disabling fallback**: the client reads a listing individually only when it arrives without `specs`, so the id list is empty against a current API and the requests disappear. **Why this is not a frozen-contract break:** the endpoint's OpenAPI entry is `'200': description: No response body` (`backend/openapi/schema.yml:1794`) because the view declares `@extend_schema(responses={200: None})` — drf-spectacular has no serializer to introspect, so regenerating the schema produces a **verified zero diff**; no test in the repo asserts an exhaustive key set for any endpoint (`test_storefront_public_shape` is presence-based, and even search's `test_asset_row_shape` uses a `>=` superset assertion); and `specs`/`tier` were **already public** — `ListingSerializer` serves the identical dict to anonymous callers via `GET /listings/{id}` (`AllowAny` for GET), so this is a relocation, not a disclosure. No fee fields exist on `specs`, so D-014 is unaffected. `available` reuses `hires.availability.availability_map` with default (today) semantics — the same question the map and list rows ask — at **two queries for the whole storefront** regardless of listing count, mirroring `search/services/common.py`'s existing use of it. **Also fixes a latent bug at the root:** the Live-listing query ran no `.order_by()` and `Listing.Meta` declares no ordering, so `live_listings` arrived in **Postgres heap order** and could reorder after any row `UPDATE` — the storefront was non-deterministic for every consumer, not just the app. It now orders by `id`, which is UUIDv7 and therefore creation order; the client keeps its own sort so an un-deployed backend still renders stably. Presentation-adjacent only: no schema, migration, pricing, availability-engine, auth or messaging change. |
| D-031 | 2026-08-20 | **Hourly hire is deferred from the launch MVP pending transaction evidence** | A request for an `hourly_price` field is not a rate-only change: a truthful hourly product needs a scheduled start time, hour-level availability, minimum/call-out rules, cancellation/no-show semantics, fee-scheme interaction and handover timing. The launch MVP keeps the existing daily/weekly/monthly transaction model. Research should collect recent completed short-hire jobs by asset type, minimum hours, mobilisation, operator/fuel inclusions, overtime and whether an asset can serve multiple sites in one day. Hourly hire may return as a narrowly scoped, prepaid fixed-duration pilot after the evidence produces repeatable terms; open-ended meter billing is not approved. This decision is a deferral, not a rejection of the market. |
| D-032 | 2026-08-20 | **Make Terminal's existing trust mechanics visible as one transaction proposition** | The map and storefront acquire demand; the reason to complete through Terminal is the transaction layer already built: reviewed identity/business documents, accepted terms locked in the hire record, Paystack-confirmed payment, explicit refund rules, append-only events, private handover evidence and the ability to freeze a supplier payout during a dispute. These mechanics must become visible at the listing decision, request review, checkout, supplier acceptance and active-hire stages instead of appearing as disconnected backend rules. Claims must remain literal: Terminal is not escrow, insurance, an asset-condition guarantee or a guaranteed dispute outcome. No user-facing "protected", "guaranteed", "funds held" or equivalent badge/copy ships until a claims review confirms that the exact wording matches the collect-only model. Contact masking remains a pre-payment control, not the value proposition. Transaction-earned reputation is the long-term anti-leakage incentive, but fabricated demo hires must never contribute to a public completion claim. Implementation sequencing and acceptance gates live in `TRUST_FOUNDATION_VISIBILITY_PLAN.md`. |

## Open items (tracked in task list)
- **MUST DO — structured certification/compliance field on `warehousing`** (founder, 2026-08-10: *"add it to future implementation, a must do, but for now we don't have the capacity"*). Today a cold store can only assert HACCP/NAFDAC compliance as prose in its description; `certifications` is free text and **plant-only**, so nothing about compliance is structured, filterable, or renderable as a capability, and the S13 storefront deliberately shows no such tick rather than inventing one. Founder's stated preference is **Ops-verified, not self-asserted** — consistent with the "trust is earned, not stamped" rule (account badges, listing tiers), which means a claim + review queue, not merely a spec field. **Two blockers found while scoping it, both must be cleared first:** (1) `listings/spec_data.py` has a single module-level `VERSION = 1` and `spec_seed.seed_spec_templates` upserts on `(asset_class, asset_type, version)`, so adding a field **without bumping `VERSION` silently mutates the published template in place** — contradicting that module's own docstring and breaking the portal's `staleTime: Infinity` "immutable per version" cache (`portal/lib/queries.ts`); (2) the portal's `SpecInput` (`portal/components/assets/form-steps.tsx`) renders `kind: "multi"` with a **single native `<select>` writing a string**, while the backend's `validate_specs` requires a list — so a `multi` certification field cannot be filled in correctly until the portal grows a real multi-select. Existing Live listings need no data migration either way: `specs` is stored and served verbatim, and the publish gate only re-checks required fields on a `publish` transition.
- Tasks #1 & #2 resolved by Stage 2 (03_MVP_Scope.md §6–§7 and D-006/D-007/D-008).
- Stage 2 sign-off checklist (`03` §9) — pending founder review.
- Stage 1 / 1b sign-off checklists (`01` §6, `02` §7) — verbally on track ("good, wonderful"); confirm formally with Stage 2 sign-off.
